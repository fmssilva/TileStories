# 2.1.1 — Project Clean-Up (one-time)

Four small, independent, low-risk deletions found during the 2026-08-06
file-by-file audit (see `_2.1_Project_Structure.md` §2). None of these are
referenced by any working code — confirmed before listing them here, not
assumed. Do each one, confirm compile + tests still clean after each,
before moving to the next. No new code, no refactor — this is deletion only.

## 1. Delete the stray `POIAuthor` file

**What:** `Assets/Framework/Editor/POIAuthor` (no extension) and its
`Assets/Framework/Editor/POIAuthor.meta`.

**Why it's safe to delete:** it's a truncated leftover duplicate of the old
single-file `POIAuthoringToolWindow.cs`, from whatever move/rename created
the current `Assets/Framework/Editor/POIAuthoring/` folder split. It has no
`.cs` extension, so Unity does not compile it — it's inert. Confirm by
opening it: it should contain old `POIAuthoringToolWindow` C# source,
truncated partway through. It has its own `.meta`, so Unity is tracking it
as a generic asset (harmless, but confusing clutter sitting in a
shipped-looking Editor folder).

**How:**
1. Open the file, confirm it looks like truncated old
   `POIAuthoringToolWindow.cs` source (not something unrelated that
   happens to share the name prefix).
2. Delete both `POIAuthor` and `POIAuthor.meta` through the Unity Project
   window (not just the OS file explorer) so Unity's AssetDatabase updates
   cleanly.
3. Compile + test (`40-testing.md` §4.2) — this should be a pure no-op,
   since nothing referenced this file.

## 2. Delete the scratch note left in the Editor folder

**What:** `Assets/Framework/Editor/POIOutlineTableColumnFix.txt`.

**Why it's safe to delete:** it's an implementing agent's own working
notes (a "here's the diff to make" scratch file), left behind in the
actual project instead of a private working location. Not compiled, not
referenced by anything, purely a leftover.

**How:** delete via the Unity Project window. Compile + test (should be a
no-op).

## 3. Confirm and delete the stray init scene

**What:** `Assets/InitTestScene<guid>.unity` (a GUID-suffixed `.unity` file
sitting directly in `Assets/` root, not in any of the project's normal
scene locations).

**Why it's likely safe to delete:** the name and location strongly suggest
an accidental/temporary scene (possibly a Unity Test Runner artifact or an
accidental save), not intentional project content.

**How:**
1. Before deleting, open it and confirm it's empty/trivial (not
   accidentally the only copy of real work).
2. Check it isn't referenced in Build Settings' scene list.
3. If both check out, delete via the Unity Project window.
4. Compile + test.

## 4. Delete the empty leftover media folder

**What:** `Assets/Apps/LivingRoom/MediaAssets/Images/marker_symbols/`
(confirmed empty — zero files inside).

**Why it's safe to delete:** leftover scaffolding from before
`Assets/Apps/LivingRoom/MarkerAssets/` (the location actually used for
marker symbol PNGs, per `_5.1_Editor_Tab.md` §7) was settled on. An empty
folder has no build/behaviour impact either way, but it's a second,
confusing "where do marker images go" location sitting next to the real
one.

**How:** confirm it's still empty, delete via the Unity Project window.
Compile + test.

## Checklist

- [ ] `POIAuthor` + `.meta` deleted, compile+test clean.
- [ ] `POIOutlineTableColumnFix.txt` deleted, compile+test clean.
- [ ] Stray `InitTestScene*.unity` confirmed trivial and not in Build
      Settings, then deleted, compile+test clean.
- [ ] Empty `MediaAssets/Images/marker_symbols/` confirmed empty, deleted,
      compile+test clean.
- [ ] `_2.1_Project_Structure.md` §2 updated to remove these four items
      from the "found, not yet fixed" list once done.