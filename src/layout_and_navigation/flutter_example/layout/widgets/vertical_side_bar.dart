// this manages creates a container for the "open" side bar and a chevron and a "close" side bar. so we can open and see the labels or close and see only the icons.

// maybe we can implement a row_item with a icon and a label and then we can use that row_item in the side bar. and then we can use a stack to stack the open side bar and the closed side bar on top of each other and then we can use a boolean to toggle between them.

// in terms of the content of this side bar, should it be nav tabs like the normal header or other things like some action buttons...?? or even customized and different pages can choose the buttons to have??
