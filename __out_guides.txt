
proj_guides\_2.7_Corrections_TODO.md:99:| 20 | **1.2 (rest of the full entry)** | Position system simplified redesign 
— substantial, schedule as its own effort, not bundled. Depends on step 19 for 1.2-b specifically; every other 
sub-item is independent. | [DONE] 2026-09-13 — Position model collapsed from 4 fields (`captured_position`, 
`has_captured_position`, `captured_position_source`, `captured_position_timestamp`) to 2: `PositionData position` 
(nullable) + `bool position_verified` (editor-only QA). x_norm/y_norm and calibration-anchor interpolation removed. 
`POIPositionResolver` uses `poi.position != null` as sole gate; verified flag never consulted at runtime. New POIs get 
initial position written immediately by `SpawnAndFocusNewPoi`. `CapturePositions` is a single unconditional rig→config 
sync. Editor UI: read-only XYZ + Verified toggle + Capture/Clear/Mark-Unverified buttons. JSON configs migrated. All 
687 tests green (623 EditMode + 64 PlayMode). |
proj_guides\_2.7_Corrections_TODO.md:2107:| 1.2-f | Non-destructive Precise→Draft reversion (data preserved, grayed, 
not deleted) + separate explicit destructive reset action | ✅ SUPERSEDED, equivalent built differently — "Mark 
Unverified" (non-destructive flag flip) and "Clear Position" (destructive) are separate buttons in PositionTabs.cs |
proj_guides\_2.7_Corrections_TODO.md:2909:Tool` (Shift+P), `POI Editor ▸ Rig Safety Prompt on Play/Build`,
proj_guides\_2.7_Corrections_TODO.md:2915:- **`Rig Safety Prompt on Play/Build`** — a legitimate, small, checkable
proj_guides\_2.7_Corrections_TODO.md:2967:3. Leave `Rig Safety Prompt on Play/Build` untouched.
proj_guides\_5.1_Editor_Tab.md:55:| `SpecificMarker/POIEditorToolWindow.PositionTabs.cs` | DrawPositionTabs (Position 
foldout): shows POI name + position state. When `poi.position != null` shows read-only XYZ + Verified toggle + Capture 
Position / Clear Position / Mark Unverified buttons. When null shows hint + Capture Position button. |
proj_guides\__cline_simple_task.md:37:and now lets confirm something: currently we still have 3 buttons: capture 
position, clear position, mark unverified. 
proj_guides\__cline_simple_task.md:40:a) why would we want to "clear position" at all? what would be the benefit of 
that? should we just remove that button and the whole logic related with that?? 
proj_guides\__cline_simple_task.md:42:b) and then we have the capture position and the mark unverified buttons. and so 
maybe what really matters is just to have 1 button to say "unverified position" and painted in redish color, and then 
when developer wants so he clicks in it and the button becomes "verified position" in greenish color, and that's it... 
we update the verifieed flag... 


