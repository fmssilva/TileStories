# Command

Ok, so yes lets rename RingPulse to AccentContourPulse.

And about effects, so yes lets have the 4 colums in the hierarchy table: sun: maybe lets simplify things. we don't need to decide by ourselves what is self excluding or not.lets have these 



# Command

## So about the concept of "Hero"... lets remove that. lets not have a "hero vs no hero" levels. Instead lets be able simple levels of hierarchy of markers and we can define as many levels we want and give them the lables we want, example level 1,2,3,4... 

## About the "Size adjustments" - for now lets maybe just forget about that. Lets just make the marker to have a normal "real size". like if we print the marker of the poi to actually paste it in the real wall. and so we define those sizes example 20cm, and then, because we put the uGUI directly in the real world, they will be automatically adjusted. Can we do that? Can we sort of know the scale of the immeersal byte map scale or somethign and so we have a sense of scale and we know how big a 20cm marker should be in the scene/real world??? (and can we know this in editor mode and/or only in play game mode???)... And so can we set up this "real world size of markers"?
And then, maybe we only put in place some "very nearby max size"?? so for example if we put the phone very near the marker, we don't see the whole marker covering up the whole screen, and instead it stays with some max size in the screen?? or maybe we just do some sort of "size reduction from 1m away until 0m distance to camera? so for example from 1m away to infinity we use the simple marker size in uGUI and basically real world. And from 1m to 0m we sort of multiply the size * the distance in order to reduce it? example 20cm * 0.5m = 10cm ?? or what is the simplest way then? 
So lets only apply the "nearby limiting size correction"? Or what things do we already have implemented about this? example only later i read your text about: Distance/screen-space compensation → root Transform.localScale, recomputed continuously in MarkerBillboard.LateUpdate() (exactly where I originally proposed putting it — that part still holds) These two compose multiplicatively without any conflict: final apparent size = level's Symbol.sizeDelta × the per-frame distance-compensation factor on the root. And because the effect components already read target.localScale as a relative baseline rather than assuming any fixed value, nothing about the pulse/sun/accent system needs to change to accommodate either mechanism. This is good news structurally — it means "same marker at every level, just resize" isn't a design goal we have to build toward, it's already mechanically true today; we're just adding a new caller into an already-correct system. The continuous-ratio distance formula we landed on last time (scale = baseSize * (distance/referenceDistance)^compensationRatio, no clamping, no rounding) is unaffected by this correction — it still belongs on the root, still composes the same way. One nice tie-in I found while reading: the work plan's own RQ1 technical claim is that marker rendering is "tested explicitly at 1m, 3m, and 8m viewing distance" — 3m is a natural, already-established default for reference_distance_m, not an arbitrary number we'd be inventing.??? so what is the simplest way to do things now? this things i was talking about only adjust nearby sizes is a good thing to do now to keep things simple... or do already have the system and we should already implement some general scalling to manage between distance and "visual size on screen" of the markers?? Or basically... maybe we can think a bit ahead. and for now we can implement a simple hierarchy definitions things... and later we will think about the "visual scalling" things? and we do latter a complete and compreensive document just to think about these visual adjustments? for now, maybe lets focus only on hierarchy and we do simple marker size put in the real world... and if we already have some adjustment code, so maybe we can leave it as is... and later we can implement some "adjustmnt ratio" value, and we can latter toggle it to see how strong we adjust sizes??? and so for now we can have that constant to be something neutral that don't have any effect on the sizes??? what is the simplest way so we can focus now only on the hierarchy plan? 

## About the whole LOD and "how many markers we show up" in the scene at the same time... lets not think about that now. For now, lets focus on defining a good hierarchy and basically just show all the markers that we have. We'll think about clustering or selection latter. So the whole LOD question, lets not think about that. for now we show all the markers, all the time, at all distances. 

## »» so basically, lets focus now only on the Hierachy plan. lets forget about the size adjustments and the LOD. we'll think about those latter. 


## Editor Window set up 
I also send now attached a document about the "more or less" current state of the Editor Window we have to help a developer set up and configure the "Markers Design" in an easy way. 

Note that we have some global scene options main component where we set up the main options for each domain: the markers symbol and color; the badge; the outline; the available effects... 

and then we have the specific POI markers components where we select for each marker a specific category, badge, outline, etc... 


So read it all and confirm how we have the Editor window set up. 

### Global Scene Hierarchy Component:
And so now about the whole hierarchy thing, lets also add a "Hierarchy" component to the global scene options. 

In this hierarchy component we can have a table where we define each level of hierarchy that we want. Maybe something similar to the tables we already have for the marker, badge and outline components... 

and in this table we add as many hierarchy levels as we want in each row. 

And in terms of colums: 
a) we can have the hierarchy level label (example can be a simple 1,2,3,4...??)
b) then a colum where we select the default size of the marker in the wall (So basically, if we have the markers printed and pasted in the wall in the real world, how big would we want each marker of each level to be? And this measure should be done in cm maybe? or maybe we can even have some toggle on top of that colum where we can set cm or inches??? (if it is easy and not error prone to set up because if it is a bit complex so it is not worth to have that work and we can leave it only in cm...)) (and in terms of what the "size" actually measures or means, what should it be? because i think we have some "outer container" that might vary a lot depending on effects used or labels etc... so maybe lets have this size to be the size of the actual marker symbol. is this a good idea? so the marker is a fixed size and it never change... can we do it like that? or will it be hard to do like this because we cannot have the "child marker symbol" to define the size of the parent container (the outer cntainer that contains all the elements of the marker - marker, backgroudn shape, outline, badge, effects, label...))??? (and in terms of size of marker, what is better to use? width? heigh? area??? diagonal?? what is more simple and intuitive??)... and can we allow the developer to adjust the size of the marker in the editor screen in unity directly and we sync the respective marker size? and vice versa: if we hange the size in the editor tab so we see the real size of the markers in the scene? 
only later i saw your text about: MarkerLayout.cs — and this is the important one. It's a pure function that derives Ring/Badge/Label size and position purely from the Symbol's own sizeDelta. Its own comment says, verbatim: "Every offset is relative to the Symbol's own size, so resizing the Symbol (Inspector, or later at runtime for LOD) re-derives ring/badge/label correctly instead of drifting out of position." Someone already built the resize mechanism this domain needs and left a comment pointing at exactly this future work.... and then: Concrete current sizes on POI_Marker.prefab: outer container (root RectTransform) 0.3 × 0.15 world units, Symbol 0.12 × 0.12, Ring 0.14 × 0.14 (the 1.18× ratio MarkerLayoutProportions describes), Badge 0.043 × 0.043, Label 0.25 × 0.06. »» so don't know if this has something to do with the questions I am no putting? 
b.1) **More Info** - And in each colum of the table, can we have some sort of little "(i)" where the developer can click and it opens some simple floating component or box explaing tht specific colum? example in this case we explain that the measure of the marker should be though like the real size of the marker in the real word in the wall... and then we will adjust its size to the scene adapting to how far or close the marker is to the camera...??? (can we have this info (i) to be easy to click and explain important details??). So notice that in the other current tables we have some simple text boxes explaining the functionality and use of each table... so what is the best approach? would it b clean to have some (i) buttons spread around in the points that might give some doubt to the developer about how to use things, and so the developer can click in the (i) and some floating component opens with a more complete and well organized info of things? so instead of a permanent little text boxes we can have these (i) that the developer can click when he wants??? what is the best approach to have? what is the most common thing in the unity comunity and in the "general UI principlees" ??? 
c) a colum for us to select if we want or not to show the "lable" of the marker in that level. Example in that colum we have a simple drop down witht the options "Show Label" or "NOT show Label" and like that it is clear for the developer the option he is choosing 
d) a set of collums for us to select the effects we want to have in each hierarchy level. And we should be able to select as many effects as we want... so what good and simple UI should we have for that? Maybe a good system might be - we can have groups of effects in different rows grouped by "type" (example some effects might be self excluding, meaning we must choose one or the other... for example maybe we must decide between sun and beacon... while other effects might be possible to combine together, example beacon/sun + pulsing size... ?? so maybe we should have in this case 2 colums for main types of effects? (And so we need to confirm what groups of effects do we actually have in our code that are self excluding? all the self excluding should be grouped in the same colum. the ones that we can combine so we put in different colums so we can select as many as we want)...)
d.1) and also, lets add a "effects colom" for the "Rotate Contour" effect. This rotate contour in truth is not some effect like the others but yes is a direct effect on the outline. So (IF we select to use outline in the global scene Outline componet, we show also this colum for us to select if we want the outline to rotate or not) (so this colum appears dynamically, it only shows if we select to actually use the outline in the above outline component)
d.1) And then in each cell (row*col) maybe we can have a drop down menu where we select the effect that we want (example we have a drop down with the options [sun, beacon, ..., None]) (It is important to always have the None option so it is clear for the developer if he doesn't want to choose no effect of that specific colum). 


### Specific Markers Components:
And so, then, in the Specific Markers components, we currently have there some lines to select these options. and so lets remove all those options from the specific markers coomponents, and instead we will add a line with a drop down for us to choose the "hierarchy level that we want for that specific marker". So like that we have better uniformity in our app, and the whole set up process is faster, and so we define the levels we want to have in the gloobal scene component, and then in each marker we just select the hierarchy level we want. 
And so in the specific markers components: 
1) lets add to the specific markers components the line with the drop down for us to select the hierarchy level we want. 
2) lets remove the lines we currently have in the specific markers components, that now are part of the hierarchy level definition: 
   2.1) the "Is Hero" line » lets remove that... we don't have the concept of hero anymore, instead we have the concept of levels. BUT ALSO, lets confirm what that current "Is Hero" line actually does in code. And i think we have the functionality that we can have some custom marker symbol to be used by the marker instead of using the normal category symbol?? I think we have that. So instead of deleting that functionality lets just clean it and "separate it from other functionalities, so each option stays well isolated and independent. AND SO, lets rename this line from "Is Hero" to "Use Custom Symbol" and then lets add the field for us to add some custom symbol that we want and also in front of it lets have the preview of that symbol (basically similar to what we have already in the marker, badge and outline tables, in the first 3 colums - so we have something similar here to select a custom symbol to the marker if we want - maybe we can have some toggle... and if we click it to enable the custom symbol then the "select and preview symbol" fields show up...??)
   (and all the other functionalities, currently associated with that "Is Hero" line, lets remove it from there... and so basically we select eveerythign now like use label or not in the Hierarchy level )
   » and so, only latter i read your text about this: is_hero (bool) — controls exactly two things: persistent label visibility, and eligibility for pulse/glow/sun/accent effects. It does not touch marker size at all right now. » and so, lets, basically lets redirect all those functionalities of this "boolean" field "is_hero" to the correct and individual place. the effects and label are defined in the hierarchy table... and here in the specific marker we just have a line to "add a custom marker symbol if we want" (the hero_icon_key...). 
   2.2) "Rotate contour" » this line is removed and this "effect" is also selected directly in the hierarchy table in the global scene level
   2.3) "Effects" » we can remove this whole component of effects, and so now in this place we just have the "Hierarchy Level" line with the drop down in front of it for us to choose the level of that marker, and so with that all the effects definitions are set up automatically




### Defaults: 
for us to confirm that everything works, and so we can test evrything very well, lets have some defaults. 

#### Hierarchy levels defaults: 
Example lets set up these levels in the hierarchy definition: 
1 - 20cm - show label - sun circles - pulse - ring pulse - rotate contour
2 - 15cm - NOT show label - sun countours - pulse - no ring pulse - rotate contour
3 - 10cm - NOT show label - simple sun - pulse - no ring pulse - rotate contour
4 - 5cm - NOT show label - beacon - pulse - no ring pulse - rotate contour
5 - 2cm - NOT show label - no "color effect" -  pulse - no ring pulse - rotate contour

about these effects (pulse and ring pulse), we need to confirm:
i) is the pulse and ring pulse self excluding or we can choose both? if we can use both so then we should have 2 colums, one for each of them so we can select one or the other or both, in the respective colum drop down in each row*colum cell
ii) is the ring pulse assciated with the "outline"? So is it some effect that works on the existing outline only? or what is it? If this effect is just to make the outline to pulse, so, like the "Rotate Countour" it should also be a "conditional dynamic" colum to be shown in the hierarchy table ONLY if the outline is enbled in the glbal scene outline component. 


#### Specific POI Markers defaults:
And then in the specific POI Markers, lets define in the config json file in the Living room folder, to have some POIs to each level. 
Example currently we have some 3 sets of Markers. we have Lamp, Painting and Camera - and lets set them up as level 1; 
then we have some markers around each of those central markers, and so lets set: 
religious as level 2
military as level 3
residence as level 4
trade/industry  and infrastructure as level 5

And we only need to set this new data in the config json file in the LivingRoom folder. Then we click in the Load + Copy to StreamingAssets to make a clean copy for the Stream cnfig file (I think the local agent can do this through the editor window and unity MCP... if not, so maybe just make a copy of the file from the living room folder to the stream folder... or if not possible so ask the human to do that.)

