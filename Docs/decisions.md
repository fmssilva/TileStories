# Decisions

## 2026-08-26 — Block 3
- **Minimap tap-target: real fix, not exemption (Decision 3.3).** Split `minimap_dot_size_px` (visual, default 20) from `minimap_dot_tap_target_px` (invisible hit zone, default 44 = WCAG floor), both developer-tunable in the Search & Filter foldout; overlapping zones resolve by nearest-dot-center via one background-level handler. Reason: visual size and touch target are different concerns; A1-exemption would route around accessibility, A2/A3 conflated the two numbers.
- **Displacement tap-target finding documented, not buried.** New test proves displacement never alters a marker's tappable rect (control-vs-displaced size invariance). Measured absolute size of a 6cm tier at ~5m is ~23x12px -- below WCAG independent of displacement; _2_3 explicitly scopes distance compensation out. Any fix is a future product decision.
- **2.6-d resolved: toggle now, wrapper open.** Shipped `search_filter_select_enabled` + single pure gate `WallSession.ShouldWireSelectionResponders`; Editor foldout dims when off. Wrapper-class restructure left explicitly open (breaking migration, consistency-only justification).
- **JSON back-compat trick reused:** additive WallConfigData fields with C# initializers -- JsonUtility fills missing keys from defaults, so LivingRoom config needed zero migration.
- **PlayMode determinism flake confirmed environmental:** RealConfig_DisplacementPipeline_DeterministicAcrossRuns failed once at 1e-3px screen tolerance while editor focus changed Game View resolution; immediate re-run with zero changes was 58/58 green.
- **2.5-g/2.5-h displacement closeout (2026-08-31):** Wired the existing `DisplacementTieBreakStrategy.TryGetPrimaryAnchorLocalIndex` into all three `ComputeOffsets` branches (fixed_axis / candidate_position / force_directed) instead of deleting it or inlining — keeps the resolver thin, makes the comment at L632 true rather than a lie. Hide-fallback (`ResolveLabelsToHide`) was already implemented but never called; fixed the CS0103 by calling it once per `ApplyDisplacement` cycle. String dispatch kept (no enum re-introduction). Full suites green in single runs (EditMode 589/589, PlayMode 64/64), including the two previously-flaky determinism tests — the `committedOffset` hold fix is accepted. Tier-0 test coverage: 9 strategy tests + 7 compute-level across branches + 4 direct hide tests + real-config PlayMode E2E.
- **Comment-claim vs. call-site gap (§4.2.1 instance #4):** An in-code comment asserted "`lower_priority_only` is fully implemented across all three algorithm branches via DisplacementTieBreakStrategy" but zero call sites existed. Caught by project-wide grep, not by tests. Reinforces: trust grep, not comments; write composition test alongside isolated units.

## 2026-09-08 — Block 9 (Sub‑task 9.1 / Add POI button)
- **Add POI button implemented:** Added a “+ Add POI” button at the top of the Specific‑Marker tab (and also when the list is empty) that creates a new `POIData` with a generated GUID, sensible defaults (centered normalized position, default category, no captured position, no status, etc.), wraps the addition in `DrawConfigMutationScope` so undo/history works, and calls `RefreshRigVisuals()` so the new POI appears instantly in the editor rig.
- **Implementation details:** Method `AddNewPoi()` added to `POIAuthoringToolWindow.SpecificMarker.cs`; button rendered via `GUILayout.Button` before the POI loop. Uses existing `DrawConfigMutationScope` for mutation tracking.
- **Tests:** Added `POIAuthoringAddPoiTests.cs` (Tier‑0 EditMode) covering list increment, default field values, and GUID uniqueness. Both tests pass; full EditMode suite 591/591 green.
- **No runtime code changed** — purely editor‑side, keeping the Framework boundary clean.

## 2026-09-08 — Block 9 (Sub‑tasks 9.2–9.4 / Draft-Precise Position tabs)
- **Draft / Precise tabs skeleton implemented:** Created `POIAuthoringToolWindow.PositionTabs.cs` with two tabs inside the Position foldout:
  * **Draft** – shows `x_norm` / `y_norm` sliders + a “Capture Position” button that delegates to existing rig capture logic for the selected POI.
  * **Precise** – shows captured position fields read‑only (world-space X/Y/Z, source, timestamp) + a “Clear Capture” button to revert to Draft.
  * **Validation hook** – when Precise tab is selected but `has_captured_position == false`, an inline warning (“Capture position first – press the Capture button in Draft tab”) is shown.
- **Integration:** Hooked into `DrawSpecificMarkerOptions` via `DrawPositionTabs(poi)` replacing the old `DrawPoiPositionFields`. Uses existing `DrawConfigMutationScope` for undo/history and calls `RefreshRigVisuals()` on capture/clear.
- **Tests:** Added `PositionTabsTests.cs` (Tier‑0 EditMode) covering clear-capture reset and JSON round-trip preservation of captured data. All 3 tests pass; full EditMode suite 594/594 green.
- **Docs:** Added `PositionTabs.cs` to `_5.1_Editor_Tab.md` §1.1 file table.
- **No runtime code changed** — purely editor‑side.

## 2026-09-08 — Block 8 (Task 22 / 2.6-al synonym wiring)
- **Synonym feature completed end-to-end (2.6-al):** `ConfigureWithSynonyms` existed in `POISearchIndex` and was unit-tested but never reached the running app — the canonical §4.2.1 composition gap. Fixed by: (1) `List<SynonymGroup> synonym_groups` field on `WallConfigData` (matches every other taxonomy list; JSON back-compat via C# initializer); (2) `WallSession` now creates + `Build()`s a `POISearchIndex` in `LoadConfigCoroutine` (exposed via `WallSession.SearchIndex`) and calls `ConfigureWithSynonyms` when groups present; (3) Editor "Synonym Groups" foldout in `POIAuthoringToolWindow.SearchFilter.cs` (key + comma-separated synonyms + add/remove); (4) deleted the Editor-only `SearchSynonymGroups.cs` asset + `SearchSynonyms.asset`.
- **Key finding:** `POISearchIndex` was NOT created anywhere in runtime code before this task — only in tests. Task 22 created the runtime instantiation point in WallSession.
- **Test rewritten, not deleted:** `SearchSynonymGroupsTests.cs` retained but its `MakeSynonymAsset` helper (which instantiated the deleted `SearchSynonymGroups` ScriptableObject) replaced with a plain `List<SynonymGroup>` builder — the runtime consumes plain data, not the asset.
- **PlayMode displacement flake confirmed pre-existing, not a regression:** the 5 failing `RealConfig_DisplacementPipeline_*` tests instantiate `WallSession` with `SetActive(false)` so `Awake`/`LoadConfigCoroutine` (my code) never runs there. They are the documented ±1px camera-projection determinism flake (baseline lines 20-23); still need to be made deterministic/isolated-friendly before PlayMode can be called "green".

## 2026-09-08 — Block 8 (Task 23 / doc-hygiene batch)
- **Doc-hygiene batch completed:** 2.5-b stale ApplyCountCap note removed from `_2.5_Marker_Displacement.md`; 2.6-ab `weight_*` notes in `_2.6` already carry "CORRECTION (audit, 2026-08-25)" and are accurate; 5.1-r missing partial files (`LodZoom.cs`, `Displacement.cs`, `SearchFilter.cs`) added to `_5.1_Editor_Tab.md` §1.1 file table; 5.1-z zoom fields (including new `zoom_double_tap_window_s` + `zoom_double_tap_move_tolerance_px`) documented in `_5.1` `LodZoom.cs` description. All four items resolved without code changes.