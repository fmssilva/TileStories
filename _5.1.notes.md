tasks i asked the agent to do in complement of the guide files because things needed to be improved... and now we need to confirm thigns and update the guide files for them to be up to date...

now lets improve somethings in the editor tab:

1. about the initial lines with the config path, streaming path, marker prefab, correction anchor, wall mesh... lets put that inside a container that we can expand and minimize to not ocupy so much space there on top of the tab. 
don't move the buttons to inside the foldout. the buttons stay in the "root".
so the foldout should only have:
Move all the path fields and object references into this foldout:
- Config path field
- Streaming path field
- Marker prefab path field
- Correction anchor object field
- Wall mesh object field



2. in the global scene options, in the marker component: 2.1. we have a line "wall icon library" but that is done automatic yes? so should we keep that line there visible for the developer or should we remove it because it might just be confusing? 2.2. And the line "resources path" is that row also filled automatic? is that information usefull or should we eliminate that row also?? 2.3 about the table (and this is valid for the marker and badge tables..) lets change the order of the colums, lets have the clums: "Category", "Details" (and when we click in details it opens the overflowing component, but it is missing some "X" closing symbol to that floating component, so that we can write some description, and then close it (and the info should be kept "in memory" so when we click in the global button "Save All to JSON that info is also saved), "Symbol", "Preview", (the colum symbol name we can remove that colum... we don't need it... in the specific markers bellow we will just select the category and so the symbol will be attached automatic yes? so lets remove this symbol name which is not needed); then the 2 colums of the colors: and the first colum, lets have a simple square displaying the actuall color that we choose (currently it is a really narrow rectangle, and when we click on it nothing is happening... so lets make it a good square easy to click, and with the appearance that shows that it "affords" to be clicked, and when we click on it it opens the floating component that we can use to choose a new color... it was working before but now is not working...); and then the colum with the name of the choosen color, and lets confirm that these 2 colums are always in sync, so if we pick a color, the color code is updated, and if we write a color code so the color is updated); and finally we have the "remove" colum, and currently it has a "minus sign", but lets change it and show instead a "trash bin" symbol can we do that? 

so make a detailed plan to execute these changes in a clean and organized and well refactored way, remember the guidelines