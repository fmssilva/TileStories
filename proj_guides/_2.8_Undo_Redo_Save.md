»» lets do a full analysis of the project and confirm for all domains all the undo/redo/save features we have??? 


### 1. Undo/redo — verify it, don't refactor it yet
»» ok we'll do this later. now lets focus on making the design domain better and then we'll check the undo/redo
It works, so no cleanup needed on principle. But hand-test this exact matrix before trusting it:

- __Slider drag → Ctrl+Z ×N__ — expect noisy multi-step undo (one snapshot per frame), landing back eventually. Annoying but functional; only worth fixing if it actually bothers you.
- __Add POI → Ctrl+Z__ — config loses the row, but the __rig child stays__ (orphan). Check the sync label goes red.
- __Delete POI → Ctrl+Z__ — config comes back, but the __rig child is gone__ and `RefreshRigVisuals` never respawns children. Same red label.
- __Save → Ctrl+Z__ — history survives save, so you can undo *past* a save point. Arguably correct; just confirm that's what you want.

The pattern: undo restores *config*, never *rig children*. If that desync annoys you in practice, the fix is small and well-scoped — make undo/redo reconcile children (spawn missing, drop orphans) instead of only refreshing visuals. But confirm the behavior first, then I'll implement it. Don't pay for the refactor before you feel the pain.


