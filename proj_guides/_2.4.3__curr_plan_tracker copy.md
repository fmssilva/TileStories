# Work Plan - Marker LOD (_2.4) domain

## DECISIONS & PLAN REVISION (Round 2)
Here are the answers to the questions raised in the retrospective audit:

- ___1_2_POI_Colision_Solver.md not found:__ Reference superseded — the overlap resolver's history and the displacement tiebreak field now live in `_2.5_Marker_Displacement.md`. Update all plan cross-references to point at `_2.5` instead of the missing `_1_2` file. (This domain's displacement_tiebreak field was removed from LodSettings when Displacement domain was created; it now lives in DisplacementSettings.)

- __CanvasGroup on POI_Marker.prefab:__ Confirmed in `_2.3_Marker_Hierarchy.md` row 5 — CanvasGroup already exists on the prefab (added by 2.3's reveal effect). The painting cluster has 6 POIs (1 hero + 5 satellites). Total wall: 18 POIs across 3 spatially-separated clusters (lamp, painting, camera). This is GOOD for testing:
  - Each cluster (6 POIs at ~0.3m spacing) tests the dense-region clustering pipeline
  - 3 clusters at different distances test the LOD band transitions
  - `ClusterPipelineIntegrationTests` already uses the 6-lamp-* POIs — confirmed real tests not mocks

- __Row 1 (WallConfigData.cs schema):__ The `LodSettings` and `LodBandEntry` classes plus the `lod_settings` field exist on disk (WallConfigData.cs lines 356-404). Verified this session. Status: ✅ confirmed. The LivingRoom config.json has no explicit lod_settings block → LODController runs with defaults (enabled, 3 bands, hybrid mode, zoom enabled). This is intentional: the default constructor values are sensible for any wall.

- __Block ordering (5 & 6):__ Reordered as requested:
  - Human tests FIRST (merged with vision checks, since no vision model available)
  - Then vision-specific items as a subset within the human pass
  - Removed separate "BLOCK 5 - VISION TESTS" — it's now a checklist inside the human handover


## DIRECTIVE:

The plan below is organized as follows:
- **Block 4:** Completion documentation — audit, spec status updates, handover guide population, final test pass. (Next active block)
- **Block 5:** Human-in-the-Loop tests — real-device ARFoundation FOV spike + merged vision/human gallery checks on the cluster gallery. (After Block 4)
- **Vision Tests:** Inline as a checklist within Block 5's handover doc, not as a separate block.


## BLOCK 1 - RETROSPECTIVE (COMPLETE THIS SESSION) - verified GREEN, no pending
- 1a POI_Cluster.prefab present + ClusterPrefabTests green
- 1b ARZoomController.cs + ARZoomState.cs present; math GREEN (ARZoomMathTests 18, ARZoomStateTests 8)
- 1c PanelSettings.asset + SafeAreaHelper.cs present (Runtime/UI/Shared)
- 1d ZoomControlView.{cs,uxml,uss} present (Runtime/UI/Zoom), buttons >=44px
- Z1 routing test moved to PlayMode (ARZoomRoutingTests.cs); EditMode 162/0, PlayMode 32/0, 0 error CS
- _2.4 rows 9/9b corrected; 10-structure updated; scratch __* deleted; tracker saved

## BLOCK 2 - EDITOR UI: LodAutoSuggest + LOD/Zoom foldouts [COMPLETE]
(_2.4 rows 5b, 12, 13)
Gate: Block 1 fully GREEN (EditMode 162/0, PlayMode 32/0, 0 error CS) - met.

Decisions (locked in Plan Mode):
- Option 2 partial-file split (new GlobalScene/POIAuthoringToolWindow.LodZoom.cs).
- Expose ALL LodSettings schema params in foldouts (incl. density_radius_px).
- LOD master enabled toggle + surface existing per-feature toggles (safety-escalation, frustum, zoom, show-ui-buttons).
- density_response_mode stays dropdown-only (none = off, no redundant toggle).
- displacement_tiebreak stays OUT (moved to _2.5_Displacement.md).

### 2-a LodAutoSuggest.cs (Editor/POIAuthoring/Shared/)
    [x] WHAT: public static class LodAutoSuggest with public static LodSettings Suggest(int totalPoiCount) per _2.4 section 6.2. outer=min(5,n), middle=min(15,n), clusterMin=max(3,round(n/10)), shrinkStart=max(2,clusterMin/2); exactly 3 explicit bands (2m/-1, 7m/middle, 9999m/outer). Guard n<=0. Sets bands+cluster_min_count+shrink_start_neighbor_count only.
    [x] TESTS (EditMode): Tests/Editor/LodAutoSuggestTests.cs - exact output at n=0,10,18,150; bands.Count==3 every case; clamps clusterMin>=3 / shrinkStart>=2.

### 2-b LOD foldout (_showGlobalLod) + bands table + Suggest button + full scalar set
    [x] WHAT: DrawGlobalLodSection() in new partial GlobalScene/POIAuthoringToolWindow.LodZoom.cs. Master enabled toggle at top; Suggest Values button (= LodAutoSuggest.Suggest(pois.Count) into _config.lod_settings); band table (Max Distance m / Max Visible int "-1 = All" / Details "..." to EntryDetailsPopup / Trash remove / +Add band); scalar fields via shared DrawScalarField + HelpInfoButton.Draw.
    [x] Fields exposed (all from schema): density_response_mode dropdown; density_radius_px; shrink_start_neighbor_count; cluster_min_count; cluster_icon_mode dropdown (gated to cluster|hybrid); density_safety_escalation_enabled toggle; density_safety_escalation_multiplier (gated); hysteresis_margin_m; transition_fade_duration_s; evaluation_interval_s; frustum_culling_enabled toggle; fov_culling_margin_deg (gated). EXCLUDE displacement_tiebreak.
    [x] WHY: Editor-only authoring convenience, zero runtime schema/dependency.

### 2-c Zoom foldout (_showGlobalZoom)
    [x] WHAT: DrawGlobalZoomSection() in same partial. zoom_enabled toggle; zoom_min; zoom_max; zoom_tap_step; zoom_tap_levels; zoom_transition_speed_s; zoom_show_ui_buttons; HelpInfoButton help per field (~ Max Zoom 5x ceiling note).

### 2-d Constants additions (POIAuthoringToolWindow.Constants.cs)
    [x] DensityModeOptions/Labels (none|select_hide|cluster|shrink_and_fade|hybrid); ClusterIconOptions/Labels (pie_and_count|dominant_category|count_only); foldout help-text constants.

### 2-e Foldout plumbing (POIAuthoringToolWindow.cs + POIAuthoringToolWindow.GlobalScene.cs)
    [x] Add [SerializeField] bool _showGlobalLod = true; + _showGlobalZoom = true; in DrawGlobalSceneOptions() after Hierarchy foldout: two new foldouts calling DrawGlobalLodSection()/DrawGlobalZoomSection().

### 2-f Tests: foldout round-trip
    [x] TESTS (EditMode): round-trip lod_settings + zoom fields through Save/Load path; structural presence of new foldout methods.

### FINISHING / DoD (Block 2)
1. refresh_unity(force) -> 0 error CS.
2. run_tests EditMode -> 0 failed (new LodAutoSuggestTests + foldout round-trip).
3. run_tests PlayMode -> 0 failed (no PlayMode change expected).
4. read_console -> 0 error CS / 0 Exception / 0 Failed.
5. Update _2.4_Marker_LOD.md rows 5b/12/13 -> confirmed; checklist items checked.
6. Update .clinerules/10-structure.md: + Shared/LodAutoSuggest.cs, + GlobalScene/POIAuthoringToolWindow.LodZoom.cs, + Tests/Editor/LodAutoSuggestTests.cs.
7. Update this tracker to all [x]; delete __* scratch.

### RESULTS / TECHNICAL DECISIONS LOG
Act B0 (prior baseline): EditMode 127/127, PlayMode 31/31, 0 CS. [Historical.]
Act B1 (prior session): Block 1 build.
Act B1F (prior session): 1a/1b/1c/1d + Z1 green. EditMode 162/0, PlayMode 32/0, 0 CS. Block 1 COMPLETE.
Act B2 (this session): Block 2 - LodAutoSuggest + Editor LOD/Zoom foldouts. (In progress.)
Act B2F (this session): Block 2 - LodAutoSuggest + Editor LOD/Zoom foldouts. COMPLETE. refresh_unity 0 error CS (after .meta fix + indent normalization); EditMode 169/169; PlayMode 32/32; read_console 0 error CS / 0 Exception / 0 Failed. New files: Editor/POIAuthoring/Shared/LodAutoSuggest.cs; Editor/POIAuthoring/GlobalScene/POIAuthoringToolWindow.LodZoom.cs; Tests/Editor/LodAutoSuggestTests.cs + LodAuthoringRoundTripTests.cs. _2.4 rows 5b/12/13 confirmed; 10-structure updated; scratch __* deleted pending.
Act B3 (prior session): Block 3 - Cluster aggregate creation + Phase A gallery.
Act B3F (verified 2026-08-14): Block 3 EMPIRICALLY COMPLETE + GREEN.
  refresh_unity 0 error CS; EditMode 224/224; PlayMode 37/37. New tests confirmed by NAME in results
  (RealConfig_LampFamily_AggregatesIntoOneRealCluster, RealConfig_LampFamily_DeterministicAcrossReRuns,
  ClusterGallery_variants_render_correctly). On-disk audit confirmed WallSession accessors,
  internal ReconcileClusters seam, 3 cluster LodSettings fields (WallConfigData L372-374),
  dominantIcon wired on POI_Cluster.prefab (non-zero fileID + inactive child), Dev/ClusterGallery scene.
  Remaining = Block 3.8 finishing + hygiene only (no functional gaps).
Act B3G (2026-08-14, Block 3.8 finishing): Block 3.8 COMPLETE. 3.8-1 cosmetic indent fixes (5 lines,
  coordinate-based apply_text_edits); 3.8-2 deleted Runtime/DevTools/__fix_constants.py + .meta;
  3.8-3 AssemblyInfo consolidated (root grant fixed to TileStories.Tests.Runtime, Core duplicate deleted);
  3.8-4 _2.4 rows 6/12/14/15 + Corrections section; 3.8-5 10-structure tree now lists all cluster/AssemblyInfo
  entries incl. Editor/DevTools/ClusterPrefabWiring.cs + Dev/ClusterGallery/ block.
  DoD: refresh_unity 0 error CS; EditMode 224/224 (0 failed); PlayMode 37/37 (0 failed); cluster test
  names Passed by name; no __* scratch under Assets/. Block 4 (ClusterIndicator + full-suite re-verification)
  is UNLOCKED but execution waits for the user's next-mode gate per MANDATORY HARD STOP.



## BLOCK 3 - CLUSTER AGGREGATE CREATION + PHASE A GALLERY  [COMPLETE]

### AUDIT SNAPSHOT + VERIFICATION REALITY  (Plan Mode, verified on disk 2026-08-13)

- 3-a WallSession accessors (WallIconLibrary / MarkerSpawnRoot / SpawnedMarkers) ........ DONE
- 3-b SafetyNet.ExceedsThreshold + ShouldAggregate + ComputeTargetDensityState refactor .. DONE
- 3-c POI/ClusterGrouping.cs (pure static, full API on disk) .............................. DONE *zero tests*
- 3-d LODController.ReconcileClusters + 3 LodSettings fields (WallConfigData L372-374) .... DONE *zero tests*
- 3-f code half (MarkerClusterView.dominantIcon field + BuildDominantIcon) ................ DONE
- 3-f prefab half (DominantIcon Image child + m_DominantIcon wiring on POI_Cluster.prefab) MISSING
- 3-e Phase A gallery Dev/ClusterGallery/ ................................................. MISSING
- 3-g Editor LOD foldout for the 3 new cluster params ..................................... MISSING
- CRITICAL: the current EditMode 169/0 + PlayMode 32/0 do NOT exercise clustering - no test
  file calls ReconcileClusters/Grouping/cluster-render. Green != verified for Block 3. 3-2/3-3
  below are the REAL gate. If they fail, debug the cluster lifecycle logic, not the scaffolding.

### 1. DECISIONS - LOCKED (carried from prior plan revision, all preserved)
- A1/I1 - ShouldAggregate (done) + shared SafetyNet.ExceedsThreshold (done): single formula, cannot drift.
- C3/I1 - cluster icon = WallSession.WallIconLibrary (wall-owned, framework-default fallback); MarkerClusterView resolves it.
- D1    - cluster parent root = WallSession.MarkerSpawnRoot (correctionAnchor, fallback transform).
- Dec.3 (RESOLVED) - cluster BAND is developer-selectable, default = centroid-effective-distance;
         PRIORITY = best-member hierarchy, FIXED (not offered).
- Dec.Hys (NEW) - clusters get margin-based band hysteresis (reuse hysteresis_margin_m, default ON)
         + dissolve-grace cycles (default 3, 0=disable) to kill band-boundary and group-pop flicker.
- Non-goals (explicit): ClusterIndicator -> Block 4 (band-overflow badge, same ApplyCountCap family).
         Expand-on-tap -> future Zoom/selection domain (never in _2.4).
- Fixed rule (documented, NOT a selector): cluster priority = best member's hierarchyLevelIndex -
         the ONLY correct semantics per _2.4 section 4 step 5.
- Naming (confirmed): Phase A gallery SPLIT per the real MarkerGallery recipe - the .unity SCENE lives at
         Dev/ClusterGallery/ClusterGalleryScene.unity (NOT Dev/LODGallery/), while the two .cs files
         (ClusterGalleryDefinitions.cs + ClusterGalleryHarness.cs) live in Framework/Runtime/DevTools/ so the
         PlayMode test can reference them. Consistent in 10-structure DoD + _2.4 handover.


### 2. DECISIONS - CONFIRMED THIS PLAN REVISION (PLAN additions)
- D2 Test seam approved: new Assets/Framework/Runtime/AssemblyInfo.cs with BOTH
       [assembly: InternalsVisibleTo("TileStories.Editor.Tests")]  AND
       [assembly: InternalsVisibleTo("TileStories.Tests")]
       (one file, two friend names: EditMode tests live in the .Editor.Tests asmdef; the PlayMode
       companion lives in TileStories.Tests, which already references TileStories). Make
       LODController.ReconcileClusters `internal`; do NOT widen any other member
       (FindReusableClusterView / SweepStaleClusterViews / ShouldAggregate are all reached
       *through* ReconcileClusters, so one seam suffices).
- D3 Doc corrections (spec _2.4 row 6, tracker STATUS, 10-structure) happen at Act FINISH, not now.
- D4 Cluster lifecycle has never run under an assertion - tests are the real gate; on failure
       debug the cluster logic, not the scaffolding.
- D5 (3-5 file split - confirmed by user vs a fresh read of .clinerules/10-structure.md): adopt Option A.
        ClusterGalleryDefinitions.cs + ClusterGalleryHarness.cs -> Assets/Framework/Runtime/DevTools/
        (inside the TileStories Runtime assembly, so TileStories.Tests.Runtime can compile ClusterGalleryTests
        against them); ONLY ClusterGalleryScene.unity lives in Dev/ClusterGallery/. Rationale: no asmdef under
        Dev/, so Dev/*.cs would fall into Assembly-CSharp and be unreachable by the PlayMode test assembly;
        this exactly mirrors the existing, proven MarkerGallery file split. 10-structure DoD already reflects it.
- D6 (cluster member anchoring): mirror MarkerGalleryTests exactly - fabricate each member as a root GO with a
        POIAnchor (POIAnchor.Initialise(POIData{category=...})) + MarkerView, parent the member roots under the
        cluster (or under a shared root that carries the anchors) so BuildCategoryCounts' GetComponentInParent
        lookup resolves each member's category. Harness + test share the same fabrication helper.
- D7 (gallery icon fallback): no WallSession in the Phase A gallery, so ClusterGalleryHarness/ClusterGalleryTests
        pass the framework default Runtime/UI/Markers/IconLibrary.asset as the icon library (exactly how
        MarkerView resolves defaults outside a session); this is the C3/I1 framework-default fallback leg.
- (Procedural) _5.1_Editor_Tab.md typo in __mode.md is fixed by the user; no plan impact.
- "All necessary tests" => guard-path coverage added: _clusterPrefab==null early-return, empty
       aggregatable, single isolated marker, and negative-check that the skip path did NOT run.

### 3-1 Runtime test seam  (was T1; standardises the visibility + friend access)
- WHAT: add [assembly: InternalsVisibleTo] (both friend names) via new
  Assets/Framework/Runtime/AssemblyInfo.cs; change `private void ReconcileClusters`
  -> `internal void ReconcileClusters` in LODController.cs. No behavioural change; no public API
  (internal + InternalsVisibleTo keeps it out of the public contract).
- HOW: one multiline accessor file + one visibility keyword. Mirrors Editor/AssemblyInfo.cs precedent
  exactly (that file grants InternalsVisibleTo to TileStories.Editor.Tests today).
- WHERE: Assets/Framework/Runtime/AssemblyInfo.cs (new); Assets/Framework/Runtime/POI/LODController.cs.
- WHY: lets ClusterReconcileTests instantiate LODController via AddComponent (like
  ClusterPrefabTests uses PrefabUtility) and drive the real pipeline deterministically.
- TESTS: none beyond compile; covered by 3-2/3-3.

### 3-2 Tests/Editor/ClusterGroupingTests.cs  (pure Tier-0, verifies EXISTING 3-c API)  [x] DONE (green-gated: EditMode 196/196, 0 error CS)
- CONTEXT: 3-c POI/ClusterGrouping.cs already exists on disk with the full API below - this step
  writes its missing test file, then VERIFIES it. If any assert fails, fix ClusterGrouping, not the API.
- API UNDER TEST (all pure static, no MonoBehaviour/Camera/scene - screenPos + effective
  distances passed in by the caller):
  * Group(List<VisualUnit> aggregatable, IReadOnlyDictionary<string,Vector2> screenPos, float radiusPx)
      - Connected if screenPos[a]->screenPos[b] sqrMagnitude <= radiusPx^2 (same criterion as EvaluateDensity).
      - Union-find (mirrors MarkerOverlapResolver idiom); deterministic output order (group order by
        lowest poiId, members sorted by poiId) so tests are stable.
  * Centroid(List<VisualUnit> members) - mean of worldPosition.
  * Overlaps(IReadOnlyCollection<string> poolIds, IReadOnlyCollection<string> groupIds) - group-vs-pooled-view
    overlap helper for reuse.
  * Signature(IReadOnlyCollection<string> poiIds) - sorted, joined member-ids; the stable cluster key
    used for hysteresis + dissolve-grace + pool identity.
  * ResolveBand(List<VisualUnit> group, string bandSource, float centroidEffectiveDistance, int defaultBandIndex)
      - bandSource: "centroid" (default) | "nearest_member" | "farthest_member"; unknown/empty -> "centroid"
        (logs once via Debug.LogWarning).
      - PLAN: 'centroid' uses centroidEffectiveDistance; 'nearest_member' uses min(member.effectiveDistance);
        'farthest_member' uses max(...).
      - RETURNS index-only LodBand { Index = <resolved index> } (the caller's FindBand lookups produce
        the full band; this method just picks WHICH effective distance feeds them).
  * BuildAggregate(List<VisualUnit> group, int bestMemberPriority, string bandSource, Vector3 centroid,
      float centroidEffectiveDistance) - returns aggregate unit: marker=null, clusterMembers=group,
      worldPosition=centroid, hierarchyLevelIndex=bestMemberPriority, densityState=Clustered, isVisible=true.
      Band is NOT set here - it is resolved + hysteresis-applied by the caller (LODController) so band
      hysteresis stays caller-owned state, matching AssignBands.
- WHAT (test cases - superset, covers both the 3-c ten-item list and the T2 six-item list):
   1. in-radius merge -> 1 group of 3.   2. beyond-radius split -> 2 groups.   3. exact-boundary (==radius) -> merged.
   2. centroid of 3 == mean worldPosition.   5. singleton -> own group.   6. empty -> empty.
   3. determinism: reordered input -> same structure (sorted by poiId).
   4. Signature sorts + joins consistently (idempotent regardless of input order).
   5. ResolveBand: centroid uses centroidEffectiveDistance; nearest_member uses min(member dist);
      farthest_member uses max; unknown -> centroid + logs once.
  1.  BuildAggregate: marker==null, clusterMembers==group, hierarchyLevelIndex==bestMemberPriority,
      worldPosition==centroid, densityState==Clustered; band left unset.
  2.  Overlaps true on shared id, false on disjoint.
- WHERE: Assets/Framework/Tests/Editor/ClusterGroupingTests.cs.
- WHY: building blocks; a bug here silently corrupts every cluster. Cheapest tier first
  (40-testing 4.2: pure, deterministic, runs across every wall).

### 3-3 Tests/Editor/ClusterReconcileTests.cs + PlayMode companion  [x] DONE (green-gated: EditMode 222/222, PlayMode 34/34, 0 error CS)
- CONTEXT: 3-d LODController.ReconcileClusters already exists + is wired (called in Evaluate() at L126;
  method at L391). It implements the full algorithm below. This step drives it with fabricated state and
  VERIFIES it. If it fails, fix the cluster lifecycle logic.
- ALGORITHM UNDER TEST (already implemented, refs current on disk):
  Step 5c in Evaluate(), between ApplyDensityResponse and ApplyCountCap:
   1. COLLECT aggregates: units where densityState==Clustered && ShouldAggregate(unit, _settings).
   2. PROJECT screen pos: _camera.WorldToScreenPoint(unit.worldPosition) -> Dictionary<string,Vector2>
      (guard z>0, same as EvaluateDensity).
   3. GROUP: ClusterGrouping.Group(aggregatable, screenPos, _settings.density_radius_px) -> groups;
      compute each group's Signature(member ids) + centroid.
   4. CENTROID EFFECTIVE DISTANCE per group = Vector3.Distance(_camera.position, centroid) / zoomFactor
      (zoomFactor = ARZoomState.ZoomFactor, <=0 -> 1, same as ComputeEffectiveDistances).
   5. REUSE OR CREATE (pool _activeClusterViews): find pooled view whose MemberPoiIds overlaps the group
      (ClusterGrouping.Overlaps); if found -> Refresh(memberList, _wallSession?.WallIconLibrary, _settings)
      + reset dissolve-miss; else Instantiate(_clusterPrefab) under MarkerSpawnRoot + Initialize(...) + add
      to pool. view.PositionAt(centroid, _wallSession?.MarkerSpawnRoot).
   6. BAND WITH HYSTERESIS (clusters): bestMemberPriority = min(member.hierarchyLevelIndex);
      rawIndex = ResolveBand(group, _settings.cluster_band_source, centroidEffectiveDistance, 0).Index;
      if cluster_band_hysteresis_enabled: committedIndex = ResolveClusterBandWithHysteresis(signature,
      rawIndex) (new private helper, mirrors AssignBands: demote immediate, promote only after crossing
      hysteresis_margin_m, via caller-owned Dictionary<string,int> _clusterBandIndexCache keyed by
      signature); else committedIndex = rawIndex. fullBand = FindBand(committedIndex) against active bands.
   7. BUILD AGGREGATE VisualUnit: replace absorbed member units in visualUnits with ONE aggregate via
      BuildAggregate(group, bestMemberPriority, _settings.cluster_band_source, centroid,
      centroidEffectiveDistance); then set aggregate.clusterView = view and aggregate.band = fullBand
      (the ONLY two fields BuildAggregate leaves for the caller).
   8. DISSOLVE-GRACE (group-membership smoothing): pooled views not matched this cycle -> increment miss
      (caller-owned Dictionary<view,int> _clusterDissolveMisses); if missCount >=
      cluster_dissolve_grace_cycles (or grace==0 == immediate) -> SetVisible(false, transition_fade_duration_s)
      + remove from active pool + clear its miss entry; else keep visible (stays pooled/active).
- NEW LodSettings fields (already in WallConfigData + LODController):
  * public string cluster_band_source = "centroid";       // centroid | nearest_member | farthest_member
  * public bool   cluster_band_hysteresis_enabled = true;  // reuse hysteresis_margin_m
  * public int    cluster_dissolve_grace_cycles = 3;       // 0 = disable grace (immediate dissolve)
- HOW (thin MonoBehaviour): only Instantiate/pool/projection/zoom + caller-owned state dicts
  (_clusterBandIndexCache, _clusterDissolveMisses); all decision logic in pure ClusterGrouping + existing
  statics (FindBand, SafetyNet, ShouldAggregate). Guards: _clusterPrefab==null -> skip (fall back to hide);
  _camera==null -> skip.
- WHAT (tests): PATTERN = AssetDatabase.LoadAssetAtPath POI_Cluster.prefab + PrefabUtility.InstantiatePrefab
  + mock camera GO + Object.DestroyImmediate in finally (exactly ClusterPrefabTests). MUST assign
  _settings/_camera/_clusterPrefab or the guard silently skips - include a NEGATIVE check that skipping did
  NOT happen (e.g. assert a clusterView was produced).
  * EditMode (1-6) + PlayMode [UnityTest] (7-8; fade needs unfrozen Time):
    1. ReconcileClusters_ProducesSingleAggregateUnit - 3 in-range Clustered (cluster mode) -> 1 aggregate,
       clusterView!=null, clusterMembers.Count==3, worldPosition==centroid, band==FindBand(centroidEff).
    2. absorbed members removed from individual-visibility list.
    3. ShouldAggregate false (select_hide, no safety) -> no aggregate, members hidden outright.
    4. band source centroid vs nearest_member -> different band index for a spread group (fabricated
       member effectiveDistances).
    5. band hysteresis: cluster at band boundary does NOT flicker across repeated ticks (committed index
       stable); disabling cluster_band_hysteresis_enabled allows immediate change.
    6. dissolve grace: group gone < grace keeps view; at grace -> fades out + removed from pool; miss
       counter resets on re-match.
    7. pooled view REUSED next cycle on overlap -> same instance, NOT re-Instantiated.
    8. dissolved group -> canvasGroup.alpha reaches 0 after fade coroutine.
  * GUARD-PATH (new, per D4 "all necessary tests"):
    1.  _clusterPrefab==null -> no crash, stale views still swept.
    2.  empty aggregatable / single isolated marker -> no aggregate, no throw.
- WHERE: Assets/Framework/Tests/Editor/ClusterReconcileTests.cs (EditMode) +
      Assets/Framework/Tests/Runtime/ClusterReconcilePlayModeTests.cs (PlayMode companion - cleaner than an
      in-file [UnityTest]; TileStories.Tests reaches internals via the second friend attribute from 3-1).
- WHY: the missing verification for the whole cluster lifecycle - the real green-gate for 3-c/3-d.

### 3-4 Complete 3-f: POI_Cluster.prefab DominantIcon child + m_DominantIcon wiring  (was T4)
- CONTEXT: MarkerClusterView.BuildDominantIcon (L148-157) null-guards dominantIcon, SetActive(isActive)
  only in dominant_category mode, sprite = iconLibrary.Get(CategoryPalette.ResolveIconKey(category)).
- BUG (root cause, already fixed in code): single-pass wiring persisted fileID:0 because a child created
  with new GameObject() is not part of the prefab asset until AddObjectToAsset embeds it. POI_Cluster.prefab
  L246 still `dominantIcon: {fileID: 0}` -> the two-phase re-persist fix (AssetDatabase.AddObjectToAsset ->
  SetDirty/SaveAssets/Refresh so Unity assigns a REAL fileID -> reload prefab -> re-resolve child by name ->
  prop.objectReferenceValue = now-persisted Image -> ApplyModifiedProperties/Save/Refresh) is ALREADY fully
  implemented in ClusterPrefabWiring.cs L39-96. NO code re-write needed for the fix itself.
- NEW 2026-08-14 (Decision C): ADD an idempotency guard at the top of WireDominantIcon (after mcv resolve):
  reflect/read the persisted 'dominantIcon' value; if it is non-null with a real object reference
  (fileID != 0), Debug.Log("[3-4] already wired (fileID:X), skipping") and return early. Prevents a
  misleading second OK line and any re-parent/re-save of a live reference; honors the tool's own
  documented "idempotent" contract + 20-code-quality (no speculative code - the guard is finishing an
  existing promise).
- HOW:
  1. apply the 5-line guard edit to WireDominantIcon (assert-anchored; it reads the persisted serialized
     field, not the in-memory one).
  2. refresh_unity(force) -> 0 error CS; read_console -> 0 error/Exception.
  3. execute_menu_item "TileStories/Cluster/Wire DominantIcon" -> expect "[3-4] OK: DominantIcon child=...
     wired to dominantIcon" (or "[3-4] already wired" on re-run).
  4. VERIFY persistence (evidence, not the log): select-strings POI_Cluster.prefab -> `dominantIcon:
     {fileID: <non-zero>}` AND a `m_Name: DominantIcon` child with `m_IsActive: 0` (inactive in asset so
     pie/count_only never leak it; BuildDominantIcon flips it active only for dominant_category).
  5. Add 2 ClusterPrefabTests (EditMode, appended to existing file): DominantIcon_child_is_inactive_in_asset
     (mcv.DominantIcon != null && activeSelf == false before any Initialize) and
     DominantIcon_only_active_in_dominant_category_mode (Initialize dominant_category -> activeSelf true +
     expected sprite; count_only / pie_and_count -> activeSelf false).
- WHERE: Assets/Framework/Editor/DevTools/ClusterPrefabWiring.cs (guard edit) +
  Assets/Framework/Runtime/UI/Markers/POI_Cluster.prefab (asset mutation via the menu item) +
  Assets/Framework/Tests/Editor/ClusterPrefabTests.cs (append 2 tests).
- WHY: without non-zero m_DominantIcon, dominant_category has no icon and 3-6 assertion #4 + 3-6b fail.
  Guard is cheap and closes the idempotency contract already promised in the file's header comment.
- TESTS (EditMode): 2 new ClusterPrefabTests. PlayMode unaffected until 3-6/3-6b.
- FINISHING: _2.4 row 6 note "DominantIcon child + m_DominantIcon wired [ok] + idempotency guard" after verify.

### 3-5 Phase A gallery scene  (was T5 / 3-e)   <-- definitions + harness ALREADY DONE on disk
- WHAT (now only the SCENE is missing): ClusterGalleryDefinitions.cs is already clean on disk (4sp indent,
  'religious' override has icon_key="temple", stale comment already rewritten to temple->IconTemple); the
  artwork note says icon_key "temple" -> CategoryPalette.ResolveIconKey -> SpriteKeyLibrary.Get("temple") =
  IconTemple, verified present in IconLibrary.asset. ClusterGalleryHarness.cs (143 lines) already compiles
  (0 error CS) and drives the IO via Start() = CategoryPalette.Configure(Overrides) + Populate().
  So 3-5 reduces to: verify definitions+harness still compile, then CREATE the gallery scene.
- HOW (Decision B: create via Unity MCP, not hand-authored YAML):
  1. refresh_unity(force) -> 0 error CS (confirms definitions+harness compile and demo no drift).
  2. Unity MCP manage_scene create Assets/Dev/ClusterGallery/ClusterGalleryScene.unity (folder auto-created).
  3. manage_gameobject create a GameObject "ClusterGalleryHarness"; manage_components add ClusterGalleryHarness
     (fields left null -> Start() resolves POI_Cluster.prefab, POI_Marker.prefab, IconLibrary.asset via
     AssetDatabase at the hardcoded Runtime paths, matching the MarkerGallery precedent).
  4. create a Main Camera (tag "MainCamera", position to frame the flat cluster plane ~ (0,0,-6)); the
     harness parents clusters under its own transform in a 3-column grid; MarkerClusterView.Awake reads
     Camera.main.
  5. Scene excluded from Build Settings (Phase-A harness, 10-structure Dev/).
- WHERE: Assets/Dev/ClusterGallery/ClusterGalleryScene.unity (+ .meta). (Definitions + harness live in
  Framework/Runtime/DevTools/, NOT Dev/ -- the scene is the Dev-side consumer.)
- WHY: the visual Phase-A surface for manual inspection + pre-wired scene for the Block 5 Vision Tests and
  Block 6 human pass. 10-structure + _2.4 row 14 both require it.
- TESTS: refresh_unity 0 error CS; PlayMode suites (3-6/3-6b) assert the same data independently of the scene.
- FINISHING: _2.4 row 14 -> "Phase A done (definitions + harness + scene)".


### 3-6 Tests/Runtime/ClusterGalleryTests.cs  (PlayMode, Phase A render-fidelity)  (was T6)
- WHAT: PlayMode [UnityTest] suite on the shared ClusterGalleryDefinitions.Entries list (the fabricated-data
  variant of the renderer). Proves the REAL prefab + REAL MarkerClusterView code draws correctly for each
  icon-mode variant. Assertions:
  1. count label == "+N" (TextMeshProUGUI, reflection on field "countLabel") for all modes.
  2. pie_and_count: slice count == distinct CategoryCount count; each slice Image.fillAmount ==
     categoryCount/total (BuildPie sorts count-desc then key-asc, L177/L191; mirror that sort).
  3. size scaling: ScaleByMemberSize (L225-234) -> 12-member cluster RectTransform.sizeDelta.x >
     2-member. (Range 48..112px via MinSizePx/MaxSizePx/SizePerMember consts - these are intentionally NOT
     config fields; see Part 2 "Editor-selection note".)
  4. dominant_category: no pie slices; mcv.DominantIcon != null && activeSelf == true && sprite != null
     ('religious'->icon_key "temple"->IconTemple chain). REQUIRES 3-4 wired.
  5. count_only: no pie slices; "+N" only.
  6. tap target: cluster root RectTransform sizeDelta.x/y >= 44px (MinSizePx = 48).
- HOW: OneTimeSetUp loads POI_Cluster + POI_Marker prefabs + IconLibrary.asset via AssetDatabase (Runtime
  test asmdef allows UnityEditor - MarkerGalleryTests precedent) and creates a Camera (MarkerClusterView.Awake
  reads Camera.main). Spawn = Object.Instantiate(prefab); fabricate members = instantiate POI_Marker,
  POIAnchor.Initialise(POIData{id,name,category}), MarkerView.Initialise(anchor, OutlineGold, Circle, None).
  Reflect private _slices / countLabel. CategoryPalette.Configure(Definitions.Overrides) in SetUp.
  IMPORTANT (Decision A): 3-6 stays the FABRICATED-DATA render-fidelity seam. It does NOT duplicate 3-6b's
  real-config job; the two cover different seams (render-variant fidelity vs. real-data integration) and are
  deliberately non-overlapping to avoid the "mock that confirms nothing" drift.
- WHERE: Assets/Framework/Tests/Runtime/ClusterGalleryTests.cs (create).
- WHY: the automated Phase-A gate for the cluster visual variants (spec 4.4/4.5 Tier 0/0.5).
- FINISHING: _2.4 row 14 -> Phase A done after green (with 3-5 scene).

### 3-6 Tests/Runtime/ClusterGalleryTests.cs  (PlayMode, Phase A render-fidelity)  (was T6)
- WHAT: PlayMode [UnityTest] suite on the shared ClusterGalleryDefinitions.Entries list (the fabricated-data
  variant of the renderer). Proves the REAL prefab + REAL MarkerClusterView code draws correctly for each
  icon-mode variant. Assertions:
  1. count label == "+N" (TextMeshProUGUI, reflection on field "countLabel") for all modes.
  2. pie_and_count: slice count == distinct CategoryCount count; each slice Image.fillAmount ==
     categoryCount/total (BuildPie sorts count-desc then key-asc, L177/L191; mirror that sort).
  3. size scaling: ScaleByMemberSize (L225-234) -> 12-member cluster RectTransform.sizeDelta.x >
     2-member. (Range 48..112px via MinSizePx/MaxSizePx/SizePerMember consts - these are intentionally NOT
     config fields; see Part 2 "Editor-selection note".)
  4. dominant_category: no pie slices; mcv.DominantIcon != null && activeSelf == true && sprite != null
     ('religious'->icon_key "temple"->IconTemple chain). REQUIRES 3-4 wired.
  5. count_only: no pie slices; "+N" only.
  6. tap target: cluster root RectTransform sizeDelta.x/y >= 44px (MinSizePx = 48).
- HOW: OneTimeSetUp loads POI_Cluster + POI_Marker prefabs + IconLibrary.asset via AssetDatabase (Runtime
  test asmdef allows UnityEditor - MarkerGalleryTests precedent) and creates a Camera (MarkerClusterView.Awake
  reads Camera.main). Spawn = Object.Instantiate(prefab); fabricate members = instantiate POI_Marker,
  POIAnchor.Initialise(POIData{id,name,category}), MarkerView.Initialise(anchor, OutlineGold, Circle, None).
  Reflect private _slices / countLabel. CategoryPalette.Configure(Definitions.Overrides) in SetUp.
  IMPORTANT (Decision A): 3-6 stays the FABRICATED-DATA render-fidelity seam. It does NOT duplicate 3-6b's
  real-config job; the two cover different seams (render-variant fidelity vs. real-data integration) and are
  deliberately non-overlapping to avoid the "mock that confirms nothing" drift.
- WHERE: Assets/Framework/Tests/Runtime/ClusterGalleryTests.cs (create).
- WHY: the automated Phase-A gate for the cluster visual variants (spec 4.4/4.5 Tier 0/0.5).
- FINISHING: _2.4 row 14 -> Phase A done after green (with 3-5 scene).


### 3-6b Tests/Runtime/ClusterPipelineIntegrationTests.cs (PlayMode, Phase B REAL pipeline)  [x] DONE
   Verified green 2026-08-14: PlayMode 37/37, both test names confirmed in results
   (RealConfig_LampFamily_AggregatesIntoOneRealCluster + RealConfig_LampFamily_DeterministicAcrossReRuns).
   File exists at expected path, full 320-line implementation with real LivingRoom config.
- WHAT: a REAL end-to-end test that drives the actual LODController decision chain on REAL
  data -- zero fabricated VisualUnit/density state. LivingRoom config has three real
  co-located POI families (lamp_* / painting_* / camera_*, each 6 POIs within ~0.5m). Use
  the lamp_* family. Assertions:
  1. Load StreamingAssets/LivingRoom/config.json (real loader) + CategoryPalette.Configure(real category_styles).
  2. Spawn the 6 real POI_Marker.prefab lamb markers at REAL resolved positions via real
     POIPositionResolver.TryResolvePosition(real captured_position); POIAnchor.Initialise +
     MarkerView.Initialise.
  3. Reflection harness: _settings(density_response_mode=cluster, cluster_icon_mode=pie_and_count,
     real bands, cluster_min_count=2), _camera at a standoff facing the lamp family so all 6
     project within density_radius_px, _clusterPrefab; _wallSession null.
  4. Reproduce Evaluate() stage order (Option 2): run ApplyDensityResponse TWICE (real 2-cycle
     hysteresis commits Clustered), then ReconcileClusters. NO fabricated VisualUnit state.
  5. Assert: one aggregate of the 6 real lamp ids; clusterMembers.Count==6; CategoryCounts has the
     6 real categories (royal_government/religious/military/residential/economic/infrastructure)
     each ==1 and sum==6; pie 6 slices; count label "+6"; absorbed markers hidden (alpha 0).
  6. Determinism: 2 cycles -> same signature + same group order + no duplicate aggregate.
- HOW (Opt 2, no mock): drive the public stages in Evaluate()'s order (FrustumCull?
   ComputeEffectiveDistances/AssignBands/EvaluateDensity/ApplyDensityResponse/ReconcileClusters/
   ApplyCountCap/ApplyVisibility), not the Update timer and not hand-built Clustered units. The
   real density strategy + CommitDensityState decide Clustered; ReconcileClusters aggregates it.
- WHERE: Assets/Framework/Tests/Runtime/ClusterPipelineIntegrationTests.cs (create), namespace
  TileStories.Tests, [UnityTest]. Assembly TileStories.Tests (PlayMode); prefab load #if UNITY_EDITOR.
- WHY: closes the config->render seam in an AUTOMATED Tier-0 form using the REAL pipeline + REAL
  dense family, so Block 6 (Human-in-the-Loop) owns only device-only checks.
- TESTS (PlayMode): 0 failed; assert names appear (RealConfig_Spawn_Reconcile_ProducesAggregate,
  RealConfig_DeterministicAcrossCycles). PlayMode total grows 35 -> ~37.
- FINISHING: _2.4 row 14 "Phase A + Phase B done"; tracker 3-6b -> [x]; then run Block 3 DoD.


### 3-7 Verify (already done on disk — confirm green, no edits expected)

- __WHAT:__ confirm Constants L30 is 8-space and the extended `LodAuthoringRoundTripTests` passes.
- __HOW:__ no code edits; run EditMode suite and assert the cluster-field round-trip test names pass.
- __FINISHING:__ `_2.4` row 12 → "3 new cluster params exposed".

---


## BLOCK 3.8 - FINISHING + HYGIENE for already-green Block 3   [COMPLETE]
Gate: EditMode 224/224, PlayMode 37/37, 0 error CS (VERIFIED this session - see Act B3F above).
This block performs ONLY the finishing checklist + hygiene debt flagged in the audit.
NO new feature logic. If any re-run after hygiene regresses, fix Block 3, not this.

### 3.8-1 Cosmetic indentation fixes (20-code-quality, whitespace-only, compile-fine today)  [x] DONE
- WHAT: normalize over-indented lines to the convention (method body 12sp, inside foreach 16sp).
- WHERE / exact edits (assert-anchored, verify by re-read):
  a. Framework/Tests/Runtime/ClusterGalleryTests.cs L109: `[UnityTest]` 16sp -> 12sp.
  b. Framework/Tests/Runtime/ClusterGalleryTests.cs (comment just above "6.5" assert): align to 16sp.
  c. Framework/Tests/Runtime/ClusterPipelineIntegrationTests.cs L124: `if (go) DestroyImmediate(go);` -> 16sp.
  d. Framework/Tests/Runtime/ClusterPipelineIntegrationTests.cs L192 (comment): -> 16sp.
  e. Framework/Tests/Runtime/ClusterPipelineIntegrationTests.cs L299: `private static T GetPrivate<T>...` -> 12sp.
- HOW: read region verbatim, single assert-anchored replacement per line, re-read. Do NOT use the editor
  tool's first-line replacement (it doubles indentation - the exact bug that caused these); prefer
  apply_text_edits with explicit coords or edit_file.py with __old/__new.
- TESTS: re-run EditMode + PlayMode (a code line changed, so re-run both). 224/224 + 37/37 expected.
- FINISHING: mark [x].

### 3.8-2 Delete leftover scratch file  [x] DONE
- WHAT: remove Runtime/DevTools/__fix_constants.py AND __fix_constants.py.meta (temp-file hygiene).
- WHERE: Assets/Framework/Runtime/DevTools/__fix_constants.py{,.meta}.
- HOW: delete both; refresh_unity so AssetDatabase drops the .meta reference if any (there is none - it was
  a scratch helper; verify no code imports it).
- TESTS: refresh_unity 0 error CS.
- FINISHING: mark [x].

### 3.8-3 Consolidate AssemblyInfo — single seam at Runtime root (per 10-structure + 20-code-quality)  [x] DONE
- WHAT: fix the root Runtime/AssemblyInfo.cs grant name, delete the duplicate Core/AssemblyInfo.cs.
- DETAILS: Two AssemblyInfo.cs files exist in the same assembly (TileStories.asmdef):
  - Runtime/Core/AssemblyInfo.cs — grants InternalsVisibleTo("TileStories.Tests.Runtime") [CORRECT PlayMode name]
    + InternalsVisibleTo("TileStories.Editor.Tests") [correct EditMode name]. But this file sits inside a
    feature domain folder (Core/), violating 10-structure/20-code-quality's "self-documenting" principle.
  - Runtime/AssemblyInfo.cs (root) — grants InternalsVisibleTo("TileStories.Editor.Tests") [ok] +
    InternalsVisibleTo("TileStories.Tests") [WRONG — no assembly with this name exists; the real PlayMode
    asmdef is "TileStories.Tests.Runtime"]. This violates 20-code-quality "no speculative code."
- CHOSEN (per 10-structure/20-code-quality analysis):
  1. Fix the root file: change `"TileStories.Tests"` → `"TileStories.Tests.Runtime"`.
  2. Delete Core/AssemblyInfo.cs (the duplicate in the wrong location).
  3. Result: single seam at the NATURAL assembly root — matching how Editor/AssemblyInfo.cs works.
- WHERE:
  a. Assets/Framework/Runtime/AssemblyInfo.cs — change line 10: `"TileStories.Tests"` → `"TileStories.Tests.Runtime"`
  b. Delete Assets/Framework/Runtime/Core/AssemblyInfo.cs
- TESTS: refresh_unity(force) 0 error CS; EditMode 224/224; PlayMode 37/37 (internal seam still resolves
  through the fixed root grant; this is a net-zero behavior change).
- FINISHING: mark [x]; add a "corrections" note in _2.4 spec.


### 3.8-4 Update _2.4_Marker_LOD.md implementation-status rows (evidence-grounded)  [x] DONE
- row 6 note  -> "DominantIcon child + m_DominantIcon wired [ok] (fileID non-zero) + idempotency guard".
- row 12 note -> "3 new cluster params exposed in LOD foldout".
- row 14      -> "Phase A + Phase B done (ClusterGallery gallery + ClusterPipelineIntegrationTests)"; add a
                 one-line note with the PlayMode 37/37 evidence + the two test names.
- rows 5/5b/5c/7/16 already [ok] - leave unchanged.
- Add a "corrections" note in the spec (20-code-quality/60-finishing §6.2 format) about the AssemblyInfo
  consolidation + the cosmetic indent fixes so future readers know they were applied.

### 3.8-5 Update .clinerules/10-structure.md tree (grep-confirmed missing entries, one sentence each)  [x] DONE

Add entries under the existing tree nodes:

Under `Assets/Framework/Runtime/POI/` (after ClusterGrouping.cs, adding a separate `AssemblyInfo.cs` entry):
- Runtime/AssemblyInfo.cs (root)       ← Grants InternalsVisibleTo to the two test assemblies so internal
                                          seams (ReconcileClusters) are testable without widening the public
                                          contract. Single seam at assembly root (consistent with Editor/ pattern).

Under `Assets/Framework/Runtime/DevTools/` (add two entries alongside MarkerGallery*):
  ├── ClusterGalleryDefinitions.cs    ← Defines `ClusterGalleryEntry` (icon_mode + member_count + label) and
  │                                      the entry list driving both the gallery scene and ClusterGalleryTests.
  └── ClusterGalleryHarness.cs        ← MonoBehaviour that instantiates and configures every ClusterGalleryEntry
                                         in the ClusterGalleryScene; also used by ClusterGalleryTests in PlayMode.

Under `Assets/Framework/Editor/DevTools/` (new folder entry):
  ├── DevTools/                       ← Editor-only prefab-wiring tools (not in Runtime assembly).
  │   └── ClusterPrefabWiring.cs      ← Idempotent menu-item (TileStories/Cluster/Wire DominantIcon) that
                                         authors the DominantIcon child + wires the serialized reference on
                                         POI_Cluster.prefab. Kept separate from Runtime for clear boundary.

Under `Assets/Dev/` (new ClusterGallery subfolder alongside MarkerGallery):
  ├── ClusterGallery/                 ← Scene + assets for the cluster gallery dev tool.
  │   ├── ClusterGalleryScene.unity   ← Scene that ClusterGalleryHarness populates with every entry from
  │   │                                  ClusterGalleryDefinitions; used for visual inspection and PlayMode tests.
  │   └── (shared backdrop.png reused from MarkerGallery/)

Under `Assets/Framework/Tests/Editor/` (add after existing LodAuthoringRoundTripTests line):
  ├── ClusterPrefabTests.cs           ← EditMode contract tests for POI_Cluster prefab: required children,
  │                                      DefaultIconImage, CountLabel, DominantIcon child + dominantIcon ref.
  ├── LODControllerTests.cs           ← EditMode unit tests for LODController pipeline stages (frustum cull,
                                         effective distance, band assignment, density, cluster reconciliation).

Under `Assets/Framework/Tests/Runtime/` (add after existing ClusterReconcilePlayModeTests line):
  ├── ClusterGalleryTests.cs          ← PlayMode Phase-A test: instantiates every ClusterGalleryDefinitions entry
  │                                      on the real POI_Cluster prefab, asserts size/count/pie-slice/icon-mode.


### 3.8-6 Update this tracker + delete scratch  [x] DONE
- Change BLOCK 3 header to [COMPLETE]; verify all 3-2/3-3/3-6/3-6b marked [x]; write Act B3F log entry.
- Delete any remaining __* files (done in 3.8-2).
- ONCE 3.8-1..3.8-6 are all green, unlock Block 4 (Full-suite re-verification + doc hygiene). Do not detail
  Block 4 past this line until Block 3.8 is confirmed green.

### FINISHING / DoD (Block 3.8)
[x] 1. refresh_unity(force) -> 0 error CS.
[x] 2. EditMode run_tests -> 0 failed (expect 224/224).
[x] 3. PlayMode run_tests -> 0 failed (expect 37/37); assert cluster test NAMES still appear.
[x] 4. read_console -> 0 error CS / 0 Exception / 0 Failed.
[x] 5. Verify no non-whitespace logic changed by 3.8-1 (diff), and that 3.8-3 kept internal seam working.
[x] 6. Update _2.4 rows + 10-structure + tracker per 3.8-4/3.8-5/3.8-6.


## [Block 4] Completion Documentation + Handover Guides   [COMPLETE]
Gate: Block 3 fully GREEN — CONFIRMED (EditMode 224/224, PlayMode 37/37, 0 errors)

### 4.1 Audit .clinerules/10-structure.md for accuracy  [x] DONE

- **WHAT:** Cross-reference the 10-structure.md tree against actual disk contents for ALL files created/modified in the LOD domain. Verify every entry correctly describes what the file does in one sentence. Flag missing entries and phantom entries.
- **WHERE:** `.clinerules/10-structure.md` — focus on: Runtime/POI/ (LODController, ClusterGrouping), Runtime/Tracking/ (ARZoomState, ARZoomController), Runtime/UI/Shared/ (SafeAreaHelper, PanelSettings.asset), Runtime/UI/Zoom/ (ZoomControlView), Runtime/UI/Markers/ (MarkerClusterView), Runtime/DevTools/ (ClusterGalleryDefinitions, ClusterGalleryHarness), Editor/DevTools/ (ClusterPrefabWiring), Editor/POIAuthoring/GlobalScene/ (LodZoom partial), Editor/POIAuthoring/Shared/ (LodAutoSuggest), Tests/Editor/ (ClusterGroupingTests, ClusterReconcileTests, ClusterPrefabTests, LODControllerTests, LodAuthoringRoundTripTests, LodAutoSuggestTests), Tests/Runtime/ (ClusterGalleryTests, ClusterReconcilePlayModeTests, ClusterPipelineIntegrationTests), Dev/ClusterGallery/ (ClusterGalleryScene.unity)
- **HOW:** Verify each file physically exists on disk (confirmed in this session's retrospective audit). For each, check the one-sentence description is accurate.
- **Key entry to add:** `MarkerClusterView.cs` in `UI/Markers/` — "Dense-region aggregate marker: pie chart + count label + level-mix rendering, instantiated by LODController.ReconcileClusters."
- **Key entry to update:** `LODController.cs` in `POI/` — the existing brief entry should mention the full 7-step pipeline (frustum cull, effective distance, band hysteresis, density evaluation, density response, count-cap, visibility), cluster reconciliation, and zoom integration.
- **WHY:** Per 60-finishing §6.3, the structure guide must reflect the actual file layout continuously.
- **FINISHING:** Confirm no missing entries, no phantom entries, no description drift.
- **DONE:** Verified. Added `ARZoomController.cs` to Tracking/; added entire `UI/Zoom/` section (3 files); added 3 missing EditMode tests (ARZoomMathTests, ARZoomStateTests, ZoomControlViewEditModeTests); split merged `LivingRoomConfigIntegrationTests.cs`/`MarkerViewRuntimeTests.cs` entry into two; fixed LivingRoom config.json from "20 POIs" to "18 POIs"; resolved a stray `>>>>>>>` git conflict marker at line 209.

### 4.2 Update _2.4_Marker_LOD.md Implementation Status (post-action report)  [x] DONE

- **WHAT:** Update `_2.4_Marker_LOD.md` Implementation Status table with this session's findings. This is a "post-action report" — what was done, what was found, what changed from the original spec.
- **WHERE:** `proj_guides/_2.4_Marker_LOD.md`
- **HOW:** Specific row updates:
  - **Row 0** (Pre-reads done): ❌ → ⚠️ "process note — requires human to read referenced files before further work"
  - **Row 1** (WallConfigData.cs schema): ⚠️ → ✅ "LodSettings + LodBandEntry classes confirmed on disk (lines 356-404). LivingRoom config uses defaults (no explicit lod_settings block). Verified via direct code read this session."
  - **Row 9a** (ARFoundation FOV spike): ⚠️ — keep as-is, "requires human-in-the-loop device test (Block 5), not verifiable without real device"
  - Add any corrections discovered: e.g., "painting cluster POIs (6) added to LivingRoom config for density/cluster testing alongside the existing lamp cluster (6) and camera cluster (6) — 18 POIs total across 3 spatial clusters"
- **Add a "Corrections Log" section** at the bottom of the Implementation Status block (same format as _2.3's corrections log, per 60-finishing §6.2) noting:
  - The `displacement_tiebreak` field removal from LodSettings (moved to DisplacementSettings per spec §11)
  - Any drift between spec and implementation (e.g., if cluster_icon_mode options differ slightly)
- **WHY:** The spec doc is the durable reference; the tracker file is the TODO list.
- **FINISHING:** Table fully consistent with verified disk state.

### 4.3 Populate `_2.4.1_Vision_Tests.md` handover guide  [x] DONE

> **NOTE:** Since vision and human tests are now merged, this file becomes an **input reference** for the human handover — it stores the itemized vision checklist that Block 5's human executes. Populate it so Block 5 can reference it.

- **WHAT:** Write the complete vision checklist that a human (or future vision agent) can execute.
- **WHERE:** `proj_guides/_2.4.1_Vision_Tests.md`
- **HOW:** Write the following content:


- **WHY:** Handover guide is required per `__mode.md` §7. Stored here separately so Block 5 can reference it without clutter.
- **FINISHING:** File is populated and ready for use.

### 4.4 Populate `_2.4.2_Human_Tests.md` handover guide  [x] DONE

- **WHAT:** Write the complete human-in-the-loop test guide covering ARFoundation FOV spike + merged vision checks + Phase B live pipeline validation.
- **WHERE:** `proj_guides/_2.4.2_Human_Tests.md`
- **HOW:** Write the following content:


- **WHY:** Handover guide required before Block 5 execution; §9a specifically gates the zoom feature's real-device verification.
- **FINISHING:** File is populated and ready for human.

### 4.5 Final check: `_5.1_Editor_Tab.md` accuracy  [x] DONE

- **WHAT:** Update the Editor Tab doc's "current build state" note to reflect that the LOD domain is now fully implemented.
- **WHERE:** `proj_guides/_5.1_Editor_Tab.md` lines 5-6
- **HOW:** Change: `"_2.4_LOD.md is partially implemented"` → `"_2.4_LOD.md is fully implemented (LODController 7-step pipeline, clustering/hybrid/shrink-fade/safety strategies, zoom, Editor LOD+Zoom foldouts with Suggest Values, ClusterGallery Phase A+B tests, all 224 EditMode + 37 PlayMode green)."`
- **Also update the editor status block** (lines ~8-13): Remove the note about density response being Phase 3 (it's done), remove the note about MarkerClusterView/cluster prefab not existing (they exist), remove the note about ARZoomController not existing (it exists).
- **FINISHING:** Doc reflects current state.

### 4.6 Re-run full test suite one final time  [x] DONE

- **WHAT:** Run full EditMode + PlayMode suite via Unity MCP to confirm zero regressions after any doc-only changes (if any code was touched).
- **WHERE:** Unity MCP
- **HOW:** `refresh_unity` (force compile) → `run_tests` (EditMode) → `get_test_job` → `run_tests` (PlayMode) → `get_test_job`.
- **FINISHING:** 224/224 EditMode + 37/37 PlayMode, 0 failed, 0 console errors.
- **DONE:** `refresh_unity` (force compile) → zero `error CS`. EditMode `run_tests`/`get_test_job`: **224 passed, 0 failed** (4.3s). PlayMode `run_tests`/`get_test_job`: **37 passed, 0 failed** (6.2s). Console (`read_console` error filter): 0 errors (only the expected "Saving results" log). Zero regressions — all doc-only changes, no code touched.

### 4.7 Mark Block 4 complete in this tracker  [x] DONE

- **WHAT:** When all 4.1-4.6 are done, update this file to mark Block 4 as [COMPLETE].
- **WHERE:** `proj_guides/__curr_plan_tracker.md`
- **FINISHING:** Block 4 header updated. Block 5 is now the active block.

---

## BLOCK 5 — HUMAN-IN-THE-LOOP + VISION CHECKS [MERGE]  -> handover in _2.4.1_Vision_Tests.md + _2.4.2_Human_Tests.md
**Gate:** Block 4 fully complete — UNLOCKED (handover docs populated, tests re-verified 224/224 + 37/37, 0 errors)

Execute the full human-in-the-loop protocol using `_2.4.2_Human_Tests.md` as the guide. This covers:
- Task A: ARFoundation FOV spike (real device)
- Task B: Cluster Gallery vision checks (Editor + device)
- Task C: Phase B live pipeline walk-through (real wall)

The vision checklist from `_2.4.1_Vision_Tests.md` is executed as part of Task B — the human opens the ClusterGalleryScene in the Editor and runs through the itemized checklist on-device.

Uses 40-testing §4.6 protocol: issue commands one at a time, wait for human confirmation, read device logs, confirm pass/fail before next step.

**Acceptance criteria:**
- [ ] Task A: All zoom mechanisms verified working on real device (or logged failure if not)
- [ ] Task B: All vision checklist items pass on Editor AND on device (screenshots captured)
- [ ] Task C: LOD behaviour observed and logged at all distances; no jarring pop-in/out
- [ ] Device logs reviewed, no unexpected errors




