# Stage 2.3 — POI Marker Design System

**Status: implemented and verified.** This domain's core build-out is done —
see Implementation Status immediately below for exactly what's confirmed and
what (little) remains open. **Audience:** the implementing agent has full
Unity Editor + project access but limited context — this document is
self-contained; do not assume prior chat history.

**This file was restructured on 2026-08-06** (per `_0_guidelines.md`'s
"Plan/Design Doc Hygiene" rule) once the domain reached this state: the full
historical build narrative — original spec, every audit round, the gallery
build-out, the sun-square bug investigation, the effects/background-shape/
hero-icon implementation logs — moved to `_2.2.1_Marker_Design_Archive.md`,
verbatim, same section numbers, zero information loss. **Read this file
first for any new work in this domain; only open the archive if you need
the full historical reasoning** (e.g. for the thesis methodology writeup,
or debugging something whose root cause traces to a specific historical
round).

**Visual reference:** `C:\Users\franc\Desktop\TileStories\_2.2_POI_design_options.html`
— the original interactive mockup. Still the source of truth for how things
should *look*, now superseded by the actual shipped code for how things
were *built*.

---

## Implementation Status (updated 2026-08-06)

**✅ Confirmed correct — verified directly against the actual project files
on 2026-08-06, not from any prior report:**

- Full rendering pipeline: `MarkerStyle`/`MarkerShape`/`MarkerOutlineMode`/
  `MarkerVisualsParser`, `CategoryPalette`, `BadgeCategoryPalette`,
  `StatusRamp`, `SpriteKeyLibrary`, `MarkerCircleGlyphView`,
  `MarkerRingView`, `MarkerLayout`, `MarkerCircleSpriteFactory`,
  `MarkerEffectFlags`, `MarkerAccentEffect`, `MarkerSunEffect`,
  `MarkerPulseEffect`. See §2 below for the current component map.
- Every feature added across the archived §17-§21 rounds (icon/shape
  rendering fixes, `status_unknown`, `OutlineSameHue` hue-drain, the sun
  square-render fix, the effects expansion incl. rotate-contour/simple-sun/
  ring-pulse/beacon, background shape `None`, per-wall `badge_shape`,
  extensible ring line styles, hero icon override) is present in code and
  covered by automated runtime/editor tests.
- `config.json` (both `Assets/Apps/LivingRoom/` and
  `Assets/StreamingAssets/LivingRoom/` copies, confirmed in sync) has every
  field `WallConfigData.cs` declares, including all recently-added ones —
  regenerated through the Editor tool's own Save path, not hand-edited.
- The Editor tool (`Assets/Framework/Editor/POIAuthoring/`) is a
  `partial class` split across a folder structure, not one 1,750-line file
  — see `_5.2_Organize_Project.md` for that refactor's own record.

**❌ Confirmed missing or broken:** none, as of 2026-08-06.

**⚠️ Still open:** none in this domain.

---

## 2. Current architecture — read this for new work, not the archive

*Note: §§2.5-2.7 below were kept from the original spec and still contain a
few internal references like "§7 Step 2" or "§8" — those numbers refer to
the **archive** file's section numbers (preserved there exactly as they
were), not to anything in this file. Harmless to leave as-is; only matters
if you go looking for them.*


### 2.1 Required reading, current file list

```
Assets/Framework/Runtime/Core/WallConfigData.cs
Assets/Framework/Runtime/Core/WallConfigLoader.cs
Assets/Framework/Runtime/Core/WallSession.cs
Assets/Framework/Runtime/Core/MarkerVisualsParser.cs
Assets/Framework/Runtime/POI/CategoryPalette.cs
Assets/Framework/Runtime/POI/BadgeCategoryPalette.cs
Assets/Framework/Runtime/POI/StatusRamp.cs
Assets/Framework/Runtime/POI/MarkerOverlapResolver.cs
Assets/Framework/Runtime/UI/Markers/MarkerView.cs
Assets/Framework/Runtime/UI/Markers/MarkerCircleGlyphView.cs
Assets/Framework/Runtime/UI/Markers/MarkerRingView.cs
Assets/Framework/Runtime/UI/Markers/MarkerCircleSpriteFactory.cs
Assets/Framework/Runtime/UI/Markers/MarkerEffectFlags.cs
Assets/Framework/Runtime/UI/Markers/MarkerAccentEffect.cs
Assets/Framework/Runtime/UI/Markers/MarkerSunEffect.cs
Assets/Framework/Runtime/UI/Markers/MarkerPulseEffect.cs
Assets/Framework/Runtime/UI/Markers/SpriteKeyLibrary.cs
Assets/Framework/Runtime/UI/Markers/POIAnchor.cs
Assets/Framework/Runtime/UI/Markers/POI_Marker.prefab
Assets/Framework/Editor/POIAuthoring/               (whole folder — see _5.1_Editor_Tab.md)
Assets/Framework/Tests/Runtime/MarkerGalleryTests.cs
Assets/Framework/Runtime/DevTools/MarkerGalleryDefinitions.cs
Assets/StreamingAssets/LivingRoom/config.json  (or Assets/Apps/LivingRoom/config.json — source of truth)
```

Also skim `_0_guidelines.md` §2 (Code Quality Rules) and §6 (Testing
Strategy incl. §6.4.1) — this domain follows them throughout.

### 2.2 Component map (one line each — read the file itself for behaviour)

| Component | Role |
|---|---|
| `MarkerView` | Root orchestrator. `Initialise(anchor, outlineMode, useBadge, shape, badgeShape, effectFlags, ...)` — the real entry point every spawn path must call. Resolves category/hero-icon/status, wires every sub-component below. |
| `MarkerCircleGlyphView` | One reusable "coloured shape + centred icon" element — used at two sizes/positions for both Symbol and Badge. `SetBackgroundVisible(bool)` supports `MarkerShape.None`. |
| `MarkerRingView` | The status ring/contour. Colour from `StatusLevel` (gold ramp or same-hue-drained-to-black), dash pattern from `lineStyleLibrary` (falls back to the fixed 5-slot `RingSpriteSet` if unassigned). `SetRotating(bool)` for `rotate_contour`. |
| `MarkerCircleSpriteFactory` | Single shared source of runtime-generated circle/ring sprites (`GetRing`/`GetFilled`), used by both `MarkerSunEffect` and `MarkerAccentEffect`. Domain-reload-safe caching (explicit `== null` checks + `[RuntimeInitializeOnLoadMethod]` reset) — don't reimplement sprite generation anywhere else. |
| `MarkerSunEffect` | 3-layer staggered hero accent (Contours or FilledCircles style). |
| `MarkerAccentEffect` | Single-layer accent, two axes: `AccentShape` (FilledCircle/Contour) × `AccentMotion` (Breathe/Beacon) — covers SimpleSun, RingPulse, Beacon from one class. |
| `MarkerPulseEffect` | Whole-marker gentle scale breathing. |
| `MarkerEffectFlags` | `[Flags]`: `Pulse`, `SunContours`, `SunCircles`, `RingPulse`, `SimpleSun`, `Beacon`. Independent of `is_hero` — any POI can opt into any combination via `effect_mode`. `is_hero` only controls the persistent label. `RingPulse`/`SimpleSun`/`Beacon` are mutually exclusive with each other (one shared `MarkerAccentEffect` instance, priority order RingPulse > SimpleSun > Beacon); stack freely with `Pulse`/`Sun*`. |
| `SpriteKeyLibrary` | Generic `{key -> Sprite}` lookup, reused for icons, shapes, and ring line styles. Framework defaults referenced directly by the prefab; wall-local overrides loaded via `Resources.Load` at a config-supplied path — see `_5.1_Editor_Tab.md` §14 for the full asset-pipeline explanation. |
| `CategoryPalette` / `BadgeCategoryPalette` | Hash-based fallback colour + `Configure`/`ClearOverrides` for wall-authored `category_styles`/`badge_categories`. |
| `StatusRamp` | Resolves a status level (by `status_level_key` or nearest `pct`) to a colour + dash pattern; `ShadeTowardBlack` for `OutlineSameHue`; includes semantic unknown fallback level (`unknown` → dotted + neutral grey). |

### 2.3 `WallConfigData`/`POIData` — current complete field set

Canonical source is the C# class itself (`JsonUtility.ToJson` always writes
every field, in this order — the file's own field order isn't a separate
design decision, see `_5.1_Editor_Tab.md` §14 for why this matters and why
`config.json` must always be regenerated through the tool, never hand-edited).

**Wall-level:** `wall_id`, `wall_name`, `immersal_map_id` · `marker_style`
(legacy fallback only — prefer the two below) · `marker_shape`,
`badge_shape` (`"circle"/"rounded_square"/"hexagon"/"diamond"/"star"/"none"`)
· `marker_outline_mode` (`"gold"/"same_hue"/"none"`), `marker_use_badge`
(bool, independent of outline mode) · `marker_icon_library_resources_path`
· `category_styles`, `badge_categories`, `outline_levels` (each entry now
has `details` for free-text notes, editable via the tool's popup) · `pois`,
`calibration_anchors`.

**Per-POI:** `id`, `name`, `category`, `x_norm`/`y_norm`,
`captured_position`/`has_captured_position`/`captured_position_source`/
`captured_position_timestamp`, `summary` · `has_status`, `status_pct`,
`status_unknown`, `status_level_key` · `is_hero`, `hero_icon_key` (overrides
just the icon, category colour/ring/badge unaffected) · `effect_mode`
(comma-separated `MarkerEffectFlags` tokens) · `rotate_contour` ·
`badge_category`.

### 2.4 Phase A test scene (the gallery)

`Assets/Dev/MarkerGallery` scene + `MarkerGalleryDefinitions.cs` (one
data-struct entry list, consumed by both the visual harness and
`MarkerGalleryTests.cs` so they can't silently drift apart) + the harness
MonoBehaviour. Every rendering variant this domain supports has a row.
**Adding a new variant**: add an entry (or a new `AddXRows` helper +
`BuildEntries()` call) to `MarkerGalleryDefinitions.cs`, run via Unity MCP,
one group at a time, MCP-green after each — per `_0_guidelines.md` §6.4's
Phase A discipline. Full historical build-out (including the sun
square-render bug's root-cause analysis) is in the archive §18 if the
methodology itself, not just the current row list, is ever in question.

## 2.5 Design principles (read once, apply throughout)

1. **`category` and `status` stay free-form wall data, forever.** Confirmed by the
   real `LivingRoom/config.json`: its categories (`furniture`, `art`, `test`,
   `electronics`) share nothing with the Panorama-wall taxonomy
   (`religious`/`military`/…) used in the design prototype. Never hardcode a
   category switch statement anywhere in this stage — this is why `CategoryPalette`'s
   fallback is a deterministic hash, not a fixed lookup table.
1b. **A wall can still hand-pick specific category colours/icons.** The hash
   fallback guarantees *every* category renders distinctly with zero authoring, but a
   dev may reasonably want "religious" to be a specific violet, not whatever the hash
   lands on. `WallConfigData.category_styles` (§7 Step 2) is an *optional*, *additive*
   list — any category not listed still falls through to the hash/`KnownIcons` path
   untouched. This is the resolution to "let the dev put whatever colours they want,
   but give a good default" raised early in the design discussion for this stage:
   automatic default + optional explicit override, not one or the other.
2. **`marker_style` and `marker_shape` ARE fixed enums.** They are a *rendering*
   choice the framework itself offers (3 styles, 5 shapes), not wall content — the
   opposite case from principle 1. Stored as plain strings in `config.json` (not
   native C# enum fields) because Unity's `JsonUtility` enum serialization has been
   inconsistent across versions; a string parsed with a safe fallback avoids that
   risk entirely. This is the same reasoning already applied to
   `captured_position_source` elsewhere in `WallConfigData`.
3. **A ring that fades to invisible is a bug.** It becomes indistinguishable from a
   POI that has no status axis at all (a real case — a mural has no "destroyed"
   axis). Every status level renders fully opaque; gradation is dash rhythm + colour
   warmth only. "No status axis" is a *structurally separate* case (no ring/badge
   element at all), gated by `has_status`, never by a faded value. There is a third,
   equally distinct case — `status_unknown` (§7 Step 2): this wall *does* track
   status, and this specific POI's fate is a genuine historical unknown, not merely
   undocumented in this dataset. Rendered as its own universal grey "?" badge (§7
   Step 7), never blended with either the "no axis" case or any known percentage.
4. **Symbol and Badge are the same component.** Both are "a coloured shape with a
   centred icon" — `MarkerCircleGlyphView` is used for both, just different
   size/position/content. Likewise `SpriteKeyLibrary` (a plain key→Sprite lookup) is
   used as both the icon library and the shape library — same data shape, one class.
5. **Sprite-swap over shader/particle, by default.** Dashed rings and shapes are
   pre-made PNGs recoloured via `Image.color`, not a procedural shader — the same
   low-tech, easy-to-debug pattern already used elsewhere in this project (see
   §8 for the one deliberate exception: the runtime-generated sun effect and the
   optional `MarkerParticleEffect`).
6. **MonoBehaviours stay thin.** `MarkerView` wires things together; it does not
   decide colours, shapes, or ramps. `CategoryPalette`, `StatusRamp`, and
   `MarkerLayout` are plain static C# classes with no Unity lifecycle — easy to unit
   test, easy to read end to end.
7. **Any icon a dev wants reduces to one workflow.** Hand-drawn line art, an
   emoji-style PNG, a downloaded icon pack, a photo — import as a Sprite (2D and UI),
   add one entry to `IconLibrary.asset`. No second rendering path is needed to
   "support emoji" — a dev who wants that look just imports an emoji-style PNG. TMP's
   built-in emoji sprite asset (`EmojiOne.asset`, already in this project) is
   available for zero-import placeholder testing, but is not wired into this system;
   it isn't needed since Sprite import already covers that case.

---

## 2.6 Where visual constants live — architecture decision

This project already draws a hard line between two UI systems (see
`_0_work_plan.md`, the `UI/` folder comment): **uGUI** for `Markers/` and
`NextStopArrow.cs` (world-space, physically placed), and **UI Toolkit** for
everything else (screen-overlay chrome — toasts, panels, guide dialogue), themed
through a central `DesignTokens.uss` (`--ts-color-primary`, `--ts-font-body`, etc.).

That existing split already answers the "where do marker colours live" question:
**`DesignTokens.uss` is explicitly scoped to UI Toolkit chrome.** It is a USS file —
not readable from uGUI C# code without a custom bridge — and its stated contents
(corner radius, spacing scale, motion curve) are generic UI-chrome concerns, not
POI-domain semantics. Routing marker colours through it would require inventing that
bridge for no benefit, and would falsely couple two things that mean different
things: "the app's brand accent colour" and "what religious-building-60%-destroyed
looks like."

### The general principle (why, not just what)

A single global "AppColors" file that holds *every* colour used anywhere is a false
economy once one domain (here: markers) needs colours that are **generated from
runtime data**, not authored design decisions — `CategoryPalette.ResolveColor` hashes
an arbitrary wall-defined string; there is no finite palette to centrally enumerate.
Forcing it into a shared namespace also creates accidental coupling: a future
developer changing "the app's warning colour" could unknowingly shift what
"destroyed" looks like on markers, or vice versa. This is the same reasoning
Domain-Driven Design calls a **bounded context** (Evans, 2003) — each subsystem owns
the vocabulary meaningful to *it*; sharing happens at an explicit boundary, not by
default.

The opposite extreme — zero shared foundation, every component invents its own
colours *and* fonts — loses product cohesion: nothing stops two UI domains drifting
to different typefaces or accent colours over time.

The resolution used throughout mature design systems (Curtis, 2020; the Design
Tokens W3C Community Group; Material Design's colour-role system) is a **tiered
token model**:

- **Global tokens** — raw values, no meaning yet (a hex code, a font file).
- **Semantic/alias tokens** — a *cross-cutting* meaning applied at the app level
  (`--ts-font-body`, `--ts-color-primary`) — this is what `DesignTokens.uss` already
  is, correctly scoped to UI Toolkit chrome.
- **Domain/component tokens** — a meaning that only makes sense *inside* one
  subsystem (`CategoryPalette`, `StatusRamp`) — owned by that subsystem, not the app
  shell.

### Concrete decisions for this project

| Concern | Where it lives | Why |
|---|---|---|
| Marker category colour | `POI/CategoryPalette.cs` (already built, this stage) | Generated from wall data, meaningless outside the marker domain |
| Marker status colour/ring/dash | `POI/StatusRamp.cs` (already built, this stage) | Same — a domain-specific ordinal scale, not a brand colour |
| UI Toolkit chrome colours/radius/spacing | `DesignTokens.uss` (already planned, unrelated to this stage) | Cross-cutting brand identity for screen-overlay UI |
| **Font family, app-wide** | **TextMeshPro's own default asset mechanism** (`Project Settings → TextMeshPro → Settings`, the *Default Font Asset* field) | Unity/TMP already solves "one font everywhere" natively — every `TextMeshProUGUI` component that doesn't explicitly override its Font Asset field inherits this one. No custom C# class needed; this is the simplest-robust-approach principle applied directly. **Action for this stage: leave `labelText`'s Font Asset field on the `POI_Marker` prefab unset/inherited — do not assign a font per-prefab.** |
| Marker layout proportions (badge offset, ring size ratio, label gap) | `UI/Markers/MarkerLayout.cs` (this stage, §7 Step 5) | Domain-specific composition rule, not a brand token |

Do not create an `AppTheme`/`DesignTokens.cs` C# asset in this stage. If a second
uGUI UI domain is ever built that genuinely needs to share 1–2 brand colours with
markers (unlikely — markers are the only planned uGUI content), *that* is the moment
to extract a minimal shared asset — not before (YAGNI).

Note on typography across the two rendering systems: UI Toolkit (USS `font-family`)
and uGUI/TMP (`TMP_FontAsset`) use different technical font resource formats, so "one
font app-wide" is a *process* discipline (both reference the same underlying
typeface file) rather than something one shared C# token class can enforce across
both systems mechanically. Worth a one-line note in the project's design doc, not a
code abstraction.

---

## 2.7 Effects catalogue — what's deferred and why

(What's *built* is in §2.2's component map above, plus `MarkerEffectFlags` in §2.3.
This table is specifically the not-built list, with reasoning, so it doesn't get
silently redone.) Three trigger sources, kept structurally separate in
`MarkerView` — do not merge them into one flag:

| Effect | Trigger | Built this stage? |
|---|---|---|
| `MarkerPulseEffect` (scale breathing) | `is_hero` | Yes |
| `MarkerSunEffect` (runtime-generated concentric rings) | `is_hero` | Yes |
| `MarkerGlowEffect` (halo opacity breathing) | `is_hero` | Legacy/optional only; `MarkerView` keeps it off in the final runtime |
| `MarkerParticleEffect` (Shuriken wrapper) | Whatever a dev wires it to | Deferred — wrapper only, no shipped art path |
| Shimmer/gleam sweep across the icon | One-shot, first discovery | **Deferred** — polish, not core; add only once the discovery-toast moment (already in the work plan) is built and needs a marker-side counterpart |
| Discovery burst (one-shot particle pop) | First reveal/tap | **Deferred** — same reasoning; also depends on a "first seen" flag not yet in `POIData` |
| Bob/float (vertical drift) | `is_hero` | **Deferred, with caution** — interacts badly with `MarkerOverlapResolver`, which assumes a fixed base position (`ApplyOverlapOffset` stores one base position and offsets from it); a moving marker would fight that assumption. Needs its own design pass, not a drop-in addition |
| Heat-haze distortion (shader) | High status | **Deferred** — full-screen-ish shader effect, real GPU cost on mobile, low value-to-cost ratio compared to the shipped sun/pulse hero cues |

`MarkerSunEffect` is the shipped hero accent for a calm glow/sun read. It uses
runtime-generated sprites, so there is no fire sprite sequence to import or
maintain in this stage. **If the sun rings/discs ever render as hard-edged
squares instead of circles, that's not a sprite-generation bug** — see the archive's §18.12
for the full root-cause analysis (a stale static sprite cache surviving
across Play Mode sessions) and fix; don't re-derive the circle math from
scratch, it's already correct.

`MarkerParticleEffect` needs a `ParticleSystem` prefab authored in-Editor (start
with Unity's default fire-ish preset via right-click → Effects → Particle System,
tune Start Color/Size/Emission) — also not a code task, and per the performance
caveat in the file, wire it to `is_hero` only, never to every burning marker.

---

## 3. Explicitly deferred (do not build unless separately requested)

Tap-to-reveal label for standard POIs · TMP icon-font/sprite-atlas migration ·
per-POI shape override · shimmer/gleam sweep · discovery burst · bob/float effect ·
heat-haze shader · shape-matched dashed rings for non-circle shapes · a project-wide
`AppTheme`/`DesignTokens.cs` C# asset (§5) · `TileStoriesSettings.cs` (doesn't exist
yet in this project; not needed for this stage) · a status/category filter UI that
exploits `OutlineSameHue`'s hue/lightness toggle potential (§7 Step 1 note — no
filter UI exists yet to drive it).

---

## 4. Bibliography / references

Used to ground the architecture decisions in §5 and §7 (design-token tiering,
bounded contexts, uGUI layout mechanics):

- Evans, E. (2003). *Domain-Driven Design: Tackling Complexity in the Heart of
  Software*. Addison-Wesley. — bounded context concept, used to justify marker
  colours as domain-owned rather than centralized in a global palette.
- Curtis, N. (2020). "Naming Tokens in Design Systems." *EightShapes*.
  https://medium.com/eightshapes-llc/naming-tokens-in-design-systems-9e86c7444676
  — the global/alias/component three-tier token model referenced in §5.
- Design Tokens Community Group, W3C. *Design Tokens Format Module* (draft
  specification). https://design-tokens.github.io/community-group/format/ — formal
  reference for the tiered-token concept in general use across the industry.
- Frost, B. (2016). *Atomic Design*. https://atomicdesign.bradfrost.com/ — general
  reference for hierarchical composition of design decisions (informs the
  global→semantic→component framing, though not quoted directly).
- Material Design (Google). "The Color System." https://m2.material.io/design/color/
  — example of a large production design system's colour-role architecture,
  referenced as a real-world comparison point for the tiered model.
- Martin, R. C. (2002). *Agile Software Development, Principles, Patterns, and
  Practices*. Prentice Hall. — Single Responsibility Principle, underlying the
  "MonoBehaviours stay thin, decisions live in plain C#" rule in §4 principle 6.
- Dijkstra, E. W. (1974). "On the Role of Scientific Thought." — origin of
  "separation of concerns," underlying the split between the uGUI marker system and
  the UI Toolkit chrome system referenced in §5.
- Unity Technologies. *RectTransform* and *Transform* class documentation, Unity
  Manual. https://docs.unity3d.com/Manual/class-RectTransform.html — technical
  basis for the "scaling the root scales all children" claim in §7 Step 5 (platform
  documentation, not academic literature — cite accordingly if used in a thesis).
- Unity Technologies. *TextMeshPro Documentation* — Default Font Asset / TMP
  Settings mechanism, basis for the typography recommendation in §5.

This project's own `_0_work_plan.md` (§"UI/" folder comment, and the
`TileStoriesSettings`/`DesignTokens.uss` mentions) is the direct primary source for
the existing uGUI/UI-Toolkit split this stage's colour-ownership decision builds on
— cite as project design documentation if referencing this decision's provenance.

---

## 5. Full historical build record

See `_2.2.1_Marker_Design_Archive.md` for the complete original spec (§1, §3,
§6, §7), Editor tool / asset / prefab / test build-out (§9-14), two audit
rounds (§17), the Marker Gallery Phase A build-out including the sun
square-render bug's full root-cause analysis (§18), the effects expansion
(§19), and the background-shape/ring-extensibility/hero-icon work (§20-21).
Same section numbers throughout, so any existing cross-reference to e.g.
`_2.2_Marker_Design.md §19.13` now resolves in the archive file instead —
update such references to point at the archive filename when you next touch
them, but they aren't wrong in spirit, just in which file.