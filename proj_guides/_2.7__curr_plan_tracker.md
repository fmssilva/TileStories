# Master Plan — `__curr_plan_tracker.md` 

## Guiding rules (apply to every block)

- Work one entry at a time, top to bottom; no batching of edits across entries.
- Read the cited domain doc section (`_1_2`, `_2_3`, `_2_4`, `_2_5`, `_2_6`, `_5_1`) before each fix.
- Framework philosophy: never hardcode one behavior where walls may differ — implement genuinely good options and expose them in the POI Authoring window, unless the entry explicitly records a "one clear winner" call.
- Verification gate per step (from `_2.7` §Master verification checklist): Tier 0 asserts → Tier 0.5 programmatic UI checks (if UI surface) → Phase A before Phase B → Edit-mode parity check if touching the authoring tool's Scene-view preview → Tier 1 vision only after 0/0.5 pass → status flip in __both__ index row + detailed row in the same edit → update any domain doc whose described behavior changed.
- Acceptance: zero `error CS` via Unity MCP `refresh_unity`; zero failed tests in EditMode + PlayMode via `run_tests`/`get_test_job`.

---

## Consolidated ground-truth baseline — session of 2026-09-08 (verified on disk, per §6.1/§4.5)

Everything in this section was independently re-derived this session, not carried over from a prior summary. It exists so the next block starts from known-correct state.

### Compile & suite state (verified via Unity MCP)
- `refresh_unity` (force, compile request) -> **zero `error CS`**. Confirmed by `read_console` returning 0 error entries.
- EditMode suite: **570/570 passed**, 0 failed, 0 skipped (job `d66e7b8d`).
- PlayMode suite: 63 total. **Two displacement-pipeline integration tests flaked under full-suite load** and BOTH pass 1/1 in isolation:
  - `LODControllerEvaluateTests.RealConfig_DisplacementPipeline_DeterministicAcrossRuns` (failed run1 with 0.10 px drift on label index 4; passed 1/1 isolated and passed on the subsequent full run).
  - `LODControllerEvaluateTests.RealConfig_DisplacementPipeline_ThreeFamilies` (failed run2 with "MinAdjacentGap > 5px but was 0.0f"; passed 1/1 isolated).
  - **Root cause = test-hygiene/order-dependence** (a ±0.001 px screen-space determinism tolerance is physically unrealistic for a PlayMode camera projection that depends on frame timing, and the two displacement tests contend with the rest of the suite). It is NOT a logic regression from this session's fixes; both tests are green in isolation. **Do not mark PlayMode "green" for Block 6 acceptance on a single full-suite pass until these two are made deterministic / isolated-friendly** — treat a lone full-suite PlayMode flip as suspect.

### Fixes applied this session (all verified)
- **Step 0 revert (pre-approved):** `DisplacementSettings.displacement_tiebreak` in `Assets/Framework/Runtime/Core/WallConfigData.cs` was an orphaned `enum DisplacementTieBreakMode` (never wired to any strategy; broke 4 call sites against the committed `string` scheme). Reverted to the committed baseline `public string displacement_tiebreak = "symmetric";` (comment text also cleaned of a trailing `riority_only"` corruption leftover from a prior coordinate edit). **Verified byte-identical to git HEAD** — `git diff` shows no `tiebreak`/`leader_line` hunk on that file. The enum must NOT be re-introduced at Block 6; the plan below was corrected from "new enum" to "string dispatch" (matches `_2.5_Marker_Displacement.md:419` and `_2.7_Corrections_TODO.md:1684`, both of which compare `displacement_tiebreak == "lower_priority_only"` as a string).
- **Constants.cs corruption:** `POIAuthoringToolWindow.Constants.cs` had a duplicated 8-space-indent pair + an over-indented pair of `ZoomDoubleTapWindowHelp`/`ZoomDoubleTapMoveToleranceHelp` (one set at lines 64-65, one corrupted set of duplicate definitions lower). Removed the duplicate lower pair; canonical definitions at 64-65 remain. `git diff --stat` now shows `Constants.cs | 10 ++++++++++` (the intended Search/Filter helper strings only).
- No other files changed this session.

### Block-status reconciliation (which `DONE` claims are real, verified this session)
- Block 1 (Steps 1-5): Tasks 1.1-1.5 marked DONE 2026-08-26 — statuses already flipped; consistent, nothing re-opened.
- Block 2 (Steps 6-8): **DONE** 2026-08-26 — selection highlight, zoom-on-select, `SelectionEventBus` round-trip. `MarkerSelectionEditModeTest.cs` 268/268 green, `ARZoomRoutingTests_additions.cs` 38/38 green.
- Block 3 (Steps 9-11 + re-scoped 18): Tasks 3.1-3.5 marked DONE 2026-08-26 — consistent with file-existence checks this session (`UIAccessibility` Runtime-hosted, minimap fields present).
- Block 4 (Step 12 = 2.6-i): Tasks 4.0-4.8 marked DONE 2026-08-26 — **corroborated this session**: `ResultSetCoordinator.cs` exists at `Runtime/UI/Navigation/` (NOT `Runtime/UI/Filter/` as the plan body said), `FilterCompositionTests.cs` exists.
- Block 5 (Steps 13-14 = 2.4-l, 2.4-m): Tasks 5.1-5.3 code+tests DONE on disk (verified 2026-08-31: FOV driver, `ARZoomGestureInput`, `ARZoomCameraFovTests`, `ARZoomMath.cs` split). **Task C2 done 2026-08-31:** `ZoomRig` GO (ARZoomController + ARZoomGestureInput, refs wired) added to LivingRoomScene — before this neither component existed in ANY scene. **5.4 Tier 1 vision + Tier 2 device remain open** (human/device-gated) — do not mark Block 5 complete; `_2.7` rows 13/14 now [PENDING-DEVICE].
- Block 6 (Steps 15-16 = 2.5-g, 2.5-h): **DONE 2026-08-31** (corrects this file's earlier "NOT built" line, which was stale — an interim ACT session had added `DisplacementTieBreakStrategy.cs` but left it UNWIRED with a false in-code claim, and the tree did not compile). This session: fixed the `hiddenLabels` CS0103 (Task A), wired the strategy into all three algorithm branches (Task B), shipped the full test set (Task D), flipped `_2.7` rows 15/16 + `_2.5` rows 15/16. Suites green in single full runs: EditMode 589/589, PlayMode 64/64 (incl. the two previously-flaky determinism tests — the `committedOffset` hold fix is accepted).
- Block 7 (Step 17 = 2.6-al): **PARTIAL** — `ConfigureWithSynonyms` exists in `POISearchIndex.cs:480` but is NOT wired to `WallSession`; no `synonym_groups` field on `WallConfigData`; `SearchSynonymGroups.cs` asset still exists at `Editor/POI/`. **Needs: field + bootstrap + Editor UI + delete dead asset + delete `SearchSynonyms.asset`.**
- Block 8 (Steps 18-21 + new 22-24): Step 18 absorbed into Block 3 Task 3.5 (DONE). Steps 19-21 open (1.2-p Add POI, 1.2 Position redesign, FW-1 part 2). Steps 22-24 are new: 2.6-al-wiring (synonym bootstrap), doc-hygiene batch, test-guide expansion.

### File-path corrections for the detailed plan bodies (stale paths found this session)
- `ResultSetCoordinator.cs` lives at `Assets/Framework/Runtime/UI/Navigation/ResultSetCoordinator.cs`, not `Runtime/UI/Filter/`.
- `SearchSynonymGroups.cs` lives at `Assets/Framework/Editor/POI/SearchSynonymGroups.cs` (and `SearchSynonymGroupsTests.cs` at `Tests/Editor/`), not `Editor/` root.
- `POIAuthoringToolWindow.SearchFilter.cs` lives at `Assets/Framework/Editor/POIAuthoring/GlobalScene/POIAuthoringToolWindow.SearchFilter.cs` (per `10-structure.md`).

---

## Block 1 — Doc hygiene & trivial code fixes (Steps 1–5) — LOW RISK, CLEARS NOISE

__WHAT:__ (S1 batch) remove stale `weight_*` / `search_keywords` / signature / type-description references across `_2_5`, `_2_6`, `_5_1`, `_1_2` (exact locations listed under 2.5-b, 2.6-ab, 5.1-r/z/ac/ad, 1.2-n/l). (S2 = 2.3-m) DRY refactor of duplicated `activeFlags` ternary in `MarkerView.cs`. (S3 = 2.6-y) enlarge DetailCard close button to ≥44×44px. (S4 = 5.1-b) split Search & Filter Editor code into its own partial-class file under `POIAuthoring/`. (S5 = FW-1 part 1) remove `TileStories/Cluster/Wire DominantIcon` top-level menu item (per `.clinerules` §1.3: framework-internal repair tools must not sit bare at the top level). __HOW/WHERE:__ docs edited in place in `proj_guides/`; code in `Assets/Framework/Runtime/UI/Markers/MarkerView.cs`, `Assets/Framework/Runtime/UI/Cards/`, `Assets/Framework/Editor/POIAuthoring/*`, `Assets/Framework/Editor/DevTools/ClusterPrefabWiring.cs` (keep the method, drop only the `[MenuItem]`). __WHY:__ pure-noise removal first so later work doesn't build on stale references; zero behavioral risk. __TESTS:__ S2: existing Marker tests stay green; S3: Tier 0.5 tap-target assert (≥44×44); S4: compile + authoring round-trip tests green; others: doc-only. __FINISHING:__ flip both index + detailed rows; no behavior change to document.

## Block 2 — Data integrity & validation-warning gaps (Steps 6–8)

__WHAT:__ (S6 = 2.3-l) sync LivingRoom source config ↔ StreamingAssets copy (__sync, do not delete__ — dev/test POIs are intended content). (S7 = 2.3-j) add warning when a POI's `hierarchy_level_key` is unset or unresolvable. (S8 = 2.4-d) add misconfiguration warnings for contradictory `shrink_start_neighbor_count` / `cluster_min_count`. __HOW/WHERE:__ S6 via the authoring tool's existing "Copy to StreamingAssets" path, then verify byte-equality. S7/S8 extend `POIAuthoringToolWindow.ConfigValidation.cs` (pattern already established by `ValidateHierarchyLevelKeys` / size-range checks). __WHY:__ shared test data must not be dirty before functional work builds on it; validation gaps silently let broken configs ship. __TESTS:__ extend `HierarchyLevelSizeRangeTests`-style EditMode tests for the new warning paths (set/unset key, contradictory LOD numbers); assert warnings fire and don't fire on valid configs. __FINISHING:__ doc updates where these fields are described (`_2_3`, `_2_4`) in the same session.

## Block 3 — Pure test additions (Steps 9–11) — ADDITIVE, CAN'T REGRESS

__WHAT:__ (S9 = 2.4-p) two required LOD Phase A Tier 0 tests (shallow-angle frustum case; zoom-unlocks-markers) + two lower-priority addenda. (S10 = 2.5-q) tap-target clearance test for displaced markers (Tier 0.5 raycast occlusion at the displaced label position). (S11 = 2.6-af) Tier 0.5 tap-target/contrast tests for minimap dots, filter chips, results-list rows. __WHERE:__ `Framework/Tests/Editor|Runtime` alongside each domain's existing suites (`LODControllerTests`, `DisplacementComputeTests`, `FilterTrayViewTests` etc.), using the established Tier 0.5 helpers pattern (§4.5). __WHY:__ additive-only block; closes known coverage holes before the functional blocks change behavior underneath them.

## Block 4 — Highest-priority functional gap: filtering composition (Step 12 = 2.6-i)

__WHAT:__ make filtering actually affect the app: (1) optional `candidatePoiIds` param on `POISearchIndex.Search` (null = unrestricted, backward compatible); (2) `ResultsListView` subscribes to `FilterTrayView.OnFiltersChanged` (orchestration likely owned by `SearchOverlayView`); candidate set computed by exposing `FilterFacetEvaluator`'s existing pass/fail logic publicly rather than duplicating it; (3) same candidate set drives scene marker reveal/hide via `WallSession.SpawnedMarkers` + correct entry point (`SetVisible` vs `MarkerRevealEffect` reveal path — read both first, §7 explicitly cites the reveal mechanism); (4) wire `MinimapView._searchIndex` (already present, unused) so list/scene/minimap show __one result set__; (5) `camera_highlight` view mode dims non-matching markers. __WHY:__ highest-priority finding of the entire audit — filter UI renders, tracks state, even has unit-tested relax-suggestions for an unreachable scenario, yet touches nothing else. This is also the canonical instance of the §4.2.1 composition-gap failure class. __PHASES:__ Phase A — extend existing isolated harnesses (`ResultsListViewTests`/`FilterTrayViewTests`/`MinimapViewTests`) with a composition test (facet-toggle → result-set-change across all three surfaces); no gallery scene needed. Do not proceed until green. Phase B — real-config E2E through LivingRoom (Play Mode, mock localization), modeled on `RealConfig_DisplacementPipeline_E2E` if reusable. __FINISHING:__ correct the false "[x] §17 facet-filtering asserts pass" claim in `_2_6`; note the process fix (write integration test alongside isolated units).

## Block 5 — Zoom & gesture wiring (Steps 13–14 = 2.4-l, 2.4-m)

__WHAT:__ AR camera zoom currently never changes camera FOV/zoom despite full controller→state plumbing; pinch/double-tap gestures have no input wiring. __HOW:__ connect `ARZoomController`/`ARZoomState` output to the actual camera (`ARZoomState.ZoomFactor` → FOV math already unit-tested in `ARZoomMathTests`); wire Input System pinch/double-tap into `ZoomControlView`/`ARZoomController` (sequence after 2.4-l — same device session). __TESTS:__ PlayMode routing tests asserting ZoomFactor change produces actual camera FOV change (the missing seam); Phase A isolated harness before real-config verification.

## Block 6 — Displacement stubs (Steps 15–16 = 2.5-g, 2.5-h)

__WHAT:__ implement `displacement_tiebreak = "lower_priority_only"` (currently an accepted-but-unimplemented config string); implement `max_displacement_px` documented hide-fallback (marker hidden when displacement exceeds the cap). Sequenced together — they share tiebreak/priority logic. __WHERE:__ `Assets/Framework/Runtime/POI/` displacement algorithm code + `MarkerOverlapResolver.ApplyDisplacement`; extend `DisplacementAlgorithmTest` / `DisplacementComputeTests`. __WHY:__ silent acceptance of an unimplemented option violates the framework contract that every exposed config choice is real.

## Block 7 — Synonym feature rescue (Step 17 = 2.6-al)

__WHAT:__ synonyms never reach the running app (`SearchSynonymGroups` asset is `#if UNITY_EDITOR`-only; only callers are its own tests). Preferred fix recorded in the entry: replace the ScriptableObject approach with a plain `List<SynonymGroup>` field on `WallConfigData` (matching every other taxonomy list) + runtime wiring into `POISearchIndex.ConfigureWithSynonyms` + authoring-window UI for editing them. __WHY:__ structurally unable to ship to players in current form; data-not-code rule.

## Block 8 — Decision-gated + new features (Steps 18–21)

- __Step 18 = 2.6-d [BLOCKED]:__ master enable/disable toggle for Search/Filter/Select — requires developer decision first (cheap toggle-only fix vs. larger wrapper-class JSON-migration restructure). Present both options when unblocking.
- __Step 19 = 1.2-p [prerequisite for Step 20]:__ add an "Add POI" mechanism to the POI Authoring tool (none exists anywhere today).
- __Step 20 = 1.2 rest:__ Position system Draft/Precise redesign — substantial own effort; includes Edit-Mode Tooling Parity checks (per checklist §Master verification).
- __Step 21 = FW-1 part 2:__ remove `Search ▸ Create Synonym Asset` menu item — only after Block 7 lands.

__Non-code human-required flags__ (tracked, not sequenced): 2.3-n/o/p, 2.4 suite-execution items, 2.5-t/u, 2.6-aa/aj.

Confirm decisions to make and explain well what we are talking about in each one. Example: 
(1) __Step 18 (2.6-d)__ — toggle-only vs. wrapper-class JSON-migration restructure; needs your pick before Block 8 can start
(2) Confirm Block 8's Step 19/20 scope split ("Add POI" standalone vs. folded into the position redesign session) — either works; I defaulted to the file's stated prerequisite ordering.
(3) others??
---

# --- 

# Block 1 — Doc hygiene & trivial code fixes (Steps 1–5 of `_2.7` Priority Action Index)

__Scope:__ 2 doc-hygiene batch items, 3 code items, 1 menu-hygiene item + 1 new test file addition. All LOW RISK. No Phase A/B galleries needed — no new visual features.

### Task 1.1 — Doc-hygiene batch (Index Step 1; entries 2.5-b, 2.6-ab, 5.1-r, 5.1-z, 5.1-ac, 5.1-ad, 1.2-n, 1.2-l)  [x] DONE 2026-08-26 — see _2.7 rows flipped

- __WHAT:__ One single pass correcting all stale doc text. Do them together, not as eight separate sessions:

  - __2.5-b__ (`_2_5_Marker_Displacement.md`): delete/strike the "ApplyCountCap sorts by effectiveDistance not hierarchy" bug paragraph at lines ~61–68, replace with one line: `Resolved - ComparePriority now sorts priority-then-distance, verified [date]`. Also fix §16 lines ~590–592 ("flagged for that file, not fixed here") same way.
  - __2.6-ab__ (`_2_6_Select_Filter_Search.md`): remove stale `weight_*` references from §5's ranking-formula code block, §15's Editor field list, and §20 checklist line ~727. (Confirmed: no dead `weight_*` code exists in `ConfigValidation.cs` — pure doc staleness.)
  - __5.1-r / 5.1-z__ (`_5_1_Editor_Tab.md`): remove both "search_keywords pending, not yet built" claims (§4.3 lines ~327–328; §4.5 lines ~391–395) — search_keywords is built (confirmed via 2.6-l).
  - __5.1-ac__ (`_5_1_Editor_Tab.md` §5.3): change "Struct with float x, y, z" → "Class" (verified: `CapturedPosition` is a class at `WallConfigData.cs` line 480; reference type is intentional — it makes `captured_position != null` checks meaningful).
  - __5.1-ad__ (`_5_1_Editor_Tab.md` §6): soften "Tests are pure data tests -- no scene, no EditorWindow instance" to acknowledge that `POIAuthoringToolWriteBackTests.cs` deliberately instantiates real rig GameObjects + a live `EditorWindow` because Transform-to-config capture requires real Transforms.
  - __1.2-n__ (`_1_2__Marker_Positioning_future_notes.md`): fix A4's documented resolver signature from `(Transform correctionAnchor)` to actual `(CalibrationAnchor[] calibrationAnchors)`.
  - __1.2-l__ (same doc, §12 checklist): correct the overlap-test cluster description (`painting`/`painting_religious`/etc., 6 members, not `painting_1..4`, 4).
  - Bonus fold-in (from 5.1-b's own note): add "Displacement" to `_5_1`'s §1.2 Global Scene section list (stale — predates Displacement landing).

- __HOW:__ Straight text edits. Strict ASCII (replace any ✓/→ with `[ok]`/`->` per `.clinerules` §2.1). Do NOT renumber sections; strike-through or one-line resolution notes preferred over silent deletion where history matters.

- __WHERE:__ `C:\Users\franc\Desktop\TileStories\proj_guides_2_5_Marker_Displacement.md`, `_2_6_Select_Filter_Search.md`, `_5_1_Editor_Tab.md`, `_1_2__Marker_Positioning_future_notes.md`. No code.

- __WHY:__ Later blocks read these docs before acting; stale claims burn context budget and can trigger re-fixing of already-fixed bugs (the exact failure §6.2 exists to prevent).

- __TESTS:__ None (doc-only).

- __FINISHING:__ Flip Index row 1 + rows 2.5-b, 2.6-ab, 5.1-r, 5.1-z, 5.1-ac, 5.1-ad, 1.2-n, 1.2-l in `_2.7` in the same edit session.

### Task 1.2 — `MarkerView.cs` DRY refactor (Index Step 2; entry 2.3-m)  [x] DONE 2026-08-26

- __WHAT:__ Extract the duplicated fallback ternary into one private property; replace both call sites. Zero behavior change.

- __HOW:__ Add near the `_hasHierarchy`/`_hierarchyStyle` fields (~line 85):

  ```csharp
  // Single source of truth for effect-flag fallback (spec _2_3 section 9)
  private MarkerEffectFlags ActiveEffectFlags => _hasHierarchy ? _hierarchyStyle.EffectFlags : effectFlags;
  ```

  Replace `MarkerEffectFlags activeFlags = ...` at lines __458__ (`ApplyVisuals()`) and __699__ (`GetVisualRadiusWorld()`) with `MarkerEffectFlags activeFlags = ActiveEffectFlags;`. Keep local variable name so diffs stay minimal.

- __WHERE:__ `TileStories\Assets\Framework\Runtime\UI\Markers\MarkerView.cs` (lines ~85, 458, 699 — all three verified on disk this turn). Runtime assembly.

- __WHY:__ The visual-effects path and Domain 2.5's displacement-bounds path must never drift if the fallback rule changes; single-source-of-truth per `20-code-quality.md`.

- __TESTS:__ Tier 0 regression only — full EditMode + PlayMode suites green after change (existing `MarkerGalleryTests` etc. exercise both call sites). No new tests required for a pure refactor.

- __FINISHING:__ Flip Index row 2 + row 2.3-m; note raw suite output.

### Task 1.3 — DetailCard close button tap target (Index Step 3; entry 2.6-y)  [x] DONE 2026-08-26

- __WHAT:__ Bump `_closeButton.style.width/height` from 28 to 44 px; add the missing Tier 0.5 tap-target assert.
- __HOW:__ In `Build()` (~lines 88–89) change `28` → `44`. Then add a sibling test in `UIAccessibilityTests.cs`, same pattern as `MeetsMinTapTarget_DeveloperCheck_OnEachSearchOverlayHitTarget`: instantiate `DetailCardView`, locate the close button, assert `MeetsMinTapTarget`.
- __WHERE:__ `TileStories\Assets\Framework\Runtime\UI\Cards\DetailCardView.cs`; `TileStories\Assets\Framework\Tests\Editor\UI\Shared\UIAccessibilityTests.cs` (exists, verified).
- __WHY:__ WCAG 44×44px minimum this codebase already enforces elsewhere; §15a covers accessibility labels, not tap size — no waiver applies.
- __TESTS:__ New EditMode Tier 0.5 test above; run full EditMode suite.
- __FINISHING:__ Flip Index row 3 + row 2.6-y.

### Task 1.4 — Split Search & Filter Editor code into its own partial file (Index Step 4; entry 5.1-b)  [x] DONE 2026-08-26

- __WHAT:__ Move `DrawGlobalSearchFilterSection` (line 479), `DrawSearchFieldsTable` (line 556), `DrawSystemKeywordRow` (line 653) — i.e., lines 479–663 — out of `GlobalScene.cs` into a new partial-class file. Zero behavior change.
- __HOW:__ Create new file declaring `partial class POIAuthoringToolWindow` with the exact same using-set and namespace as sibling files (copy header pattern from `GlobalScene/POIAuthoringToolWindow.Displacement.cs`). Leave the call site (line 51 in `DrawGlobalSceneOptions`) untouched — C# partials resolve project-wide. Option arrays (e.g. `SearchModeOptions`) stay in `Constants.cs` if that file's convention is "all option arrays live together" — check `LodBandOptions` placement first and follow whatever the existing pattern is; do not invent a new rule.
- __WHERE:__ New: `TileStories\Assets\Framework\Editor\POIAuthoring\GlobalScene\POIAuthoringToolWindow.SearchFilter.cs`. Remove extracted methods from `...\POIAuthoringToolWindow.GlobalScene.cs` (shrinks 665 → ~480 lines). Update `10-structure.md` tree with the new file entry (§6.3 obligation).
- __WHY:__ Restores the documented one-domain-per-partial-file convention (LOD+Zoom and Displacement both follow it); prevents the next domain from either appending to a 700+-line grab-bag or having to do this split under pressure.
- __ARCHITECTURE NOTE (per §8 framework):__ structural options considered were (a) leave appended (status quo, rejected — violates convention), (b) separate non-partial static helper class (rejected — loses access to instance serialized fields used by the foldout), (c) dedicated partial file (__chosen__ — exact established pattern, zero risk). Implementation options: cut/paste verbatim vs. rewrite-while-moving — verbatim only, this is a move not an opportunity.
- __TESTS:__ Compile check + one full EditMode suite sanity run (should be unaffected); authoring round-trip tests (`LodAuthoringRoundTripTests` etc.) confirm window still functions.
- __FINISHING:__ Flip Index row 4 + row 5.1-b; update `10-structure.md`.

### Task 1.5 — Remove `Cluster ▸ Wire DominantIcon` top-level menu item (Index Step 5; FW-1 part 1 ONLY)  [x] DONE 2026-08-26

- __WHAT:__ Delete the `[MenuItem("TileStories/Cluster/Wire DominantIcon")]` attribute (line 20 of `ClusterPrefabWiring.cs`). Keep the method body intact.
- __HOW:__ Option (a) chosen per the entry's own default: plain internal static method, callable manually from the C# console during framework maintenance, with a code comment stating how to invoke it and why there's deliberately no menu path (audience = framework maintainer, not app developer, per `.clinerules` §1.3). Do NOT relocate under a submenu — the tool is explicitly one-off/rare. Do NOT touch `Search ▸ Create Synonym Asset` (FW-1 part 2 waits for Block 7 / 2.6-al). Do NOT touch `Rig Safety Prompt` toggle (recorded keep-as-is).
- __WHERE:__ `TileStories\Assets\Framework\Editor\DevTools\ClusterPrefabWiring.cs`.
- __WHY:__ Internal prefab-overwriting repair tool must not be discoverable next to real wall-config actions — this exact mixing caused the original FW-1 incident.
- __TESTS:__ Compile check only (attribute removal; method bodies untouched). Existing `ClusterPrefabTests` remain green.
- __FINISHING:__ Flip Index row 5 + FW-1 status noting part 2 remains gated on 2.6-al.

### Block 1 — TESTS summary

No vision or human tests are required for Block 1: Tasks 1.2/1.4/1.5 are zero-behavior-change refactors covered by existing suites; Task 1.3's check is fully programmatic (Tier 0.5 numeric rect assert); Task 1.1 is doc-only. Therefore __no handover content for `_2.7.1_Vision_Tests.md` or `_2.7.2_Human_Tests.md`__ this block.

### Block 1 — FINISHING checklist

1. Per task: edit → re-read edited region → Unity MCP `refresh_unity` (zero `error CS`) → `run_tests` EditMode + PlayMode (zero failures; acceptance is zero failures, not a fixed count) → flip the corresponding row in `_2.7`'s Priority Action Index AND its detailed per-domain row in the same edit.
2. After Task 1.4: update `.clinerules/10-structure.md` tree (new `POIAuthoringToolWindow.SearchFilter.cs` entry).
3. After Task 1.1: verify no remaining `weight_*`/`search_keywords pending` strings via grep across `proj_guides/_2_5*`, `_2_6*`, `_5_1*`, `_1_2*`.
4. End-of-block: append Block 1 completion note to `proj_guides/decisions.md`; report chat summary distinguishing what was independently re-verified vs. taken from `_2.7`.

---

## Decisions

__a) Made autonomously:__ FW-1 option (a) — plain internal method, no submenu relocation (entry's own stated default); verbatim-move-only for the 5.1-b split; option arrays stay wherever the existing convention puts them (checked at implementation time against `LodBandOptions` precedent); doc fixes done as one batched pass; strike-through-with-resolution-note style over deletion for 2.5-b.

__b) Requiring developer input:__ none for Block 1 — every choice here was either dictated by the entry text or had a clear winner. (Reminder: Step 18 / 2.6-d stays BLOCKED for later blocks.)

##


# Block 2 — Data integrity & validation warnings (Steps 6–8 of `_2.7` Priority Action Index)

__Scope:__ 1 data-hygiene fix (2.3-l) + 2 additive validator extensions (2.3-j, 2.4-d) with their Tier 0 tests. No runtime behavior changes; Editor-side visibility only.

### Task 2.1 — Sync LivingRoom source ↔ StreamingAssets configs (Index Step 6; entry 2.3-l)

- __WHAT:__ Make both copies byte-identical again. Verified on disk this turn: hashes differ; source has 18 POIs (630 lines), StreamingAssets has 22 (718 lines); the delta is exactly the four `dev_disp_eq_royal` / `dev_disp_eq_mil` / `dev_disp_eq_eco` / `dev_disp_singleton` fixtures present only in StreamingAssets.
- __DECISION (resolved by new evidence):__ __Promote, not delete.__ The entry says cross-check `_2_5` before deciding; the decisive fact found instead is that `Framework/Tests/Runtime/LODControllerEvaluateTests.cs` lines 524–530 reference all four IDs by name and assert real-config E2E displacement behavior through them (T5 equal-priority triple, T6c singleton). Deleting them breaks the PlayMode suite; they must become tracked source content.
- __HOW:__ Before copying, run a full bidirectional `Compare-Object` to confirm the delta contains ONLY right-side (`=>`) lines — i.e., source has nothing StreamingAssets lacks (first-40 sample showed none; confirm exhaustively). Then copy `StreamingAssets/LivingRoom/config.json` → `Apps/LivingRoom/config.json` (the StreamingAssets copy is itself a valid `JsonUtility` serialization from a prior `Save All to JSON`, so copying preserves field ordering/defaults — honoring the entry's no-hand-editing intent without needing to drive the Editor UI). Re-hash both; must match. If any left-side (`<=`) delta appears, STOP and re-plan — it would mean source has newer content that must win instead.
- __WHERE:__ `TileStories\Assets\Apps\LivingRoom\config.json`, `TileStories\Assets\StreamingAssets\LivingRoom\config.json`.
- __WHY:__ §13's single-source-of-truth workflow is meaningless if the two copies silently diverge; and the dev fixtures being live in builds while absent from source means any future source edit + `Copy to StreamingAssets` would silently destroy required test content.
- __TESTS:__ Shell evidence only — paste both SHA256 hashes (must be equal) into the status row. Then run the PlayMode suite once (confirms `LODControllerEvaluateTests` still green with the now-canonical file).
- __FINISHING:__ Flip Index row 6 + row 2.3-l noting "promoted (step 2 chosen); reason: dev_disp_* referenced by name in LODControllerEvaluateTests T5/T6c". Correct `_2_3_Marker_Hierarchy.md` §16's `[x] in sync` checklist claim only if it is actually false on disk after this fix (it should then be true).

### Task 2.2 — Warn on unset `hierarchy_level_key` (Index Step 7; entry 2.3-j)

- __WHAT:__ In `ValidateHierarchyLevelKeys()` (`ConfigValidation.cs` line 15), replace the silent `continue` on empty key (line ~33) with an `EditorAlertItem` whose problem text distinguishes "never assigned" from stale-reference, keeping the stale-key branch and empty-table edge case untouched.
- __HOW:__ Exactly the code shape given in the `_2.7` entry (empty-key issue with `<empty>` value, fixHint branching on `levelKeys.Count == 0`: "Add at least one hierarchy level row..." vs. "Assign one of: {keys}"). Do NOT touch `MarkerHierarchyResolver`/`MarkerView` — runtime Fallback behavior stays intentionally correct; only Editor visibility changes. Note for implementation: the method is a private instance member — follow whatever instantiation pattern `POIAuthoringToolWriteBackTests.cs` uses for reaching window members in EditMode (read it first); if it uses `EditorWindow.GetWindow<>` + reflection-free internal access, mirror that; opening the method to `internal` is acceptable only if the existing pattern requires it (Runtime-style InternalsVisibleTo already covers the Editor test assembly).
- __WHERE:__ `Assets\Framework\Editor\POIAuthoring\ConfigData\POIAuthoringToolWindow.ConfigValidation.cs`; new test file `Assets\Framework\Tests\Editor\HierarchyLevelKeyValidationTests.cs`.
- __WHY:__ §11b's point: a forgotten level assignment should surface at authoring time, not be discovered because a marker "looks generic".
- __TESTS:__ New EditMode file, cases: (a) null key → 1 issue containing "never assigned"; (b) "" key → same; (c) unknown key → existing stale-key text unchanged; (d) valid key → 0 issues; (e) null/empty hierarchy_levels table + empty key → the "Add at least one hierarchy level row" fixHint variant. Then full EditMode suite (sibling validators in `ValidateAndAlert` must still pass together).
- __FINISHING:__ Flip Index row 7 + row 2.3-j with raw suite output reference.

### Task 2.3 — Wire the orphaned density-threshold detector into validation (Index Step 8; entry 2.4-d)

- __WHAT:__ Add `ValidateDensityThresholds()` calling the existing, correct, unit-tested-but-orphaned `LODController.IsDensityConfigValid(LodSettings)` (LODController.cs line 887), and add one `issues.AddRange(ValidateDensityThresholds());` line inside `ValidateAndAlert` (line 81 area) — the aggregator already fires on Load Config and pre-Save (ConfigFileIO.cs), so no other call sites.
- __HOW:__ Verbatim method body from the `_2.7` entry (null-guard `lod_settings`, single alert item: poiId `<LOD settings>`, problem text explains the silent DensityFactor==1.0 no-op, fixHint offers lower/raise/Suggest Values). Do NOT modify `IsDensityConfigValid` or `DensityFactor`.
- __WHERE:__ Same `ConfigValidation.cs`; new sibling test file `Assets\Framework\Tests\Editor\DensityThresholdValidationTests.cs`.
- __WHY:__ The exact scenario §6 warned about reproduces today (Shrink Start=6 / Cluster Min=5 saves with zero warning); wiring an existing detector is cheaper and safer than writing a new one.
- __TESTS:__ Cases: (a) shrink < cluster_min → 0 issues; (b) equal → 1; (c) reversed → 1; (d) `lod_settings == null` → 0. Full EditMode suite after (batched finishing with Task 2.2 per the entry).
- __FINISHING:__ Flip Index row 8 + row 2.4-d; batch the tracker/status updates with Task 2.2's.

### Block 2 — TESTS summary

No vision or human handover needed: 2.3-l is shell-verifiable (hash equality); 2.3-j/2.4-d are pure data-validation logic (Tier 0 only, per both entries' own reasoning). No new gallery/harness. Real code paths only, no mocks.

### Block 2 — FINISHING checklist

1. Per task: edit → re-read region → `refresh_unity` (zero `error CS`) → relevant new tests + full EditMode suite (zero failures) → flip `_2.7` index + detailed rows in the same session.
2. Task 2.1 additionally: paste hash evidence; run PlayMode once.
3. Tasks 2.2+2.3 share one combined finishing pass (same file family).
4. Update `.clinerules/10-structure.md` only if new test-file folders appear (they don't — both go in existing `Tests/Editor/` root; tree lists individual files there, so add the two new test file entries).
5. Append decisions.md entries (dev_disp promotion decision; validator-pattern choice).
6. End-of-block chat summary distinguishing independently re-verified facts vs. taken from `_2.7`.

---

## Retrospective audit (Block 1) — summary

- Re-ran EditMode gate fresh this turn: __540/540 passed, 0 failures__ (job `6afc960c02c34cf793a2a3f1625cacb6`). PlayMode was verified green (57/57) immediately after Block 1's last code change; nothing code-touching changed since, so no re-run needed.
- No pending gaps from Block 1; no dead code introduced (`WireDominantIcon` retained deliberately per FW-1 decision, documented).

## Decisions

__a) Made autonomously:__ 2.3-l resolves to *promote* (not delete) — settled by the hard evidence that PlayMode E2E tests reference the four `dev_disp_*` IDs by name (this upgrades the master plan's earlier "sync, do not delete" assumption into a proven requirement); file-copy direction StreamingAssets→source chosen because the copy is itself a prior `JsonUtility` save (honors no-hand-editing intent) contingent on the exhaustive one-directional delta check.

__b) Requiring developer input:__ none for Block 2.

##

# Block 3 — Test additions + two scoped accessibility/config fixes (Steps 9–11 & re-scoped 18) — mostly CAN'T REGRESS, two small code touches

__Scope:__ the original six pure test additions (Tasks 3.1–3.2 per `_2.7`; Task 3.4 per 2.6-af) __plus__ the real minimap tap-target fix coerced by your Decision 3.3 (Task 3.3) __plus__ the 2.6-d master-toggle (moved up from Block 8 as Task 3.5). All Tier 0 / Tier 0.5, deterministic, no vision, no device. One code touch in `MinimapView.cs`, one in `POIAuthoringToolWindow.SearchFilter.cs`, one `WallConfigData` field pair, one `WallConfigData` flag — all additive and back-compatible. __Grounding verified this session:__ `UIAccessibility` is Runtime-hosted (`Runtime/UI/Shared/UIAccessibility.cs`: `ContrastRatio`, `MeetsContrastAA`, `MeetsMinTapTarget`) → freely callable from Editor & Runtime test asms (2.5-q caveat resolved, no duplication). LOD pipeline methods are `LODController` instance methods (`ComputeEffectiveDistances` divides by `ARZoomState.ZoomFactor` already). Placement analyzed; every test lives in an existing test file under the pattern each file already owns.

### Task 3.1 — LOD Phase A: shallow-angle + zoom-unlocks markers (Index Step 9; entry 2.4-p) — __both tests, same file__  [x] DONE 2026-08-26

- __WHAT:__ (1) *Shallow-angle/foreshortening* proof of the §2 core invariant: markers are thinned per real 3D distance, not per "camera is close to the wall". (2) *Zoom-unlocks-markers* proof of §10's reason zoom exists: changing `ARZoomState.ZoomFactor` changes effective distance → changes visible-set. These two are the most load-bearing claims in Domain 2.4 and currently rest only on "the formula looks right".

- __HOW:__

  - __Shallow test__ (`LODControllerTests.cs`): fabricate a straight row of 10 markers spaced 0.5m on a line (positions `t = k*0.5`, `k=0..9`, y=0); place a virtual `Camera` near the start, rotated ~5-10° off the wall axis (true 3D ~0→~4.5m across the row, all in frame). Call `ComputeEffectiveDistances(markers)` (ZoomFactor=1) → assert returned distances strictly increase and match true ordering; feed `AssignBands` → assert far end falls in a strictly higher band index than near end.
  - __Zoom test:__ fixture where real positions land in different bands at different `ZoomFactor`. At 1×: some markers sit in the `9999/5` outer band (capped → several hidden); at 3×: effectiveDistance (`realDistance/3`) drops them into `7/15`/nearer → all visible. Run `ComputeEffectiveDistances`→`AssignBands`→`ApplyCountCap` at both zoom values with identical real positions; assert visible-count strictly greater at 3× vs 1×. This operates directly on the pipeline — does NOT need Block 5's camera-FOV hookup to be meaningful (per 2.4-p's own note).
  - Always reset `ZoomFactor` to `1f` in `finally`/`TearDown` (it's static) so no test leaks state.

- __WHERE:__ `Assets/Framework/Tests/Editor/LODControllerTests.cs`.

- __WHY:__ these two invariants are the domain's core and lacked a deterministic assert while every sibling claim (§7 hysteresis, §6 density, §8 frustum) has one.

- __TESTS:__ the two methods are the tests. Full EditMode suite green + paste output.

- __FINISHING:__ flip `_2.7` Index row 9 + row 2.4-p; per 2.4-p's own finishing, correct `_2_4_Marker_LOD.md` Implementation Status rows 14/15 (currently premature "DONE/green") to note found+closed.

### Task 3.2 — Displacement tap-target clearance (Index Step 10; entry 2.5-q)  [x] DONE 2026-08-26

- __WHAT:__ Tier 0.5 assert that a small hierarchy-level marker in a crowded, near-`max_displacement_px` group still resolves to a tappable (≥44×44 screen px) target after displacement — the self-inflicted accessibility regression class §12 catches.
- __HOW:__ add to `DisplacementGalleryTests.cs` (Runtime/PlayMode real-prefab harness already present): fabricate a small marker in an overlapping group forcing near-`max_displacement_px` offset, run real displacement, assert `UIAccessibility.MeetsMinTapTarget(w,h)` on the final resolved screen-space hit rect. __Read `MarkerView`'s actual hit-target definition (`RectTransform`/Collider) first__; case may well pass by construction (offset ≠ shrink) — that's still a valid regression guard per §12 "confirm with the test, not the audit".
  - __Assembly resolved:__ `MeetsMinTapTarget` is Runtime-accessible → call directly from Runtime tests, zero duplication.
- __WHERE:__ `Assets/Framework/Tests/Runtime/DisplacementGalleryTests.cs` (new method).
- __WHY:__ the one accessibility axis Displacement could self-corrupt (position+small base size → unreachable target). Reusable utility already exists; Displacement just never calls it.
- __TESTS:__ this IS the test. PlayMode run, paste raw.
- __FINISHING:__ flip `_2.7` Index row 10 + row 2.5-q.

### Task 3.3 — REAL minimap fix: split dot visual-size vs tap-target size, both configurable; then test (merged from original 2.6-af minimap dot check, per Decision 3.3)  [x] DONE 2026-08-26

- __WHAT:__ Today `MinimapView.DOT_SIZE_PX = 20f` is used both for the visual `dot` and for its tap receiver (it registers `MouseDownEvent` itself) — so "dots are tappable at ≥44px" is structurally impossible. Fix: split into (a) `minimap_dot_size_px` (default 20f; the dot's visual) and (b) `minimap_dot_tap_target_px` (default 44f; an invisible, centered hit-zone per dot — the actual tap receiver). Overlapping hit-zones resolved by __nearest-dot-center__ (dense maps: expected, handled). Both fields __developer-editable in the Editor Tab__. Then Tier 0.5 test(s) assert: each dot's final screen-space hit rect meets `MeetsMinTapTarget` (reading a real reset __from the built UI__, not a hardcoded value).

- __HOW:__

  - `WallConfigData.cs` Minimap block (lines 143–148): add `public float minimap_dot_size_px = 20f;` and `public float minimap_dot_tap_target_px = 44f;` — additive, `JsonUtility` backfills missing → defaults, __zero migration__ for LivingRoom JSON.
  - `MinimapView.cs`: replace `const DOT_SIZE_PX` with a runtime value read from `_config.minimap_dot_size_px`; keep a visual child (size = dot size) and add/hold a hit-zone VisualElement sized `minimap_dot_tap_target_px`, centered on the dot, the one that owns the `MouseDownEvent`. Implement nearest-dot-center on the parent when hit-zones overlap (don't dodge overlap — resolve it).
  - `POIAuthoringToolWindow.SearchFilter.cs` (Minimap sub-section, around lines 46–52): add two `DrawFloatField("Dot size", _config.minimap_dot_size_px, ...)` / `DrawFloatField("Tap target", _config.minimap_dot_tap_target_px, ...)` rows using the established helper pattern, with help text.
  - Test: `MinimapViewTests.cs` or a sibling — a real minimap with N POIs, assert computed/composed hit-rect `MeetsMinTapTarget(dot_size…)` reads true from the *authored* 44 default (and fails if a test overrides to <44 to prove the writer is honest). Keep the "AR marker is also a larger target" only as a coordinating secondary note, not the fix.

- __WHERE:__ `Assets/RunTime/UI/Minimap/MinimapView.cs`, `Assets/RunTime/Core/WallConfigData.cs`, `Assets/Framework/Editor/POIAuthoring/GlobalScene/POIAuthoringToolWindow.SearchFilter.cs`, `Assets/Framework/Tests/Editor/UI/Minimap/MinimapViewTests.cs`.

- __WHY:__ closes the WCAG gap for real (not by exception), uses the framework's "expose the tunable" philosophy, and the same additive-field default trick `_2.5` already used for leader-line leader/opacity (verified) keeps existing walls green.

- __TESTS:__ Tier 0.5 tap-target (this file's assertion now meaningful because the fix makes it true); EditMode suite green; paste raw.

- __FINISHING:__ flip `_2.7` Index row 11 + row 2.6-af __with a sub-note that the minimap dot sub-duty was resolved by a real product fix (dot/tap split + config) rather than an accessibility exemption — the user's Decision 3.3__. Add a decisions.md entry.

### Task 3.4 (folded into 3.3 finish) — Search/Filter menu style

*(No longer separate; see Block note that sub-Field FINISHING shared.)*

### Task 3.5 — NEW (from Block 8 Step 18 / 2.6-d) — master enable/disable toggle for Search/Filter/Select (do NOTTOGLE now, wrapper restructure explicitly left open)  [x] DONE 2026-08-26

- __WHAT:__ Add `bool search_filter_select_enabled = true;` (top-level, flattened — consistent with the existing ~20 flattened fields in that WallConfigData range) as the __single master switch__ the whole domain gates on, mirroring `LodSettings.enabled`/`DisplacementSettings.enabled`. When false, no Search/Filter/Select UI Toolkit panel is created/attached and `MinimapView`/`MarkerSelectable` don't activate. Backward-compatible: `JsonUtility` → missing → `true`, no migration.
- __HOW:__ in `WallConfigData.cs` add the flag; locate the __single right place__ where `WallSession` (or equivalent) currently spawns/activates the Search/Filter/Select panels and gate creation there (not redundantly in each view) — trace how the sibling `lod_settings.enabled`/`displacement_settings` gate is wired and that same call-site pattern. Confirm `MarkerSelectable`'s tap handler so it's also silent when disabled. Add Editor UI in `POIAuthorizationService.POIAuthoring ToolWindow.SearchFilter.cs` / `GlobalScene` (a top-level "Enable Enabling -> visual" toggle, grey the foldout under it, matching the "gate children on audit" pattern used for the existing `enabled` toggles in that foldout).
- __WHERE:__ `Assets/RunTime/Core/WallConfigData.cs`, the bootstrap/wiring in Search Overlay / WallSession, and Editor foldout.
- __WHY:__ clean consistency across all three domain wrappers (LOD, Displacement, Select/Filter/Search), the framework philosophy "gives each kind of wall the option to choose, including disable", and it closes a real capability gap at low risk.
- __TESTS:__ Tier 0: with `false`, assert no Search/Filter/Select UI Toolkit panel is created (structural: e.g. panel returns null) __and__ `MarkerSelectable` tap fires nothing dataset; with `true` (default) assert no regression. Sibling-toggle style, so no Tier 0.5/1/2 needed.
- __FINISHING:__ do not do the restructure (2.5's documented-options wrapper) now — per your decision it's explicitly __open, not scheduled__, revisited only if a second code-consistency reason surfaces. Rewrite `_4.6-d` and `_2.6` §3 code block (still stale re: `weight_*`) to show the __actual flattened__ field layout, not the never-built wrapper. Flip `_2.7` Index row for 2.6-d + detailed row, and update `_2.7` Step 18 from [BLOCKED] to [DONE] with the note "master toggle shipped in Block Three; wrapper restructure left explicitly open (not a delay, a decision)".

### NEW Block 3 — TESTS summary

All Tier 0 / Tier 0.5 (EditMode or Runtime as per file). __No vision or human tests required__ this block (Tier 0.5 is explicitly no-vision) → still __no content for `_2.7.1_Vision_Tests.md` / `_2.7.2_Human_Tests.md`__. Real code paths, no mocks; harnesses already exist (LOD bare-marker; DisplacementGallery prefab-camera; Minimap/Filter/Results real-UI). This block is no longer *pure* additive — Task 3.3 adds two config fields + a MinimapView tap structure, Task 3.5 a WallConfig flag + bootstrap gate + Editor toggle — but all are small, additive, and back-compatible.

### Block 3 — FINISHING checklist

1. Per task: edit → re-read edited region → Unity MCP `refresh_unity` (zero `error`) → run new test + full EditMode suite (zero) → flip each `_2.7` index row + detailed row in the same edit. (Tasks 3.1 additionally correct `_2_4` §12 Implementation Status 14/15; Task 3.5 rewrite `_2.6` §3 block to the real flattened layout and mark Step 18 [DONE].)
2. Run new Runtime(); (Task 3.2) in PlayMode (`run_tests` PlayMode + `get_test_job`).
3. Update `.clinerules/10-structure.md` if any file is created — only `POIAddNew`… none should be newly created (all in existing files). If Task 3.3's config fields or Task 3.5's flag need a data-schema doc line in `_2.6` §8/§3, update those too.
4. Add decisions.md: Decision 3.3 real-fix record, the JSON-back-compatibility-defaults trick, the single-gate-point for 2.6-d, and the "done now; leave restructure open, not scheduled" note for 2.6-d.

---

## Block 8 — Remaining Steps (19-21 + new 22-24) — OPEN

### Task 22 (Step 17 / 2.6-al-wiring) — Synonym feature completion [x] DONE 2026-09-08

- __WHAT:__ Wire the existing `ConfigureWithSynonyms` method into the runtime bootstrap so synonym search actually works end-to-end. Four sub-tasks — ALL DONE 2026-09-08:
  1. [x] Add `List<SynonymGroup> synonym_groups = new();` field to `WallConfigData.cs` (matches the pattern of every other taxonomy list).
  2. [x] Bootstrap in `WallSession.LoadConfigCoroutine`: create `_searchIndex = new POISearchIndex(); _searchIndex.Build(_config); if configured, ConfigureWithSynonyms(_config.synonym_groups);`. Exposed publicly via `WallSession.SearchIndex`. **Note:** POISearchIndex was NOT created anywhere in runtime code before this (only in tests) — Task 22 created it.
  3. [x] Add Editor UI in `POIAuthoringToolWindow.SearchFilter.cs`: a "Synonym Groups" foldout + `DrawSynonymGroupsTable()` (key + comma-separated synonyms + add/remove).
  4. [x] Delete `Assets/Framework/Editor/POI/SearchSynonymGroups.cs` + `Assets/SearchSynonyms.asset` (+ .meta). Test file `SearchSynonymGroupsTests.cs` RETAINED but rewritten to test `POISearchIndex.ConfigureWithSynonyms` against plain `List<SynonymGroup>` (not the deleted asset).
- __VERIFIED:__ zero `error CS` (refresh_unity + read_console 0 errors); EditMode 589/589; synonym tests 3/3. **PlayMode:** 5 `RealConfig_DisplacementPipeline_*` tests fail — these are the PRE-EXISTING flaky determinism tests documented in the baseline (lines 20-23, ±1px camera-projection tolerance). Provably unrelated to Task 22: those tests instantiate WallSession with `SetActive(false)`, so Awake/LoadConfigCoroutine (my code) never runs there. Not a regression from this task.

### Task 23 (new) — Doc-hygiene batch [x] DONE 2026-09-08

- __WHAT:__ Fix all remaining stale doc notes in one pass:
  1. **2.5-b:** Deleted `_2.5_Marker_Displacement.md` lines 61-68 (stale `ApplyCountCap` bug note).
  2. **2.6-ab:** `_2.6_Select_Filter_Search.md` §5/§8 `weight_*` notes already carry "CORRECTION (audit, 2026-08-25)" — verified up-to-date.
  3. **5.1-r:** Added missing partial files `LodZoom.cs`, `Displacement.cs`, `SearchFilter.cs` to `_5.1_Editor_Tab.md` §1.1 file table.
  4. **5.1-z:** Zoom fields (including `zoom_double_tap_window_s`, `zoom_double_tap_move_tolerance_px`) documented in `_5.1` `LodZoom.cs` description.
- __WHERE:__ `proj_guides/_2.5_Marker_Displacement.md`, `proj_guides/_2.6_Select_Filter_Search.md`, `proj_guides/_5.1_Editor_Tab.md`.
- __WHY:__ Stale docs are actively misleading — a future agent would waste time re-investigating fixed bugs.
- __TESTS:__ Doc-only, no tests needed.
- __FINISHING:__ flipped `_2.7` row 23 to [DONE].

### Task 24 (new) — Test handover guide expansion [ ] NOT STARTED

- __WHAT:__ Expand `_2.7.1_Vision_Tests.md` and `_2.7.2_Human_Tests.md` to cover Blocks 1-4:
  - **Vision Block 1 (LOD):** marker count changes with distance, band hysteresis, density response, frustum culling.
  - **Vision Block 2 (Selection):** highlight dims non-selected, zoom-on-select for dense only, SelectionEventBus round-trip.
  - **Vision Block 3 (Minimap/Filter/Results):** minimap dots match visible POIs, filter toggles update all surfaces, search filters results.
  - **Vision Block 4 (Filtering composition):** one result set across all four surfaces, hide vs dim.
  - **Human Blocks 1-4:** real-device interaction flows (tap marker -> detail card, toggle filter -> results update, search -> results -> tap -> highlight).
- __WHERE:__ `proj_guides/_2.7.1_Vision_Tests.md`, `proj_guides/_2.7.2_Human_Tests.md`.
- __WHY:__ These are the ONLY gate that can close Blocks 1-4 for vision/human validation.
- __TESTS:__ These ARE the test guides — no separate test files needed.
- __FINISHING:__ flip `_2.7` row 24 from [ ] to [DONE].

### Block 8 — FINISHING checklist

1. Per task: edit → re-read edited region → Unity MCP `refresh_unity` (zero `error`) → run new test + full EditMode suite (zero) → flip each `_2.7` index row + detailed row in the same edit.
2. Task 22: run new synonym bootstrap test in EditMode (`run_tests` EditMode + `get_test_job`).
3. Task 23: doc-only, no compile/test gate.
4. Task 24: doc-only, no compile/test gate.
5. Update `.clinerules/10-structure.md` to remove `SearchSynonymGroups.cs` from the tree (Task 22 sub-task 4).
6. Add decisions.md: Decision 22 (synonym wiring), Decision 23 (doc-hygiene), Decision 24 (test-guide expansion).
  4. Delete `Assets/Framework/Editor/POI/SearchSynonymGroups.cs` and `Assets/SearchSynonyms.asset`.
- __WHERE:__ `Assets/Framework/Runtime/Core/WallConfigData.cs`, `Assets/Framework/Runtime/Core/WallSession.cs`, `Assets/Framework/Editor/POIAuthoring/GlobalScene/POIAuthoringToolWindow.SearchFilter.cs`, delete `SearchSynonymGroups.cs` + `SearchSynonyms.asset`.
- __WHY:__ Closes the last remaining composition gap — `ConfigureWithSynonyms` exists but is never called.
- __TESTS:__ Tier 0: add one new test verifying the bootstrap path (build index, call ConfigureWithSynonyms, search for synonym term, assert expected POI returned).
- __FINISHING:__ flip `_2.7` row 17 + row 22; update `10-structure.md`; add decisions.md note.
4. Add decisions.md: Decision 3.3 real-fix record, the JSON-back-compatibility-defaults trick, the single-gate-point for 2.6-d, and the "done now; leave restructure open, not scheduled" note for 2.6-d.
5. End-of-block chat summary — flag what was independently re-verified on disk this block (minimap 20/44 & WallConfig defaults, one gate point in WallSession) vs taken from `_2.7`.

### Block 8 update — replace `Step 18 = 2.6-d`

`Step 18 2.6-d see 3.5: master toggle DONE in Block 3 via Task 3.5. Wrapper-class restructure remains EXPLICITLY OPEN and NOT SCHEDULED (breaking migration justified only by internal consistency; revisit only if a second code-consistency need surfaces). No longer blocking; 18 unblocked and absorbed.'`
---

# Block 9 — Position authoring: Add POI + Draft/Precise tabs (Step 19 = 1.2‑p, Step 20 = 1.2)  [ ] NOT STARTED

## Overview
Implement the missing **“Add POI”** entry point (Step 19) and lay the groundwork for the full **Draft / Precise Position redesign** (Step 20). The block stays small, editor‑only, and fully testable in EditMode.

---
### Sub‑task 9.1 – “Add POI” button  (Step 19)  [x] DONE 2026-09-08
- **WHAT:** In the Specific‑Marker tab, a “+” button that appends a new `POIData` (generated GUID, sensible defaults) to `_config.pois`, wraps the mutation in `DrawConfigMutationScope`, and calls `RefreshRigVisuals()` so the new POI appears immediately in the rig. The new POI starts in **Draft** mode so the author can move it in‑scene right away.
- **HOW:** Add `DrawAddPoiButton()` in `POIAuthoringToolWindow.SpecificMarker.cs` (inside the Position fold‑out). The helper creates a `POIData { id = Guid.NewGuid().ToString("N"), name = "New POI", category = "default", x_norm = 0.5f, y_norm = 0.5f }`, pushes it into `_config.pois` inside a `DrawConfigMutationScope` (so undo/history works), then calls `RefreshRigVisuals()`. No new runtime code.
- **WHERE:** `Assets/Framework/Editor/POIAuthoring/SpecificMarker/POIAuthoringToolWindow.SpecificMarker.cs`.
- **WHY:** Gives authors a single‑click way to create POIs; matches the existing per‑POI editing flow (the new POI gets its own fold‑out with Position / Marker Style / Badge Style / Outline sections ready to fill).
- **TESTS:** Tier‑0 `POIAuthoringAddPoiTests` – verify list length increments, GUID is unique, rig refresh creates a `POIAnchor` with the new id, and JSON round‑trip preserves the new entry. No PlayMode test needed.
- **FINISHING:** flip `_2.7` row 19 → [DONE]; add `decision.md` entry.

## §8.1 / §8.2 — Open questions & scope notes

- __No architecture reset needed.__ Both decisions fit the existing patterns (per-stream master toggle; per-wall tunable fields). Neither requires dep-loop decision before Act.
- __Minor consequence to note in `_2.6` §8 when implementing:__ the `minimap_dot_*_px` defaults (20/44) become the minimum-documented product; mention the nearest-center overlap-res too.
- __No duplicate-class risk__ found: the new fields are additive to existing `WallConfigData` (not wrappers), and the Editor rows call the same `DrawFloatField` helpers as all the other numbers — no `Utility.cs` drift.

---

## Decisions recap

- __a) Auto-decided:__ adopt-miner fields as additive-with-defaults (back-compat); route nearest-center on overlapping touch zones; master-toggle single gate-point at `WallSession`; 3.3 uses real config-tunable rather than exemption.
- __b) None left pending:__ both your decisions are final and non-BLOCKed; nothing remains gated.
### Sub‑task 9.2 – Draft / Precise tabs inside Position fold‑out (Step 20 – skeleton)  [x] DONE 2026-09-08
- **WHAT:** Replace the single Position section with two tabs **Draft** and **Precise**.
  * **Draft tab** – shows only `x_norm` / `y_norm` fields plus a **Capture Position** button that writes the current scene‑rig transform into `captured_position` and sets `has_captured_position = true`.
  * **Precise tab** – shows the captured‑position fields (`captured_position.x/y/z`, `captured_position_source`, `captured_position_timestamp`) **read‑only** while `has_captured_position` is true, plus a **Clear** button to revert to Draft.
  * Switching to **Precise** when `has_captured_position == false` shows an inline warning (“Capture position first”).
- **HOW:** Create a new partial file `POIAuthoringToolWindow.PositionTabs.cs` under `SpecificMarker/`. Use the existing `DrawTabContentContainer` + `DrawFramedFoldout` helpers; tab state stored in a private `enum PositionTab { Draft, Precise } _positionTab` per POI (dictionary keyed by POI id). The Capture button calls `CapturePositions()` (already exists in `RigLifecycle`) for the selected POI only.
- **WHERE:** `Assets/Framework/Editor/POIAuthoring/SpecificMarker/POIAuthoringToolWindow.PositionTabs.cs`; hooked from `DrawSpecificMarkerOptions` in `SpecificMarker.cs`.
- **WHY:** Separates quick placement (Draft) from high‑precision survey capture (Precise) exactly as spec 1.2‑b describes; the “auto‑lock Precise when moving in scene” rule is enforced by the Capture button – moving the rig only updates Draft normals; Precise stays frozen until the author explicitly captures.
- **TESTS:** Tier‑0 `PositionTabsTests` – switching tabs shows/hides correct fields; Capture button sets `has_captured_position` and populates captured fields; Clear button resets flag; JSON round‑trip retains `captured_position` and flag. No PlayMode test.
- **FINISHING:** flip `_2.7` row 20 → [WIP] with skeleton note; add `decision.md` entry.

# Block 4 — Highest-priority functional gap: filtering composition (Step 12 = 2.6-i)

__Scope:__ make facet filtering actually affect the app across all four result surfaces — results list, scene markers, minimap, camera-highlight. This is the single most load-bearing composition gap in the whole audit (§4.2.1's canonical instance: every piece unit-tests green in isolation, the seam was never built). __Grounding verified on disk this session (audit finished before Act):__

- `POISearchIndex.Search(string query, SearchMatchMode = Any)` (POISearchIndex.cs:312) has __no__ `candidatePoiIds`; empty query short-circuits to empty (line 316).
- `FilterTrayView.OnFiltersChanged` (event, :30) has __zero subscribers__; `GetActiveCategories/BadgeCategories/OutlineLevels/HierarchyLevels` accessors (:163-172) exist and are correct.
- `FilterFacetEvaluator.PoiPassesFilters(POIData,...)` (:14) is public, pure, stateless — the exact "which POIs pass active facets" logic; just no public set-builder exposed.
- `ResultsListView.RefreshResults(query, matchMode)` (:163) calls `_searchIndex.Search(query, matchMode)` directly; keeps its own `_searchIndex` + `_config`.
- `MinimapView._searchIndex` (:19/34) assigned once in `Initialize`, __never read again__ (dead config foreshadowing §8's intended integration).
- `WallSession.SpawnedMarkers` is `public IReadOnlyList<MarkerView>` (:39).
- `MarkerView.SetVisible(float targetAlpha, float fadeDuration)` (:846) is the documented __LOD-coexistent__ alpha seam (`_highlightAlpha`), distinct from `SetVisible(bool)` and from `MarkerRevealEffect.Play` (initial-spawn scale+fade, not per-toggle).
- `SelectionHighlightController` is a plain `IDisposable` that dims via `MarkerView.SetVisible(float, float)` on single-selection event only; has no result-set concept.
- `ViewModeControl` line 148-149 comment "SelectionHighlightController handles dimming" is __misleading__ (it only does single-tap selection); line 146 hides the filter tray in `CameraHighlight` mode.
- The PlayMode Search suite `SearchOverlayRuntimeTests` is the domain's existing Phase-B-style harness (job `cc79...` showed it passing 58/58 PM).

---

### Task 4.0 — Integration test FIRST (mandatory per `.clinerules/40-testing.md §4.2.1 / 2.6-i FINISHING)  [x] DONE 2026-08-26

### Sub‑task 9.3 – Validation hook for Precise tab  [x] DONE 2026-09-08
- **WHAT:** When the author selects the Precise tab while `has_captured_position == false`, display an inline `EditorGUILayout.HelpBox` (“Capture position first – press the Capture button in Draft tab”). The warning disappears once the flag is true.
- **HOW:** In `PositionTabs.cs` draw call, after tab header, `if (_positionTab == PositionTab.Precise && !poi.has_captured_position) EditorGUILayout.HelpBox(...);`
- **WHERE:** Same `PositionTabs.cs`.
- **WHY:** Early feedback, prevents silent bad data.
- **TESTS:** Tier‑0 – warning appears/disappears correctly.
- **FINISHING:** included in 9.2 flip.
- __WHAT:__ Before writing any production wiring, write the composition test that toggling a facet changes the result set across all four surfaces. This is the regression test §20 falsely claimed already passed; it is the thing that would have caught this entire gap. Write it as the first Block-4 artifact.
- __HOW:__ Fabricated `WallConfigData` (mirror the `MinimapViewTests`/`FilterTrayViewTests` SetUp pattern with a handful of POIs across 2 categories/2 hierarchy keys), drive the real `POISearchIndex` + real `FilterFacetEvaluator` + real `ResultsListView` + real `MinimapView` (after Tasks 4.1-4.4 land) + real `ResultSetCoordinator` (4.5). Sequence: build index → run `Search("", candidateIds=all)` → tap/activate facets → assert list rows / minimap visible dots / marker target list change together. Keep it in `Tests/Editor` (pure logic, no scene) per the existing harness pattern.
- __WHERE:__ `Assets/Framework/Tests/Editor/UI/Filter/FilterCompositionTests.cs` (new file; sibling of FilterTrayViewTests).
- __TESTS:__ `ToggledFacet_ChangesResultSet_AcrossAllSurfaces` (the killer regression), plus `EmptyQuery_WithActiveFilter_ReturnsFilteredSet`, `ClearFilters_RevertsToUnrestricted`. Full EditMode suite green.
- __FINISHING:__ Flip status where §20's false "asserts pass" claim is; note this test is the §4.2.1 precedent.

### Task 4.2 — `POISearchIndex.Search` candidate narrowing (2.6-i HOW step 1)  [x] DONE 2026-08-26

- __WHAT:__ Add optional `IReadOnlyCollection<string> candidatePoiIds = null`. Non-null non-empty → restrict scoring to those ids. Add empty-query-with-candidates behavior: when candidates are supplied and query is empty, return the full candidate set unscored (deterministic index order + name set) instead of the empty short-circuit.
- __HOW:__ precompute `HashSet<int> candidateIndices` from `candidatePoiIds` vs `_poiDatas[].id`; in the two scoring loops guard `RecordMatch`/insertion with membership; post-sort skip non-candidates. Add second eager return: `if (query empty && candidatePoiIds != null) { iterate _poiDatas; if candidateIndices contains index, add SearchResult(index, 0, id); sort by name (stable); return; }`.
- __WHERE:__ `Assets/Runtime/POI/POISearchIndex.cs` (~line 312).
- __WHY:__ backward-compatible default (`null` = old behavior); empty-query + filter is the #1 usage pattern (user opens tray, taps "Religious", sees results without typing).
- __TESTS:__ (a) candidatePoiIds narrows results even if a non-candidate would rank higher (excluded id never appears); (b) empty query + candidates → all candidates, deterministic order; (c) null/empty candidates → unchanged existing behavior (full suite); (d) clear filters = no candidates → unrestricted.
### Sub‑task 9.4 – Documentation & decisions  [x] DONE 2026-09-08
- **WHAT:** Update `_5.1_Editor_Tab.md` §1.1 file‑table to list the new `PositionTabs.cs`. Append a Block 9 entry to `Docs/decisions.md` summarising 9.1‑9.3.
- **WHERE:** `proj_guides/_5.1_Editor_Tab.md`, `Docs/decisions.md`.
- **FINISHING:** part of Block 9 checklist.

### Sub‑task 9.5 – Human Tier 2 zoom/gesture session (non‑code)  [ ] PENDING (developer to run on device)
- **WHAT:** Execute the H1‑H7 checklist from `_2.7.2_Human_Tests.md` on a device (mock or real Immersal). Record PASS/FAIL + one‑line observation per item.
- **WHERE:** Device build; hand‑over guide already expanded in `_2.7.1_Vision_Tests.md` and `_2.7.2_Human_Tests.md`.
- **WHY:** Only gate that can close 2.4‑l / 2.4‑m (FOV write + gestures) on hardware.
- **FINISHING:** developer records results in `decisions.md` when done.

---

## Block 9 — FINISHING checklist
1. Edit → re‑read → `refresh_unity` (force, compile) → **zero `error CS`**.  
2. Run new EditMode tests (`POIAuthoringAddPoiTests`, `PositionTabsTests`) + full EditMode suite → **589/589 green**.  
3. PlayMode suite unchanged → verify no regressions.  
4. Delete any `__*.py / __out.txt` scratch files.  
5. Update `__curr_plan_tracker.md` (flip row 19 → [DONE], row 20 → [WIP] with note).  
6. Update `.clinerules/10‑structure.md` (add `PositionTabs.cs`).  
7. Append `decisions.md` (Block 9 entry).  
8. End‑of‑block chat summary – list files changed, test counts, and any open non‑code flags (2.3‑n/o/p, 2.4‑…, 2.5‑t/u, 2.6‑aa/aj) noted for later manual verification.

---

## Non‑code flags (2.3‑n/o/p, 2.4‑…, 2.5‑t/u, 2.6‑aa/aj)
These are **manual‑only verification items** recorded in `_2.7_Corrections_TODO.md` (e.g. “run PlayMode suite on device”, “check minimap contrast on‑device”). They do **not** block code work; we simply acknowledge them here and will schedule a separate manual‑review session after Block 9.
---

# Block 10 — All-green closeout (2.6-af Tier-0.5 checks, vision pass, doc cleanup) — 2026-09-08

**Scope:** everything that can be closed without a human/device session. Explicitly OUT of scope: human Tier-2 device session (H1-H7) and scheduling the manual non-code flags.

### Task 10.1 — 2.6-af: filter-chip tap-target test + results-list contrast test (design-token decision)  [x] DONE 2026-09-08
- **WHAT:** close the two remaining 2.6-af gaps: (a) Tier-0.5 test asserting every rendered filter-tray facet chip meets the 44px minimum tap target; (b) deterministic WCAG contrast assert for the results-list text/background pair. The blocker ("backgrounds are theme-resolved with no explicit pair") is resolved by a design-token decision: introduce `UIPalette` (Runtime/UI/Shared/UIPalette.cs) — `SurfaceDark` (0,0,0,0.85), `TextPrimary` (white), `TextSecondary` (0.78 grey) — matching DetailCardView's established 0.85-black + white visual language — authored explicitly on the results list rows/container and the filter tray container/headers.
- **HOW:** create `UIPalette.cs` (pure static Color constants). Apply in `ResultsListView` (list container bg, row unselected bg, name/category/empty-state colors) and `FilterTrayView` (tray container bg, section header colors, empty-state color). Change `CreateUI` from `private` to `internal` in `MinimapView`, `FilterTrayView`, `ResultsListView` (parity with `DetailCardView`'s InternalsVisibleTo seam). Tests read the REAL authored style values off built VisualElements (no hardcoded assumptions), per the 2.6-af HOW.
- **WHERE:** new `Assets/Framework/Runtime/UI/Shared/UIPalette.cs`; edits in `Runtime/UI/Results/ResultsListView.cs`, `Runtime/UI/Filter/FilterTrayView.cs`, `Runtime/UI/Minimap/MinimapView.cs`; new test `Assets/Framework/Tests/Editor/UI/Shared/SelectFilterAccessibilityTests.cs`.
- **WHY:** these are the exact §17 Phase A Tier-0.5 gates the domain doc requires; the palette closes the "no deterministic pair" blocker with the smallest possible surface (one constants file), honoring the no-hardcoded-visual-values rule.
- **TESTS:** new EditMode tests: (1) every `facet-toggle-*` authored minHeight >= 44; (2) tray container authored bg == `UIPalette.SurfaceDark`; (3) results list container bg == SurfaceDark + row template label colors == palette; (4) palette text colors meet WCAG AA (>= 4.5:1) against SurfaceDark composited over BOTH black and white (worst-case camera feed); (5) every `minimap-hit-*` authored size >= 44. Full EditMode + PlayMode suites green after.
- **FINISHING:** flip `_2.7` index row 11 + per-domain row 2.6-af + detailed section header to [DONE] with evidence; add decisions.md entry (design-token decision recorded).

### Task 10.2 — DetailCard close-button tap-target test (2.6-y)  [x] DONE (already shipped — verified, no new work)
- **WHAT:** verify, not re-implement: `UIAccessibilityTests.MeetsMinTapTarget_DeveloperCheck_DetailCardCloseButton` already builds the REAL DetailCardView UI and asserts the close button is >= 44x44; `DetailCardView.CreateUI` authors width/height = 44 (spec _2.7 #2.6-y comment). The test is part of the green EditMode suite.
- **FINISHING:** confirm the 2.6-y status row reflects [DONE]; if still open, flip with this evidence.

### Task 10.3 — Vision-agent pass over Blocks 1-4 (headless-best-effort)  [x] DONE 2026-09-08 (headless portion; interactive items flagged for human)
- **WHAT:** execute the headlessly-verifiable portion of the `_2.7.1_Vision_Tests.md` Blocks 1-4 checklist: enter Play Mode on LivingRoomScene via Unity MCP, capture game-view screenshots (default LOD view; zoomed view), record per-item falsifiable observations. Interactive-only items (marker tap selection, filter toggling, typing) explicitly marked "needs interactive/human" rather than guessed.
- **HOW:** `manage_editor` play -> `manage_camera` screenshot (game_view, include_image) -> vision evaluation -> append a dated "Run record" section to `_2.7.1_Vision_Tests.md`. Stop play mode afterwards.
- **WHERE:** `proj_guides/_2.7.1_Vision_Tests.md` (appendix), PNGs under `Assets/Screenshots/`.
- **WHY:** Tier 1 runs after Tier 0/0.5 pass; the headless portion shrinks the remaining human session to interactive-only items.
- **TESTS:** the run record IS the deliverable; no code changes.
- **FINISHING:** decisions.md note that the headless vision pass ran and which items remain human-only.

### Task 10.4 — Doc cleanup: remove dead `SearchSynonymGroups` claims  [x] DONE 2026-09-08
- **WHAT:** `_2.6_Select_Filter_Search.md` Implementation Status row 5 still says the synonym feature "never reaches the running app" — stale after 2.6-al landed. Update to DONE with wiring summary + pointer to `_2.7` rows 17/22. Grep sweep `proj_guides/*.md` + `.clinerules/10-structure.md` so no doc states "currently unwired" as present-tense fact (historical/audit records may keep the name).
- **WHERE:** `proj_guides/_2.6_Select_Filter_Search.md` + grep sweep.
- **FINISHING:** the _2.6 row 5 flip IS this task's deliverable.

### Block 10 — FINISHING checklist
1. Per task: edit -> re-read region -> `refresh_unity` (zero `error CS`) -> new tests + full EditMode suite (zero failures) -> PlayMode suite (zero failures).
2. Flip `_2.7` rows (11 / 2.6-af / 2.6-y if needed) in the same edit as the work.
3. Update `10-structure.md` with `UIPalette.cs` + `SelectFilterAccessibilityTests.cs` entries.
4. Append `decisions.md` entry (palette design-token decision + headless vision pass record).
5. No scratch files left on disk.

### Task 4.3 — `FilterFacetEvaluator`: expose candidate-set builder  [x] DONE 2026-08-26

- __WHAT:__ Add `public static HashSet<string> GetFilterCandidateIds(List<POIData> pois, HashSet activeCass, ...activeBadge, ...activeOutline, ...activeHierarchy)` returning the ids of every POI where `PoiPassesFilters` is true (empty active sets → all ids). Reuses the existing public `PoiPassesFilters` (no duplication).
- __WHERE:__ `Runtime/UI/Filter/FilterFacetEvaluator.cs` (note: lives under UI/Filter, not POI/ — verified on disk).
- __WHY:__ the entry explicitly says "expose existing tested logic publicly rather than duplicating" — this is that single new access method.
- __TESTS:__ fabricated POIs; active filter → only matching ids; no active → all ids; null/bad id-best → [] . Extend `FilterFacetEvaluatorTests`.

### Task 4.4 — `ResultsListView.RefreshResults` candidate overload + `MinimapView` filter support  [x] DONE 2026-08-26

- __WHAT (Results):__ add `RefreshResults(string query, SearchMatchMode mode, IReadOnlyCollection<string> candidatePoiIds)` forwarding `candidatePoiIds` to `Search`; keep the existing 2-arg overload delegating with `null` (so untouched callers/voice path keep working). Store `_filterCandidateIds` so filter toggles can re-run without the query.
- __WHAT (Minimap):__ add `public void SetFilterCandidateIds(IReadOnlyCollection<string> ids)` (and internal `_filterCandidateIds` + `_anyFilterActive`), hiding/showing each `_hitZones[id]`/dot via `style.display` when a filter is active. When no active filter → all dots visible (existing). Wire the currently-dead `_searchIndex` field's §8 intent only (the coordinator pushes ids; the field may be retained for parity but dot toggling is what matters).
- __WHERE:__ `Runtime/UI/Results/ResultsListView.cs`, `Runtime/UI/Minimap/MinimapView.cs`.
- __TESTS:__ extend `ResultsListViewTests` (candidate-set narrows its rows) + `MinimapViewTests` (`SetFilterCandidateIds` hides non-matching dot, restores all when cleared).

### Task 4.5 — `ResultSetCoordinator` (the actual seam) — NEW plain C# class  [x] DONE 2026-08-26

- __WHAT:__ Single orchestrator that turns "facet change or search submit" into one refreshed result set pushed to list + minimap + scene markers + highlight. Pure class, constructor-injected deps, subscribes to `FilterTrayView.OnFiltersChanged`; exposes `RefreshSearch(query, mode)` to be called by the search input.
- __HOW (constructor):__ `(POISearchIndex index, WallConfigData config, FilterTrayView tray, ResultsListView results, MinimapView minimap, Func<IReadOnlyList<MarkerView>> getSpawned, SelectionHighlightController selHighlight)`. `RefreshSearch(query, mode)`: candidateIds = `FilterFacetEvaluator.GetFilterCandidateIds(...)` (Task 4.3); dispatch: `results.RefreshResults(query, mode, allCandidate ? null : candidateIds)`; `minimap.SetFilterCandidateIds(candidateSet)`; for each marker in `getSpawned()` → `m.SetVisible(inSet ? FULL : mismatchAlpha, fade)` using the alpha seam per `filter_marker_behaviour` (see Task 4.6); `highlights.SetResultSet(candidateSet)` (Task 4.7).
- __WHERE:__ new `Assets/Framework/Runtime/UI/Navigation/ResultSetCoordinator.cs`, owned/created by `SearchOverlayView` (extend its `Initialize` + `SubmitSearch` to call `coordinator.RefreshSearch`).
- __WHY:__ centralizes the composition in one plain, Tier-0-testable class (matches §20's "orchestrator= wiring-only, logic in plain classes"); also naturally gated by the 2.6-d toggle we shipped in Block 3 (when `search_filter_select_enabled == false`, SearchOverlayView.Initialize skips constructing the coordinator).
- __TESTS:__ Tier-0 on the coordinator (Task 4.1's composition test uses it); plus a direct unit asserting each surface receives the same candidate set. No visual pass needed.

### Task 4.6 — Scene-marker reveal/hide + per-wall `filter_mismatch_behaviour` Option  [x] DONE 2026-08-26

- __WHAT:__ after the coordinator computes the candidate set, visible/spawned markers get revealed/hidden (or dimmed) with the §7 CanvasGroup fade. Also add a __per-wall developer option__ `filter_mismatch_behaviour: "hide" | "dim"` selectable in the Editor foldout (framework philosophy: expose both good options, default to the spec's hide).
- __HOW:__ coordinator calls `MarkerView.SetVisible(targetAlpha, fade)` with `targetAlpha = 0` for "hide" or `DimAlpha` for "dim" on non-matching, and `SetVisible(1, fade)` on matching (when any filter active) — reusing the LOD-coexisting alpha seam so it doesn't fight `LODController` per-tick `SetVisible`. __Do NOT__ use `MarkerRevealEffect.Play` for toggling (that's the initial-spawn scale+fade; §7's "reveal via _set3 CanvasGroup-based fade" = the alpha seam, resolved by reading both entry points). Default behavior when no filter active: leave markers fully under LOD's control (coordinator no-ops).
- __WHERE:__ coordinator driver + new config field `WallConfigData.string filter_search_mismatch_behaviour = "hide"` + `WallEdropper` in `DrawGlobalSearchFilterSection` (drop `Hide`/`Dim` options + help).
- __WHY:__ gives developers the genuinely-useful both-way option (dims preserve positional context in dense walls); default stays the spec's §7 hide.
- __TESTS:__ Tier 0: coordinator with "hide" → `SetVisible(0)` unifying markers; "dim" → `SetVisible(DimAlpha)`. Editor round-trip for the new field (extend `LodAuthoringRoundTripTests`-style pattern).

### Task 4.7 — `camera_highlight` result-set highlight + stale comment fix  [x] DONE 2026-08-26

- __WHAT:__ extend `SelectionHighlightController` with `SetTargetCandidates(IReadOnlyCollection<string> inSet)` (reuses existing `SetVisible(alpha, fade)` dim seam) applied across all `SpawnedMarkers`; used by the coordinator when view mode is `CameraHighlight`. Fix the misleading `ViewModeControl` line 148-149 comment; __re-examine line 146__ and make the filter tray visible in `CameraHighlight` too (that mode is exactly when a visitor wants to filter while looking at AR) — an explicit decision from the entry.
- __WHERE:__ `Runtime/UI/Markers/SelectionHighlightController.cs`, `Runtime/UI/Navigation/ViewModeControl.cs`.
- __TESTS:__ the controller's `SetTargetCandidates` dims the disjoint set (mirror existing SelectionHighlightController tests); ViewModeControl a-a toggles tray visibility in CameraHighlight.

### Task 4.8 — Phase B (end-to-end) + doc corrections  [x] DONE 2026-08-26

- __WHAT:__ Real LivingRoom real config end-to-end via the existing PlayMode `SearchOverlayRuntimeTests` harness: load real config, run search + toggle a real facet in the real FilterTray UI, assert list/minimap/markers literally change together. Model on the existing `RealConfig_*` pattern in the domain's own Play suite.
- __WHERE:__ extend `Assets/RunFrame/Tests/Runtime/SearchOverlayRuntimeTests.cs` (or new sibling in `Tests/Runtime`).
- __FINISHING:__ after green: correct `_2.6` §20 checklist false claim (line ~709), add explicit sub-notes to `_2.6` Status item 10 (`FilterTrayView`) + item 9 (`ResultsListView`) that were overclaiming; record in `_2.7` and `decisions.md`; update `.clinerules/10-structure.md` (new `ResultSetCoordinator.cs`, new test files); if `filter_mismatch_behaviour` config field added, note in `_2.6` §7/§10 schema.

### Block 4 — TESTS summary

Tier 0 (the composition test) done FIRST; then per-task tests; Tier 0.5 accessibility re-run of `UIAccessibilityTests.cs` against the now-functional filter tray after wiring; Tier 1 = vision screenshot pass toggling a facet and confirming markers fade-appear with the intended transition (per 2.6-i §TESTS Tier 1) — handover guide for `_2.7.1_Vision_Tests.md` for that. No Tier 2 (no device-specific). No mock code — all real code paths + fabricated data.

### Block 4 — Decisions

- __a) Auto-decided:__ (1) orchestrator is a new plain-class `ResultSetCoordinator` owned by `SearchOverlayView`, not fattening any one view; (2) scene-marker mechanism = `MarkerView.SetVisible(float, fade)` alpha seam (LOD-coexisting), NOT `MarkerRevealEffect.Flip` (resolved by reading both); (3) all four surfaces get one shared candidate set (list/minimap/markers/highlight) per §7's "one result set"; (4) default user-facing scaffold = `filter_mismatch_behaviour=hide`, with `dim` as the exposed alternative; (5) empty-query-with-active-filter returns the full filtered set (the real usage pattern).
- __b) Requiring developer input:__ none blocking — but note the single design judgement: whether in `CameraHighlight` mode the filter tray should be visible (I recommend yes, and defaulted it), and whether you want the field named `filter_mismatch_behaviour` with the hide/dim choice or a different naming/label. Both are low-stakes and I've committed to the recommended default; tell me if you'd rather rename.

### Learning summary

- __What we're implementing:__ the seam that makes "Filter" a feature. Filter already renders, tracks state, computes correct relax-suggestions — everything except touching anything. Block 4 wires facets+search into ONE candidate set and pushes it to the results list, scene markers, minimap, and camera-highlight.
- __Key decisions (WHAT/HOW/WHERE/WHY/TESTS):__ (WHAT) one result set, four surfaces — the doc's core claim; (HOW) add an optional `candidatePoiIds` to `Search` (back-compat default), expose the existing pure facet-checker as a set-builder, and put the composition in a single plain `ResultSetCoordinator` so it's Tier-0 testable and `WallSession`/views stay thin (`20-code-quality` clears orchetrators); (WHERE) only the four surfaces + the coordinator + `WallConfigData` one field; no new systems; (WHY) a developer's framework promise is "pick real, working features per wall" and filter was the *most misleading* gap in the audit (everything looked done); (TESTS) write the integration test FIRST (that's the §4.2.1 rule — it would have caught the whole gap), then each unit; a real-config PlayMode E2E last closes the loop. Also: the differing-types-seam trick — reuse the `SetVisible(float alpha)` seam LOD already tolerates so our marker filtering doesn't fight LOD's own culler.

__8.2 open architectural items to watch before Act:__ (1) marker visibility must not conflict with `LODController` per-tick `SetVisible` — resolved by the alpha seam, but the coordinator should skip markers when `LODController` is actively re-culling (add one guard); (2) `ViewModeControl` is hidden filter tray in CameraHighlight — I've recommended showing it, but that's a product-behavior decision worth keeping flagged; (3) the `SearchResult` struct carries only `(PoiIndex, Score, PoiId)` — the empty-query+filter path needs a name-based ordering, so decide deterministic order = wall config order to avoid needing POI-name-in-Result (I defaulted to config order).Confirm none of these are as stated or raise concerns, then toggle to

Act Mode (⌘⇧A) and I'll execute Block 4 in its new form — refreshing the EditMode+PlayMode gate at the end.

# Block 5 — Plan (confirmed & grounded)

__Scope:__ `_2.7_Corrections_TODO.md` __Step 13 (2.4-l)__ + __Step 14 (2.4-m)__ — the AR camera zoom feature that, today, only mutates `ARZoomState.ZoomFactor` (a pure LOD input) and never zooms the actual camera, plus the missing pinch/double-tap input wiring. Both fix tasks + their Editor tunables + the doc corrections + Tier 1/Tier 2 handovers, done as one block (the entry mandates a single combined device pass for 2.4-l+2.4-m).

__Grounding verified this session (PLAN-mode disk audit + green suites run live):__

- `ARZoomMath` is a `public static class` __co-located inside__ `Assets/Framework/Runtime/Tracking/ARZoomController.cs` (lines 14–107) in namespace `TileStories`, not a standalone file. It already exposes `ComputeTapLevels`, `NextTapLevel`, `PreviousTapLevel`, `NextDoubleTapTarget`, `ApplyPinchScale`, `StepTowardTarget` — all pure, all Tier-0-tested in `Tests/Editor/ARZoomMathTests.cs`. __Additive__ helpers only.
- `ARZoomController` (the MonoBehaviour, lines 115–241): `Settings => _wallSession?.LodSettings` (L130), `Awake()` GetComponent fallback for `_wallSession` (L134), `Update()` (L140) that only steps `ARZoomState.ZoomFactor` toward `_targetZoom` via `StepTowardTarget`. It has __no `Camera` field, no `_baseFov`, no `Start()`, no `LateUpdate()`, and never writes `Camera.fieldOfView`__. Grep of all Runtime confirms `fieldOfView` is __only read__ — `LODController.cs` (frustum half-angle) and `MarkerLayout.cs` — never written. Task 5.1 has __no duplicate__ to worry about. 2.4-l's premise is exactly true.
- `ARZoomState.cs` (`Runtime/Tracking`): `static float ZoomFactor; SetZoom(value,min,max)` hard-clamps (`Mathf.Min`/`Mathf.Clamp`); `ResetToBase`. Consumed by `LODController.ComputeEffectiveDistances` only. Task 5.1 must NOT touch this file.
- __Gesture-API fork DECISIVELY resolved on disk:__ `ProjectSettings/ProjectSettings.asset:933` = `activeInputHandler: 1` → __new Input System only__ (legacy `Input.Input.touchCount` disabled project-wide). This settles 2.4-m: use `UnityEngine.InputSystem.EnhancedTouch`. This was the one fact `_2.7` explicitly could not verify (it lies outside `Assets/`); I confirmed it. Also `MockLocalizationProvider.cs` already calls `UnityEngine.InputSystem.Keyboard/Mouse`.
- Zoom UI buttons (already correct, nothing to change): `ZoomControlView.uxml` names `zoom-in-button`, `zoom-out-button`, `zoom-reset-button`; `ZoomControlView.cs` binds all three to `ARZoomController.ZoomIn/Out/ResetToBase`. (Note: the __older `10-structure.md`__ text still says `zoom_in_btn/reset_btn` — a stale doc name, corrected in Task 5.3.)
- Test-harness to mirror: `Tests/Runtime/ARZoomRoutingTests.cs` — active controller GO `AddComponent<ARZoomController>()` + __inactive__ `WallSession` GO (`AddComponent` on a `SetActive(false)` object so its `Awake` never fires a tracker), `SetField(ws,"_config",cfg)` + `SetField(zoom,"_wallSession",ws)`, with file-bottom static reflection helpers `SetField/GetField/InvokePrivate`. Task 5.1's new test adds `SetField(zoom,"_camera",cam)`.
- Editor surface: `DrawGlobalZoomSection()` lives in `Assets/Framework/Editor/POIAuthoring/GlobalScene/POIAuthoringToolWindow.LodZoom.cs:165`; existing zoom scalar rows at lines 176–181, last one `zoom_show_ui_buttons` (L181, `DrawToggleField`). The two new scalar rows insert __after line 181__. Help constants in `POIAuthoringToolWindow.Constants.cs` follow the `ZoomEnabledHelp`(L57)/`ZoomMin`(…)`ZoomUiButtonsHelp`(63) style.

---

### Task 5.1 — (`__2.4-l`): ARZoomController actually drives `Camera.fieldOfView`   [x] CODE+TESTS DONE 2026-08-26 — FOV driver (`_camera`/`_baseFov`/`_fovCaptured` + `Start()` capture + `LateUpdate()` write) added to `ARZoomController.cs`; new `Tests/Runtime/ARZoomCameraFovTests.cs` 4/4 green; full EditMode 561/561 + PlayMode 63/63 (incl. the 4 FOV tests) green via Unity MCP. Doc/`_2.7` status flip to pending-device in Task 5.3; ✅ only after Tier 2 human device check.

- __What:__ put the visible zoom back where the spec's premise (`_2.4` §9, title "AR camera zoom — global, FOV-based") says it belongs. `ARZoomState.ZoomFactor` already feeds LOD effective-distance; now also drives a real FOV so the passthrough + markers zoom together.

- __Where:__ `Runtime/Tracking/ARZoomController.cs` (only). New test file `Tests/Runtime/ARZoomCameraFovTests.cs`. Don't touch `ARZoomState` / `ARZoomMath` / `LODController`.

- __How:__ add

  ```javascript
  [Header("FOV")]
  [SerializeField] private Camera _camera;
  private float _baseFov; private bool _fovCaptured;
  void Start()  { if (_camera==null) _camera=Camera.main; if(_camera!=null){_baseFov=_camera.fieldOfView;_fovCaptured=true;} }
  void LateUpdate(){ if(_fovCaptured && _camera) _camera.fieldOfView = _baseFov / Mathf.Max(ARZoomState.ZoomFactor, 0.0001f); }
  ```

  Keep `Update()` for the animation step; __LateUpdate AFTER Update__ (`Update`->`LateUpdate` ordering is load-bearing so `ZoomFactor` is final before the camera reads it). Use guard `Mathf.Max(...,0.0001f)` so zoom 1(fit) never divides by 0.

- __Why:__ ship the feature the spec promised; `ZoomOnSelectController` (already calling `SetZoomAnimated`) inherits this fix for free — it was silently non-functional only because the FOV write never existed.

- __Phase A / B:__ Phase A = isolated PlayMode FOV test (bare GO+`Camera`+`ARZoomController`, reflected-settings, no scene). Phase B after A = drive through `WallSession`/LivingRoom Play Mode (mock localization, Tier A), confirm buttons pig thru the *real* scene camera. B differs from A → wiring bug, not FOV math.

- __Tests (PlayMode, new `ARZoomCameraFovTests.cs`):__ immediate seam `SetZoomImmediate(2f)` + `yield return null` (Start+LateUpdate run) → `Assert.AreEqual(baseFov/2, cam.fieldOfView, tol)`; animated `zoom_transition_speed_s=0.1f` `SetZoomAnimated(2f)` step several frames → converges to target, left start value, and `_last`TransitionSpeed picks up on first `settings != _lastSettings` differ; zero-guard `SetZoom(…)` clamps min and `Mathf.Max(…)` keeps division finite. Layer `SetField(zoom,"_camera",cam)`. Edit-mode `refresh_unity` zero `error CS`. (Edit-mode parity §4.4.1: N/A for the SceneView; FOV applies to the Game camera.)

- __Finish:__ FLIP `_2.7` index-row 13 + detail-row 2.4-l to __pending-device__ only (not ✅ until Tier 2), and update `_2.4` §9 / Implementation-Status row note that the FOV driver now exists.

### Task 5.2 — (`__2.4-m`): pinch + double-tap gesture input (new Input System)  [x] CODE+TESTS DONE 2026-08-26

- __Why A:__ §9 "three layered mechanisms": pinch + double-tap + buttons; only buttons wired. OnPinch/OnDoubleTap exist and are unit-tested; nothing calls them. `activeInputHandler: 1` ⇒ EnhancedTouch. Keep detection out of `ARZoomController` (its inherited design note says "a thin Input-System action binding call").

- __How:__ new `Runtime/Tracking/ARZoomGestureInput.cs` (MonoBehaviour): `OnEnable()` calls `EnhancedTouchSupport.Enable()` once; `Update()` reads `Touch.activeTouches`:

  - __Pinch:__ when 2 touches active, keep prev/current touch distance; `deltaScale = cur/prev` (guard prev>0 else 1); call `zoom.OnPinch(deltaScale)` per live frame.
  - __Double-tap:__ a tap = phase-ended quickly + movement below drag threshold; second tap within `zoom_double_tap_window_s` and within `zoom_double_tap_move_tolerance_px` of first-tap start ⇒ `zoom.OnDoubleTap()`; consume/reset pending so triple-tap doesn't spam; the single tap routes nothing (MarkerSelectable owns select).
  - __Pure logic, Tier-0-testable:__ add `public static bool` `ARZoomMath.IsDoubleTapOK(float timeSinceLastTap, Vector2 posDelta, float windowSeconds, float tolPx)` (`time≤window && delta.magnitude≤tol`), and `public static float PinchScaleForDelta(float prev, float cur)` (`prev>0 ? cur/prev : 1f`).

- __Config — new tunables (no existing field to reuse; genuinely new):__ in `LodSettings` (`WallConfigData.cs:469` area) add `public float zoom_double_tap_window_s = 0.3f;` + `public float zoom_double_tap_move_tolerance_px = 50f;` and expose both in `DrawGlobalZoomSection` (after the toggle rows). __Do not hardcode__ in the script.

- __Wire__ component alongside `ARZoomController` on the same GameObject (same `WallSession`+`LODController` pattern).

- __Phase A/B:__ pure double-tap math = Phase A (EditMode, no scene). Real gesture *detection* has __no Phase B__ (no non-device pipeline) → straight from Phase A to __Tier 2 device__; say this explicitly in the handoff so nobody searches for a missing Phase B. (2.4-l DOES have a Phase B — don't conflate paths.)

- __Tests (editMode extensions of `ARZoomMathTests.cs`):__ `IsDoubleTap` boundaries — inside window (true), over-window (false), large movement (false), zero/neg window guard, NaN/zero delta; `PinchScaleForDelta`: cur>prev→>1, cur<prev→<1, prev=0→1, zero/zero→1. New file compile + suites green.

- __Finish:__ flip `_2.7` index-row 14 / detail 2.4-m to pending-device; correct the stale §9 premise (line correction in `_2.4`). Combine device passes with 2.1 in one session.

### Task 5.3 — Editor exposure + doc corrections  [x] CODE+TESTS DONE 2026-08-26

- Add two scalar rows after `DrawGlobalZoomSection` last row, e.g. `lod.zoom_double_tap_window_s = DrawScalarField("Double Tap Window (s)", lod.zoom_double_tap_window_s, ZoomDoubleTapWindowHelp);` and `lod.zoom_double_tap_move_tolerance_px = DrawScalarField("Move Tolerance (px)", …, ZoomDoubleTapToleranceHelp);`.
- Add `ZoomDoubleTapWindowHelp`/`ZoomDoubleTapToleranceHelp` constants to `Constants.cs` mirroring `ZoomUiButtonsHelp` style.
- Extend `Tests/Editor/LodAuthoringRoundTripTests.cs` (or sibling) to assert both new fields survive Save→Load.
- Correct `_2.4` §9 stale "Input System wasn't found in use anywhere" premise; note `activeInputHandler: 1`; update `_5_1` zoom-fields listing if present; ensure 10-structure gets `ARZoomGestureInput.cs` + `ARZoomCameraFovTests.cs`.
- __Tier notes:__ FOV seam is a numeric camera prop (Tier 0.5 N/A). Mark 2.4-l/2.4-m pending-device on code-landing; ✅ only after Tier 2 device holds.

### Task 5.4 — Tier 1 (vision) + Tier 2 (human device) handoff guides

- __Tier 1 (vision, Editor Play-Mode):__ 
» written in the file: 
C:\Users\franc\Desktop\TileStories\proj_guides\_2.6.1_Vision_Tests.md

- __Tier 2 (human, real ARFoundation device, end of block):__
» written in the file: 
C:\Users\franc\Desktop\TileStories\proj_guides\_2.6.2_Human_Tests.md

---

## Decisions

__b) Made-autonomous:__ (a) the gesture-API fork — EnhancedTouch over legacy-Input, now decided because `activeInputHandler: 1` is objectively the project state; (b) FOV mechanism — direct-driver default (matches spec) + conditional projectionMatrix fallback per the entry; (c) two new `LodSettings` tunables (window=0.3s, tolerance=50px) genuinely new; (d) pure math added to `ARZoomMath` for Tier-0; (e) 5.1 Phase B / 5.2 no-Phase-B asymmetry honored.

__b) Requiring developer input:__ (1) __Tier 2 device session__ — a human must run the Task 5.4 checklist and report whether the FOV write "sticks" on-device (or must switch to projectionMap); (2) thereafter mark 2.4-l/2.4-m ✅. No naming/schema/pseudo open trade-off remains.

##

# Block 6 – Plan (Steps 15 = 2.5‑g, 16 = 2.5‑h, 17 = 2.6‑al)
Goal: implement the missing displacement tie‑break option, wire the hide‑fallback for max_displacement_px, and make the synonym feature functional by removing the dead‑end SearchSynonymGroups asset and moving synonym data into WallConfigData. All work must follow the project’s architecture rules (no hard‑coded wall‑specific behavior, expose genuine options in the POI Authoring window, keep MonoBehaviours thin, and verify with the full verification gate).

1. Architecture Level (Where it lives)
Sub‑step	Concern	Options evaluated	Chosen location	Rationale
2.5‑g – displacement tie‑break (lower_priority_only)	Where the tie‑break logic for displacement groups lives.	1️⃣ Keep the stub in DisplacementSettings and handle it in MarkerOverlapResolver.ApplyDisplacement.<br>2️⃣ Move the decision to a dedicated DisplacementTieBreakStrategy class (Strategy pattern).<br>3️⃣ Put the logic directly in LODController when it calls the resolver.	Option 2 – new DisplacementTieBreakStrategy (plain C# class, stateless, selectable via enum).	Keeps the resolver thin, makes the tie‑break swappable (future walls may want other rules), and isolates the change so existing unit tests stay green. The strategy can be exposed as a LodSettings enum (displacement_tiebreak) matching the existing pattern for other displacement fields.
2.5‑h – max_displacement_px hide‑fallback	Where the check that hides the label when displacement exceeds the pixel limit lives.	1️⃣ Add the check inside MarkerOverlapResolver.ApplyDisplacement after computing the offset.<br>2️⃣ Put the check in LabelDisplacementApplier (a small helper used by the resolver).<br>3️⃣ Handle it in MarkerView.SetVisible based on a flag from the resolver.	Option 1 – add the check directly in ApplyDisplacement.	The resolver already has all needed data (computed offset, POI ID, settings). Adding the guard there keeps the label‑visibility decision collocated with the displacement calculation, avoids an extra indirection, and matches the pattern used for the symmetric tie‑break (which already mutates the offset).
2.6‑al – synonym feature dead‑end	Where synonym data should live so it reaches runtime search.	1️⃣ Keep the SearchSynonymGroups ScriptableObject but wire it from WallSession to POISearchIndex.ConfigureWithSynonyms.<br>2️⃣ Move synonym groups into WallConfigData (a plain List<SynonymGroup>) and delete the asset type.<br>3️⃣ Keep the asset but load it at runtime via Resources.Load<SearchSynonymGroups> and call ConfigureWithSynonyms.	Option 2 – embed List<SynonymGroup> in WallConfigData.	The project already stores all wall‑specific taxonomy (categories, badge categories, outline levels, hierarchy levels) as plain lists in WallConfigData. Adding synonyms there removes an Editor‑only asset, guarantees the data is baked into the runtime config, and follows the “configuration is data, not code” rule. Option 1 would still require an Editor‑only asset to be referenced at runtime (violates the Rule #1.2 boundary). Option 3 re‑introduces an Editor‑only asset and adds a runtime load step, which is unnecessary.
2. Implementation Level (How it works)
2.5‑g – Displacement Tie‑Break (lower_priority_only)
CONFIRMED SCHEMA (verified this session): `displacement_tiebreak` stays a STRING (`"symmetric"` / `"lower_priority_only"`), matching `_2.5_Marker_Displacement.md:419` and `_2.7_Corrections_TODO.md:1684` which compare `settings.displacement_tiebreak == "lower_priority_only"`. Do NOT introduce a DisplacementTieBreakMode enum (the Step 0 revert removed the orphaned one).
Strategy class - DisplacementTieBreakStrategy with a static method Apply(IReadOnlyList<string> poiIdsInGroup, IReadOnlyDictionary<string, POIData> poisById, string mode, out string leaderId) that dispatches on the mode string ("symmetric" | "lower_priority_only").
For Symmetric: compute angular spread as currently (existing code).
For LowerPriorityOnly: the POI with the lowest HierarchyStyle.Priority (numerically smallest) becomes the leader; others are displaced radially outward from that leader using the same angular‑spacing algorithm but only displacing the lower‑priority POIs (the leader stays at its original wall position).
Wire into resolver – In MarkerOverlapResolver.ApplyDisplacement, after the overlap groups are built, call the strategy with settings.displacement_tiebreak to obtain the leader ID and the displacement vectors for each member.
Expose in Editor – Add a dropdown in the POI Authoring → Displacement Settings foldout (reuse the existing pattern for displacement_algorithm). Store the selected string in DisplacementSettings (the field already exists in WallConfigData as displacement_tiebreak) and ensure it is serialized to/from WallConfigData.
Unit tests – Add EditMode tests in DisplacementAlgorithmTest.cs (or a new file) that:
Create a small group of POIs with known hierarchy priorities.
Run ApplyDisplacement with both modes and assert that the leader ID matches expectation and that non‑leader POIs receive non‑zero offsets only in LowerPriorityOnly mode.
Verify that the symmetric mode still produces the previous behavior.
2.5‑h – max_displacement_px hide‑fallback
Add a bool flag shouldHideLabel to the result struct returned by ApplyDisplacement (or return a tuple (Vector2 displacement, bool hideLabel)).
In ApplyDisplacement, after computing the final offset for each POI, compare offset.magnitude (in screen pixels) to settings.max_displacement_px. If greater, set hideLabel = true and zero out the displacement vector (or keep it but later the view will hide the label).
Update LabelDisplacementApplier (if exists) to respect the hide flag – when hideLabel is true, set the label’s CanvasGroup.alpha to 0 (or call MarkerView.SetLabelVisible(false) if such a method exists; otherwise reuse the existing SetVisible path with alpha 0).
Editor exposure – max_displacement_px is already a field in LodSettings; no new UI needed. Just ensure the tooltip/docstring mentions the hide‑fallback behavior.
Unit tests – Add a test in DisplacementComputeTests.cs (EditMode) that:
Places two overlapping POIs with a known offset that exceeds max_displacement_px.
Calls ApplyDisplacement and asserts the returned displacement is zero (or negligible) and hideLabel is true.
Also test that when the offset is below the threshold, the label remains visible.
2.6‑al – Synonym feature activation
Modify WallConfigData – Add a public List<SynonymGroup> synonym_groups; (with appropriate [SerializeField] if needed).
Update WallConfigLoader.cs – When deserializing the JSON, populate this list from the "synonym_groups" array (if present). If missing, initialize an empty list.
Remove SearchSynonymGroups.cs (the ScriptableObject) and its associated menu item, asset type, and any #if UNITY_EDITOR guards. Delete the file from Assets/Framework/Editor/.
Wire at runtime – In WallSession.StartSession, after the config is loaded, call POISearchIndex.ConfigureWithSynonyms(wallConfig.synonym_groups).
Editor exposure – Add a new foldout under the POI Authoring → Search & Filter section titled “Synonym Groups”. Re‑use the existing UI pattern from the removed SearchSynonymGroups asset: a ReorderableList with a text field for the key and a list of synonym strings. Store the list directly in the WallConfigData instance shown in the window.
Unit tests –
EditMode: WallConfigLoaderTests – verify that JSON containing a synonym_groups array populates the list correctly and that missing array yields an empty list.
EditMode: POISearchIndexTests – add a test that after calling ConfigureWithSynonyms with a known group, a search for a synonym returns the expected POI(s).
PlayMode (optional) – a short test that loads a test wall config with synonyms, performs a search, and asserts the result set includes synonym matches.
3. Execution Plan (TODO list)
Task	Description	Files to touch (reference only)	Verification
6.1	Create DisplacementTieBreakStrategy class (string-mode dispatch; NO new enum).	Assets/Framework/Runtime/POI/DisplacementTieBreakStrategy.cs (new).	EditMode unit test DisplacementTieBreakStrategyTests (new) - 2 cases (symmetric, lower_priority_only).
6.2	Wire strategy into MarkerOverlapResolver.ApplyDisplacement.	MarkerOverlapResolver.cs.	Existing DisplacementComputeTests still green; add a test for the new mode (see 6.1).
6.3	Expose displacement_tiebreak in POI Authoring -> Displacement Settings foldout (dropdown presenting the two string values).	Assets/Framework/Editor/POIAuthoringToolWindow.Displacement.cs (new UI field); WallConfigData already has the string field.	EditMode round-trip test LodAuthoringRoundTripTests (extend) - verify the string survives Save->Load.

6.4	Resolve PlayMode displacement determinism drift (fix code, keep tests tight; Option B).	MarkerOverlapResolver.ApplyDisplacement (in-group committedOffset snapshot branch ~L795-800); LODControllerEvaluateTests.cs unchanged (tolerances stay).	Option B: snapshot committedOffset ONCE on group-entry and HOLD while in-group (instead of re-snapshotting offsets[i] every cycle). All 4 real-config tests (E2E / ThreeFamilies / LogsNoErrors / DeterministicAcrossRuns) pass in a SINGLE full-suite PlayMode run AND isolated; DisplacementHysteresisTests stay green. Loose fallback (Option A): if B regresses hysteresis, relax the <0.5px tolerance with a documented-rationale comment instead.


6.4	Implement max_displacement_px hide‑fallback in displacement resolver.	MarkerOverlapResolver.cs (modify return type/ApplyDisplacement); LabelDisplacementApplier.cs (if exists) or MarkerView (add SetLabelVisible).	New EditMode test DisplacementHideFallbackTest (in DisplacementComputeTests.cs) – assert zero displacement & hide flag when offset > limit.
6.5	Add synonym list to WallConfigData and loader logic.	WallConfigData.cs (add List<SynonymGroup> synonym_groups); WallConfigLoader.cs (deserialize).	WallConfigLoaderTests – JSON with/without synonym groups.
6.6	Remove SearchSynonymGroups.cs asset and all Editor‑only references.	Delete Assets/Framework/Editor/SearchSynonymGroups.cs; remove any [MenuItem] or #if UNITY_EDITOR usage.	Ensure compile succeeds (refresh_unity → zero error CS).
6.7	Wire synonym list to POISearchIndex at session start.	WallSession.cs (after config load call POISearchIndex.ConfigureWithSynonyms).	EditMode test POISearchIndexSynonymTests – configure with groups, search synonyms, assert results.
6.8	Build Editor UI for synonym groups (foldout, reorderable list).	Assets/Framework/Editor/POIAuthoringToolWindow.SearchFilter.cs (add new foldout); optionally Constants.cs for UI strings.	Authoring round‑trip test: modify synonym list in window, Save→Load, verify values persist.
6.9	Update documentation (Implementation‑Status rows) in _2.5_Marker_Displacement.md and _2.6_Select_Filter_Search.md.	Doc files only (no code change).	No test needed; just ensure the doc reflects the new fields/behavior.
6.10	Final verification gate.	–	Run refresh_unity (zero error CS); run full EditMode + PlayMode test suites via Unity MCP (run_tests + get_test_job) – expect zero failures.
4. Finishing & Verification Criteria
Compile check – refresh_unity (mode = force, scope = all, compile = request) → zero error CS.
Test suites –
EditMode: all tests green (including new DisplacementTieBreakStrategyTests, extended LodAuthoringRoundTripTests, DisplacementComputeTests hide‑fallback, WallConfigLoaderTests, POISearchIndexSynonymTests).
PlayMode: all existing tests green (no new PlayMode tests required, but ensure none regress).
Documentation – Implementation‑Status rows for steps 15, 16, 17 in _2.7_Corrections_TODO.md flipped to [DONE] with date 2026‑08‑27 (or actual date after completion) and the detailed sections removed per the file’s convention.
10‑structure.md – Add entries for any new files:
Framework/Runtime/POI/DisplacementTieBreakStrategy.cs – “Strategy encapsulating displacement tie‑break modes (Symmetric, LowerPriorityOnly) used by MarkerOverlapResolver.”
Framework/Editor/POIAuthoringToolWindow.SearchFilter.cs – “UI for Search & Filter foldout, now also contains Synonym Groups editor.”
No hard‑coded wall‑specific values – All new settings (displacement_tiebreak, max_displacement_px hide‑fallback) are exposed as developer‑tunable fields in the POI Authoring window, satisfying the configuration‑data rule.
MonoBehaviour thinness – No MonoBehaviour exceeds ~150 lines after changes; the new logic resides in plain C# classes (DisplacementTieBreakStrategy, updated resolver).
5. Decisions Summary (for the user)
Decision	Type	Explanation
Use a Strategy pattern for displacement tie‑break (instead of a flag in the resolver)	Autonomous	Provides clean extensibility, keeps resolver thin, matches existing pattern for displacement algorithms.
Perform max_displacement_px hide‑fallback directly in MarkerOverlapResolver.ApplyDisplacement	Autonomous	All needed data is already present; avoids extra indirection and follows the existing placement of displacement logic.
Move synonym data from SearchSynonymGroups asset into WallConfigData (remove the asset)	Autonomous	Aligns with the project’s “configuration is data” principle, eliminates Editor‑only asset, guarantees runtime availability, and follows the same storage pattern as other taxonomy lists.
Add a new UI foldout for Synonym Groups under Search & Filter in the POI Authoring window	Autonomous	Re‑uses existing UI patterns, keeps all wall‑specific configuration in one place, and makes the feature discoverable per the Framework philosophy.
No additional master toggle required for synonyms (they are always active if data present)	Autonomous	The feature is purely data‑driven; turning it off would be equivalent to providing an empty list, which is already possible by leaving the array empty. No extra UI toggle needed.
Resolve PlayMode displacement drift by holding the entry committedOffset (Option B, code fix) rather than relaxing test tolerances	Autonomous (default)	4.55px full-suite / 0.547px isolated drift is real flap, not FP-noise; a "no-flap/no-drift" property is load-bearing, so fix the code not the test. Option A fallback (documented tolerance relax) only if B regresses DisplacementHysteresisTests.

# Block 5 and 6 corrections

> **EXECUTED 2026-08-31 — status:** Task A [x] DONE (`hiddenLabels` CS0103 fixed by calling `ResolveLabelsToHide` once per `ApplyDisplacement` cycle, L667). Task B [x] DONE (strategy wired into fixed_axis / candidate_position / force_directed). Task C1 [x] DONE (`_2.4` §9 status rows + Implementation Status note). Task C2 [x] DONE (`ZoomRig` GO in LivingRoomScene, refs verified in saved YAML). Task C3 [ ] OPEN — Tier 2 human device session (only remaining gate for 2.4-l/m ✅). Task D [x] DONE (9 strategy tests + 7 compute tests + 4 hide tests + PlayMode `RealConfig_LowerPriorityOnly_E2E`). Task E [x] DONE except C3 (`_2.7` rows 13-16 + 2.5-p flipped; `_2.5` §12 + rows 15/16; `10-structure.md` entries; tracker L35/L36 corrected; temp files deleted; decisions.md updated). **Acceptance gate: EditMode 589/589, PlayMode 64/64, 0 error CS — single full-suite runs.**

## 2. Corrected & completed plan — "Block 5/6 Closeout" (to replace/append in `__curr_plan_tracker.md`)

### Task A — Restore the build (2.5-h seam) — __MUST be first, everything else is blocked__

- __WHAT:__ zero `error CS` again.
- __HOW:__ in `MarkerOverlapResolver.ApplyDisplacement`, declare once per call, after `offsets` are computed and before the per-marker apply loop (~L662–665): `var hiddenLabels = ResolveLabelsToHide(screenPositions, offsets, visibleUnits, settings);` It's already `internal static`, pure, and `hiddenLabels.Contains(i)` at L705 is read-only against it — this is completing the interrupted edit, not redesigning.
- __WHERE:__ `Assets/Framework/Runtime/POI/MarkerOverlapResolver.cs` only.
- __WHY:__ the 2.5-h decision logic exists and is good; it was simply never connected — the exact §4.2.1 composition-gap failure class, caught this time by the compiler.
- __TESTS:__ covered by Task D; immediate gate = `refresh_unity` zero `error CS`.

### Task B — Actually wire `lower_priority_only` (2.5-g) — the real remaining work of Block 6

- __WHAT:__ make `displacement_tiebreak = "lower_priority_only"` behave per `_2.5 §7`: the highest-priority member (lowest `hierarchyLevelIndex`) __keeps its true wall position__ and the remaining members displace __radially away from that leader__, clamped by `max_displacement_px`.

- __HOW (architecture level, 3 options):__

  1. Call `DisplacementTieBreakStrategy.TryGetPrimaryAnchorLocalIndex` inside each of the three algorithm branches — chosen. The strategy class already exists, is pure, matches the "resolve anchor → branch-specific spread" shape, and the existing comment already claims this design (making the comment true instead of deleting it).
  2. Inline the leader logic per branch — rejected: three near-duplicates of the same anchor rule, exactly the drift §20 warns about.
  3. Delete the strategy and document "lower_priority_only ≈ symmetric-without-target-angles" — rejected: silently weakens a spec'd, Editor-exposed option; the dropdown already advertises two behaviors.

- __HOW (implementation):__ in `ComputeOffsets` per group: build `groupPriorities` from the already-sorted `members` list → `if (DisplacementTieBreakStrategy.TryGetPrimaryAnchorLocalIndex(settings.displacement_tiebreak, priorities, out anchor))` → skip displacement for the anchor member (offset stays `Vector2.zero`), and bias the other members' candidate scoring away from the leader's position (reuse the existing candidate-position scoring loop; the leader's screen pos becomes an additional repulsion center). Shared-minimum ties already fall back to symmetric (strategy returns false) — that fallback is implemented and correct.

- __WHERE:__ `Runtime/POI/MarkerOverlapResolver.cs` (ComputeOffsets + per-branch call points) + `Runtime/POI/DisplacementTieBreakStrategy.cs` (no change expected). Keep the string dispatch — __do not__ re-introduce the enum removed in the pre-approved Step-0 revert.

- __WHY:__ framework contract: every Editor-exposed choice must be real; the comment at L632 must become true rather than be tolerated as a lie.

- __TESTS:__ see Task D (tiebreak tests).

### Task C — Block 5 residue

- __C1 — `_2.4` doc correction (finishing the never-applied half of Task 5.3):__ add to `_2.4_Marker_LOD.md` §9 / Implementation Status: FOV driver exists (`ARZoomController` Start/LateUpdate, base/ZoomFactor formula), gesture input exists (`ARZoomGestureInput`, EnhancedTouch — `activeInputHandler: 1` fact), zoom double-tap window/tolerance tunables; mark 2.4-l/2.4-m code as shipped, ✅ gated on Tier 2.
- __C2 — Scene bootstrap check (Phase B gap):__ grep `LivingRoomScene.unity` for the `ARZoomGestureInput` script GUID; if absent, add the component (next to `ARZoomController`) in the scene + authoring-rig pass. Without this, gestures don't exist in the actual dev wall even though the class ships.
- __C3 — Tier 2 human device pass (Task 5.4, unchanged):__ run the existing handover checklist (`_2.7.2_Human_Tests.md`); only after that flip `_2.7` index rows 13/14 to done. Bundle the Block 6 displacement gallery visual check into the same device session (one session, two domains).

### Task D — Tests (real code, no mocks; Lamp family config is the 12+ POI set for real-config cases)

- __Tier 0, new `Tests/Editor/DisplacementTieBreakStrategyTests.cs`:__ unique-min returns anchor index; shared-min returns false; unknown/null/`"symmetric"` returns false; empty/null list returns false.
- __Tier 0, extend `DisplacementComputeTests.cs`:__ (a) `lower_priority_only` two-member group → leader offset `≈0`, other member offset magnitude `>0` directed away from leader; (b) shared-priority pair under `lower_priority_only` → symmetric behavior preserved (this is `_2.5 §12`'s "fallback-to-symmetric" row, 2.5-p subsumed); (c) hide-fallback: pair still crowding at clamp → loser hidden, winner visible; (d) both below clamp → nothing hidden; (e) `displace_target = "marker"` → never hides; (f) 3-member bounded-pass cap (never hides all).
- __Tier 0, `ResolveLabelsToHide` direct tests__ (it's `internal`, `InternalsVisibleTo` already granted).
- __PlayMode, extend `LODControllerEvaluateTests.cs`:__ real-config `lower_priority_only` E2E on the lamp_* cluster (Phase B) + re-run the two previously flaky determinism tests __in a single full-suite PlayMode run__ — the `committedOffset` hold (Task 6.4, code verified at L685–698) exists precisely to fix them; their full-suite pass is the acceptance for that fix.
- __EditMode + PlayMode full suites: zero failures__ (no fixed counts).

### Task E — Finishing (§6 checklist)

1. Per task: edit → re-read → `refresh_unity` (zero `error CS`) → suite gate.
2. Flip `_2.7` index rows 15/16 + detailed rows 2.5-g/2.5-h (+ note 2.5-p subsumed); update `_2.5` §7/§12 status rows and hide-fallback behavior note; __C1's `_2.4` updates__.
3. Update `.clinerules/10-structure.md`: add `DisplacementTieBreakStrategy.cs`, `ARZoomGestureInput.cs`, `ARZoomMath.cs` (split from controller), `ResultSetCoordinator.cs` if not already there (git shows the file was modified — verify, don't assume).
4. Correct `__curr_plan_tracker.md` L36 stale baseline line.
5. Audit & delete stray temp artifacts: root `__*.txt`, `__remove_resolve_tests.ps1` (after confirming no tests were silently removed — restore any that were).
6. `decisions.md` entries: strategy-call-site decision, "comment claimed wiring that didn't exist" process note (§4.2.1 instance #4).
7. Tier 1/2 handovers (C3) — reference `_2.7.1`/`_2.7.2` files; no new handover content needed beyond the displacement visual checklist.

__Deliberately NOT in this block:__ 2.6-al synonyms (Block 7) — confirmed untouched (`WallConfigData` has no `synonym_groups`; `SearchSynonymGroups.cs` still at `Editor/POI/`, only test callers of `ConfigureWithSynonyms`). Per the mode-file rule, Block 7 is not planned while Block 6 has pending work.

---

## 3. Decisions

__a) Made autonomously (embedded above):__ fix the compile error by calling the existing `ResolveLabelsToHide` at the natural per-cycle point; wire the existing strategy class rather than deleting it or duplicating logic inline; keep string dispatch (no enum); fold the displacement Tier 1 visual check into Task 5.4's existing device session; treat full-suite (not isolated) PlayMode pass of the two determinism tests as the Task 6.4 acceptance.

__b) Requiring developer input:__

1. __`lower_priority_only` exact displacement semantics__ — my reading of `_2.5 §7` is "leader pinned at true position, others pushed radially away from it, deterministic tiebreak by farther-from-camera." Confirm before Task B, since the §7 text's secondary tiebreak detail could also mean "leader keeps symmetric spread among equals" — it changes the scoring bias slightly.
2. __Scheduling the Tier 2 device session__ (Task C3) — do it right after Tasks A–D, or defer to bundle with a later multi-domain device pass? Everything else completes without it; only the ✅ flips for 2.4-l/m wait on it.

# Block 7
Block 7 – Synonym feature rescue (Step 17 = 2.6‑al)
Goal: make the synonym search data reach the running app, remove the Editor‑only SearchSynonymGroups ScriptableObject, and expose a clean authoring UI for synonym groups inside the POI Authoring window – all without breaking any existing wall.

1️⃣ Architecture‑level options (where the data lives)
##	Option	Description	Trade‑offs
A	Add List<SynonymGroup> synonym_groups to WallConfigData (plain C# list, serialized by JsonUtility).	Pros: follows the project rule “configuration is data, not code”; same pattern as category_styles, badge_categories, outline_levels, hierarchy_levels; data is baked into the runtime config.json → no Editor‑only asset, no extra load step. <br>Cons: requires a tiny JSON schema change (additive, backward‑compatible).	
B	Keep the SearchSynonymGroups ScriptableObject and load it at runtime via Resources.Load<SearchSynonymGroups>() (or addressables).	Pros: no JSON change. <br>Cons: violates the Runtime/Editor assembly boundary (the asset lives in an #if UNITY_EDITOR folder); adds a runtime Resources dependency; the asset would have to be duplicated per wall or placed in a shared Runtime folder – both break the “one wall = one config file” contract.	
C	Embed synonym groups inside CategoryStyle / BadgeCategoryData as an extra field.	Pros: no new top‑level list. <br>Cons: mixes unrelated taxonomies; a synonym is global to the wall, not per‑category; makes the data model confusing and harder to author.	
Chosen: Option A – a plain List<SynonymGroup> on WallConfigData. It matches every other wall‑specific taxonomy list, guarantees runtime availability, and costs only a one‑line JSON addition.

2️⃣ Implementation‑level options (how to wire it)
##	Option	Description	Trade‑offs
1	Call POISearchIndex.ConfigureWithSynonyms(wallConfig.synonym_groups) from WallSession.StartSession after the config is loaded.	Pros: single call site, same place where CategoryPalette.Configure and MarkerHierarchyResolver.Configure are already invoked; no new events or callbacks needed.	
2	Add a new SynonymIndexBuilder MonoBehaviour that subscribes to a OnConfigLoaded event and builds the index.	Pros: decouples building from WallSession. <br>Cons: introduces an extra component, event, and ordering complexity for a one‑liner.	
3	Make POISearchIndex a ScriptableObject that self‑initialises from a SynonymGroup asset reference.	Pros: “asset‑driven”. <br>Cons: re‑introduces an Editor‑only asset; goes against the data‑not‑code rule; more moving parts.	
Chosen: Option 1 – direct call in WallSession.StartSession. Simpler, deterministic, and consistent with existing bootstrap pattern.

3️⃣ Detailed Task List (WHAT / HOW / WHERE / WHY / TESTS)
##	Task	WHAT (acceptance)	HOW (design)	WHERE (files)	WHY
7.1	Add synonym_groups field to WallConfigData	WallConfigData now has public List<SynonymGroup> synonym_groups = new(); – JSON round‑trip works (missing array → empty list).	Add the field, initialise in ctor; JsonUtility will populate from "synonym_groups" array if present.	Assets/Framework/Runtime/Core/WallConfigData.cs	Keeps all wall taxonomy in one data object; no Editor‑only asset.
7.2	Deserialise from JSON	WallConfigLoader.LoadFromStreamingAssets returns a WallConfigData whose synonym_groups list is filled when the JSON contains the array.	No code change – JsonUtility already maps matching fields; just ensure the JSON key is synonym_groups.	Assets/Framework/Runtime/Core/WallConfigLoader.cs (verify only)	Zero‑code deserialization, backward‑compatible.
7.3	Wire into runtime search index	In WallSession.StartSession, after CategoryPalette.Configure / MarkerHierarchyResolver.Configure, call POISearchIndex.ConfigureWithSynonyms(_config.synonym_groups);.	One line, guarded by if (_config.synonym_groups != null && _config.synonym_groups.Count>0).	Assets/Framework/Runtime/Core/WallSession.cs	Guarantees synonyms are active for every wall that supplies them.
7.4	Remove dead‑end Editor‑only asset	Delete Assets/Framework/Editor/POI/SearchSynonymGroups.cs and its [CreateAssetMenu] menu item; delete the stray Assets/SearchSynonyms.asset (and .meta).	Straight delete; no references remain after 7.3.	Editor folder & project root	Eliminates the #if UNITY_EDITOR‑only asset that could never ship.
7.5	Authoring‑window UI for synonym groups	New fold‑out “Synonym Groups” under Search & Filter in POIAuthoringToolWindow. Shows a re‑orderable list: each row = key (string) + synonyms (list of strings). Edits are written back into the in‑memory WallConfigData.synonym_groups and persisted by the existing “Save → Copy to StreamingAssets” flow.	Re‑use the pattern already used for CategoryStyle / BadgeCategory tables (a ReorderableList + a small EntryDetailsPopup per row). Add the fold‑out in the partial class POIAuthoringToolWindow.SearchFilter.cs.	Assets/Framework/Editor/POIAuthoring/GlobalScene/POIAuthoringToolWindow.SearchFilter.cs (new method DrawGlobalSynonymSection) + POIAuthoringToolWindow.Constants.cs for UI strings.	Keeps all wall‑specific configuration in the single POI Authoring window – the framework’s mandated entry point (Rule 1.3).
7.6	Unit tests (EditMode)	WallConfigLoaderTests – JSON with "synonym_groups" → list populated; missing array → empty list.<br>POISearchIndexTests – after ConfigureWithSynonyms(groups), a query for a synonym returns the expected POI(s).	Pure C# tests, no scene.	Assets/Framework/Tests/Editor/WallConfigLoaderTests.cs (extend) <br> Assets/Framework/Tests/Editor/POISearchIndexTests.cs (extend)	Tier‑0 proof that data flows from JSON → runtime index → search results.
7.7	Integration test (PlayMode – optional but cheap)	Load a tiny test config.json that contains a synonym group, run a search via the real SearchOverlayView pipeline, assert the result set includes the synonym match.	Re‑use existing SearchOverlayRuntimeTests harness (DebugTranscriber → transcript → results).	Assets/Framework/Tests/Runtime/SearchOverlayRuntimeTests.cs (add one [UnityTest]).	Confirms end‑to‑end wiring without a separate test scene.
7.8	Documentation updates	Flip _2.7_Corrections_TODO.md row 17 to [DONE] with date & notes; add a line in _2.6_Select_Filter_Search.md Implementation‑Status §8 “Synonym groups now live on WallConfigData”.	Edit the two markdown files.	proj_guides/_2.7_Corrections_TODO.md, proj_guides/_2.6_Select_Filter_Search.md	Keeps living trackers accurate.
4️⃣ Finishing Checklist (per §6)
Per‑task edit → re‑read → refresh_unity (0 error CS) → run new EditMode test + full EditMode suite (0 failures).
Run PlayMode suite once (full) – 0 failures (the optional PlayMode test runs automatically).
Flip tracker rows in __curr_plan_tracker.md (Block 7 entry) and _2.7 index + detail rows.
Update 10‑structure.md – add entries for the new synonym_groups field (already covered by WallConfigData) and the new UI method DrawGlobalSynonymSection.
Delete stray temp files (none expected).
Add two decisions to Docs/decisions.md – “Synonym data moved to WallConfigData (Option A)”, “Bootstrap call placed in WallSession (Option 1)”.
5️⃣ Decisions
a) Autonomous (embedded in plan)
• Synonym groups become a plain List<SynonymGroup> on WallConfigData (Option A).
• Runtime wiring is a single call in WallSession.StartSession (Option 1).
• Editor UI lives in the existing Search & Filter fold‑out, re‑using the table‑row pattern.
• The dead SearchSynonymGroups ScriptableObject and its menu item are deleted outright.
b) Requiring developer input
None – every trade‑off has a clear winner that follows existing project conventions. If you prefer a different UI placement (e.g., a separate top‑level “Synonyms” fold‑out instead of under Search & Filter), let me know before Act‑mode; otherwise the plan proceeds with the Search & Filter location. 