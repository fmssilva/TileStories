# Vision Tests -- Select, Filter & Search (Tier 1)

Itemized, falsifiable claims to check on renders (`40-testing.md` 4.5). The renders are written by the PlayMode
suites `SearchSceneTests` / `SearchDemoTests` into `Assets/Screenshots/Search_*.png` (the real Game view, UI
included) and by the Editor Tab capture (`Search_EditorTab*.png`). Re-run the suites, open each PNG, answer each
item with yes/no + what is actually seen. Never "does it look right".

## Answers of 2026-09-25 (after the review's layout fixes)

| # | Render | Claim | Answer |
|---|---|---|---|
| 1 | `Search_Idle` | Only the search bar (field with the "Search" placeholder, Filters, Map) covers the camera view; no list, no map, no card | yes |
| 2 | `Search_Query_List` | Query "military": List / Map / Highlight row under the bar; the list shows the military POIs, the name matches first; each row = name + "category - level" inside its own row | yes (rows fixed: subtitles used to spill into the next row) |
| 3 | `Search_FilterTray` | The tray shows Category / Badge / Status / Level groups, each chip reads a label (not a key), groups never overlap, a scrollbar when taller than the screen | yes (fixed: groups overlapped before `flex-shrink: 0` + ScrollView) |
| 4 | `Search_NoResults_Relax` | Two exclusive chips ticked: "Filters (2)", the list shows the no-results message and one "Remove filter: ..." button | yes |
| 5 | `Search_View_Map` | Query "religious", Map view: the minimap shows exactly the 3 matching dots, the camera shows only matching markers | yes (the map is a floor plan: far = top) |
| 6 | `Search_View_Highlight` | Highlight view: no list, no map; only the matching markers visible | yes |
| 7 | `Search_Selected_Card` | A tapped marker: the card (name, "category - level", summary, X) at the bottom; the other markers visibly dimmer | yes |
| 8 | `Search_Minimap_Selected` | A dot tap: that dot larger than the others, the others faded | yes |
| 9 | `Search_Demo_On` | The demo: one labelled column per category, a test-case column (Cafe Alvaro, Lantern Tower, Old Hall, Stone Gate, Square Well), a 'Crowd' clump; the wall's own markers gone | yes; two level-1 labels still touch (Infrastructure 1 / Cafe Alvaro) and a Displacement leader line crosses from the clump (other domain, see `_2.6` Open items) |
| 10 | `Search_Demo_QueryCrowd` | Query "crowd": 5 rows "Crowd 1..5", only the clump visible | yes |
| 11 | `Search_Demo_ZoomOnSelect` | After selecting a clump point: the view zoomed in (markers visibly larger), the card shows "Crowd 1" | yes |
| 12 | `Search_Demo_Cluster` | With Run LOD: the clump drawn as one "+5" cluster (pie + count) | yes |
| 13 | `Search_Demo_FilterCategory` | One category chip: only that category's demo markers stay | yes |
| 14 | `Search_EditorTab*` | Global Scene > Select, Filter & Search: sub-foldouts Selection / Search / Filters / Results & Views / Minimap / Voice / Keywords & Synonyms / Test; conditional rows one step deeper; value and (i) columns aligned; Bounds Min / Max on one line; both tables with headers over their columns and a trash per row | yes (fixed: Bounds rows wrapped onto two lines before wideMode) |

## Answers of 2026-09-26 (round 2: `Search_Field_*` from SearchSettingsFieldTests, `Search_Demo_*` re-rendered)

| # | Render | Claim | Answer |
|---|---|---|---|
| 15 | `Search_Demo_On` | The five-point clump sits at the view's centre (not a corner); test column Café Álvaro ... Square Well at the right | yes (was in the right-hand column before this round); a Displacement leader line still runs from the clump to a corner (other domain) |
| 16 | `Search_Field_zoom_factor_3` | x3 on the most central point: the crowd is still in view and separated, card open | yes; the card covers the lower third, over part of the crowd (device check, `_2.6.2`) |
| 17 | `Search_Field_zoom_factor_4_off_centre` | Zoom Factor 4 on an off-centre point: zoomed less than x4, the tapped point still on screen | yes by test assert; the first render of this round (before the fix) showed the clump LOST at x3 |
| 18 | `Search_Field_search_typo_2` | 'obsrevatroy': the list shows exactly Observatory Hill | yes |
| 19 | `Search_Field_minimap_icons` | Visibility Always (no Map button), Category icons, dots laid out as the demo's rows | yes |
| 20 | `Search_Field_filter_dim_0.5` | 'crowd': Crowd rows listed, every other marker visibly faded (not gone) | yes |
| 21 | `Search_Demo_FilterMaterial` | Material = marble (tray closed): list = the marble points only | yes (Royal Government 2, Military 1, Residential 2, Infrastructure 1) |
| 22 | `Search_Field_filter_relax_on` | Two exclusive filters, tray closed: a message and "Remove filter: ... (N results)" | yes; the message read `No matches for ""` -- fixed by No-Results (Filters) (`search.no_results_filters_message`); re-render `Search_Field_search_no_results_filters` |
| 23 | `Search_Field_results_view_map` | Map view with 'crowd': the minimap shows the clump's dots only | yes (the five dots overlap: the clump is 3 cm wide on a 3 m map) |

## Still for the developer's eye

- The Editor Tab section on your own screen width (captures were at 620 pt): no label clipped, the Keyword Fields
  header (i) sits above the Details column.
- The search UI on a portrait phone (`_2.6.2_Human_Tests.md`): the renders here are a landscape Game view.
