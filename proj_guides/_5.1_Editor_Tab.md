# POI Authoring Editor Tab (5.1)

**Current build state:** reflects `_2.3_Marker_Hierarchy.md` (Hierarchy
Levels table, `has_custom_symbol` rename, `ValidateHierarchyLevelKeys` —
implemented). **`_2.4_LOD.md` is partially implemented** — corrected here
from an earlier, now-inaccurate "not yet implemented" note, based on
direct code reading while grounding `_2.5_Displacement.md`:
`LODController.cs` exists with a real, working 7-step pipeline (frustum
culling, effective distance, band hysteresis, density evaluation,
count-cap truncation, visibility). Still pass-through/absent: density
*response* (`shrink_and_fade`/`cluster`/`hybrid` strategies — the code's
own comment marks this "Phase 3"), `MarkerClusterView.cs`/cluster prefab,
`ARZoomController.cs` (`ARZoomState.cs`, the state holder, does exist).
**Editor UI (LOD band table, Zoom settings foldouts) status not verified
this pass** — check directly before assuming either way. **`_2.5
_Displacement.md` is fully planned but not yet implemented** — expect one
new Global Scene foldout (Displacement Settings, no table, plain fields)
when it lands. **`_2.6_Select_Filter_Search.md` is fully planned but not
yet implemented** — expect one new Global Scene foldout (Search & Filter,
a table plus scalar fields), and, distinctly, one new `search_keywords`
column added to each of the *existing* Category/Badge/Outline/Hierarchy
tables — this one touches shipped UI, not just adds a new foldout. Also
the first real content in `Runtime/UI/Cards/` (a minimal proof-of-life
detail card). Read the four domain docs above for the full "what/how/
why" behind anything summarized tersely here — this file is the
current-state reference, not the design rationale.

The POI Authoring window is the wall-authoring control center. It loads and edits one
wall's `config.json`, spawns a temporary Edit-Mode scene rig for visual marker placement,
provides global and per-POI editing UI, captures scene positions back to JSON, and copies
the validated config to `StreamingAssets/`.

Window path: `TileStories / POI Authoring Tool` (shortcut: `Shift+P`).

---

## 1. Architecture

### 1.1 The Partial-Class Pattern

`POIAuthoringToolWindow` is a `partial class EditorWindow` split across 10 files under
`Assets/Framework/Editor/POIAuthoring/`. All parts share the shell's serialized fields
(`_config`, `_configPath`, `_correctionAnchor`, `_wallIconLibrary`, etc.) and private
collections (`_poiFoldouts`, `_configHistory`).

| File | Responsibility |
|------|---------------|
| `POIAuthoringToolWindow.cs` | Shell: class declaration, serialized fields, menu item, OnEnable/OnDisable, OnGUI orchestration, DrawFramedFoldout, top action bar buttons, rig-safety static methods |
| `POIAuthoringToolWindow.Constants.cs` | Static option/label arrays (shapes, outline modes, line styles, sun/accent effects, show-label), section colors, TrashIcon |
| `GlobalScene/POIAuthoringToolWindow.GlobalScene.cs` | DrawGlobalSceneOptions + section drawers: Marker (shape, icon library, category table), Badge (shape, table), Outline (mode + level table), Effects (pulse/sun/accent defaults), Hierarchy Levels table |
| `SpecificMarker/POIAuthoringToolWindow.SpecificMarker.cs` | DrawSpecificMarkerOptions + per-POI field drawers: Position, Marker Style, Badge Style, Outline, helper dropdowns, ApplyUnknownStatusDefaults |
| `ConfigData/POIAuthoringToolWindow.ConfigFileIO.cs` | SaveAllToJson, SaveConfig, LoadConfig, CopyToStreamingAssets, EnsureDefaultIconLibraryLoaded, TryResolveWallIconLibraryFromConfig |
| `ConfigData/POIAuthoringToolWindow.ConfigHistory.cs` | DrawConfigMutationScope (JSON before/after diff + snapshot), RecordConfigChange, InitializeConfigHistory, Undo/Redo/ApplySnapshot, HandleUndoShortcuts |
| `ConfigData/POIAuthoringToolWindow.ConfigValidation.cs` | `ValidateHierarchyLevelKeys`, `ValidateAndAlert` — **confirmed by direct code read: `_2.3`'s soft 0.5–100cm `size_cm` sanity warning was never built.** Only the hierarchy-key check exists. Genuinely outstanding, not just undocumented. |
| `RigLifecycle/POIAuthoringToolWindow.RigLifecycle.cs` | RefreshRigVisuals, PopulateRig, CapturePositions, ClearRig, GetExistingRig/GetOrCreateRig, IsRigInSyncWithConfig, SelectRigObjects, TryResolveSceneReferences, OnSceneGUI |
| `AssetPaths/POIAuthoringToolWindow.AssetPaths.cs` | DrawPathRow, AbsoluteToAssetPath, AssetPathToResourcesPath, GetWallLibraryDirectory, EnsureAssetDirectory, SanitizeFileName |
| `Shared/POIAuthoringToolWindow.SymbolTable.cs` | DrawWallIconLibrarySelector, AssignSpriteToLibraryAndGetKey, ResolveSpriteForKey, DrawSymbolTable, DrawColorSwatchAndHex, TryParseHexColor, DrawSpritePreview |
| `Shared/EntryDetailsPopup.cs` | PopupWindowContent for in-place text editing (persisted to config) |
| `Shared/ExistingSymbolPickerPopup.cs` | Curated sprite picker (wall icons + framework defaults only) |
| `Shared/HelpInfoPopup.cs` | Read-only help popup (framework-authored static text) |
| `Shared/EditorAlertPopup.cs` | Non-blocking validation warning list with fix guidance |

### 1.2 Safety Guards (standalone static classes)

| File | Role |
|------|------|
| `POIAuthoringRigSafetyCheck.cs` | [InitializeOnLoad] static class. Subscribes to playModeStateChanged + hierarchyChanged. On Play Mode or scene save: if POIAuthoringRig has children, warns or shows save/clear dialog via PromptBeforePlayOrBuild(false) |
| `POIAuthoringRigBuildCheck.cs` | IPreprocessBuildWithReport. Hard-blocks any build while POIAuthoringRig has children -- delegates to PromptBeforePlayOrBuild(true) (no "Continue" option) |

### 1.3 Related Runtime Types

WallConfigData (Runtime): config.json mirror -- wall_id, wall_name, marker_shape,
marker_use_badge, marker_outline_mode, category_styles, badge_categories, outline_levels,
hierarchy_levels, effect_defaults, calibration_anchors, pois.

POIData: one POI -- id, name, x_norm, y_norm, category, badge_category, has_status,
status_pct, status_unknown, status_level_key, hierarchy_level_key, has_custom_symbol,
custom_symbol_key, captured_position (+ has_captured_position, captured_position_source,
captured_position_timestamp).

CategoryPalette / BadgeCategoryPalette / StatusRamp / MarkerHierarchyResolver /
MarkerVisualsParser / POIPositionResolver / POIAnchor / MarkerView / SpriteKeyLibrary.

### 1.4 Key Constants

| Constant | Value |
|----------|-------|
| DefaultConfigPath | Assets/Apps/LivingRoom/config.json |
| DefaultStreamingConfigPath | Assets/StreamingAssets/LivingRoom/config.json |
| DefaultPrefabPath | Assets/Framework/Runtime/UI/Markers/POI_Marker.prefab |
| DefaultIconLibraryPath | Assets/Framework/Runtime/UI/Markers/IconLibrary.asset |
| SyncPositionTolerance | 0.001f |
| SkipPromptPrefKey | "TileStories.RigSafetySkipPrompt" |

---

## 2. Source of Truth

Two config copies by design:

- **Authoring source (read/write):** `Assets/Apps/<Wall>/config.json`
- **Runtime copy (write-only):** `Assets/StreamingAssets/<Wall>/config.json`

Expected workflow: Load + edit source -> Save All to JSON -> Copy to StreamingAssets.

`Populate Rig from JSON` always reloads the source file first to avoid stale data.
`CopyToStreamingAssets` reads source, writes streaming, then `AssetDatabase.Refresh()`.

---

## 3. Data Flow

### 3.1 Config In (Load)

```
Load Config button
  -> ConfigFileIO.LoadConfig()
     -> File.ReadAllText(configPath)
     -> JsonUtility.FromJson<WallConfigData>
     -> seed empty sub-lists with defaults (category_styles, badge_categories,
        outline_levels, hierarchy_levels)
     -> EnsureDefaultIconLibraryLoaded() + TryResolveWallIconLibraryFromConfig()
     -> InitializeConfigHistory()  [first snapshot]
     -> ValidateAndAlert("after load")  [hierarchy key check]
```

### 3.2 Config Mutation (Undo-Aware)

Every field edit in Global or Specific sections is wrapped in `DrawConfigMutationScope`:

```
UI field changed
  -> DrawConfigMutationScope(drawContent, refreshRigOnChange: true)
     -> snapshot before = JsonUtility.ToJson(_config)
     -> drawContent()  [mutates _config fields directly]
     -> snapshot after = JsonUtility.ToJson(_config)
     -> if before == after: return (no change, no history record)
     -> RecordConfigChange(before, after)  [JSON snapshot diff, not Unity Undo]
     -> _hasUnsavedChanges = true
     -> if refreshRigOnChange: RefreshRigVisuals()
```

Key detail: undo/redo uses a JSON string list (`_configHistory`), not Unity's Undo system.
`UndoConfigChange` decrements index and calls `ApplyConfigSnapshot` (re-deserializes + Repaint).
`_isApplyingHistory` flag prevents snapshot recording during apply.

### 3.3 Config Out (Save)

```
Save All to JSON button
  -> SaveAllToJson()
     -> CapturePositions(silentWhenRigMissing: true)  [rig positions -> config]
     -> ValidateAndAlert("before save")
     -> SaveConfig()
        -> JsonUtility.ToJson(_config, prettyPrint: true) -> File.WriteAllText
        -> AssetDatabase.Refresh()
                 -> _hasUnsavedChanges = false
```

### 3.4 Rig Flow (Populate / Refresh / Capture)

```
Populate Rig from JSON
  -> LoadConfig()  [always reloads source]
  -> GetOrCreateRig()  [finds or creates POIAuthoringRig under PlacementCorrectionAnchor]
  -> if rig has children: prompt "Clear and repopulate?"
  -> load prefab from _prefabPath
  -> foreach poi:
     -> POIPositionResolver.TryResolvePosition(poi, anchors, out localPos)
     -> PrefabUtility.InstantiatePrefab(prefab, rig)
     -> instance.name = poi.id  [child name == poi.id is the binding key]
     -> instance.transform.localPosition = localPos
     -> anchor.Initialise(poi) + markerView.Initialise(...)
  -> SelectRigObjects()  [Selection.objects for editor focus]
```

```
RefreshRigVisuals (called on config mutation with refreshRigOnChange=true)
  -> GetExistingRig()  [no-op if no rig]
  -> configure palettes: CategoryPalette / BadgeCategoryPalette / StatusRamp /
     MarkerHierarchyResolver
  -> parse wall-level settings: shape, outline mode, badge mode (with legacy fallback)
  -> foreach poi:
     -> child = rig.Find(poi.id)  [name-based binding]
     -> anchor = child.GetComponentInChildren<POIAnchor>() ?? AddComponent<POIAnchor>()
     -> anchor.Initialise(poi)  [updates runtime data, NOT position]
     -> markerView.Initialise(...)  [updates visuals from current config]
```

RefreshRigVisuals does NOT move marker positions -- it only updates visuals. Position
changes require re-population or manual Transform editing + Capture Positions.

```
Capture Positions
  -> GetExistingRig()
  -> build Dictionary<string,Transform> of rig children by name
  -> foreach poi:
     -> if sceneObjects.TryGetValue(poi.id):  [only matched POIs]
        -> localPos = correctionAnchor.InverseTransformPoint(marker.worldPosition)
        -> poi.captured_position = { x, y, z }
        -> poi.has_captured_position = true
        -> poi.captured_position_source = "workflow_a_editor"
        -> poi.captured_position_timestamp = DateTimeOffset.UtcNow
     -> else: skipped++
```

```
Clear Rig
  -> GetExistingRig()
  -> if no children: "Nothing to clear" dialog
  -> IsRigInSyncWithConfig() check:
     if outOfSync: "N marker(s) differ. Clear anyway?" [save/clear or cancel]
  -> for each child: Undo.DestroyObjectImmediate(child)  [undoable]
```

### 3.5 Icon Library Flow

- `_defaultIconLibrary` auto-loaded from `DefaultIconLibraryPath` on OnEnable and LoadConfig
- `_wallIconLibrary` auto-created (copies from defaults) if not found via
  `CreateOrAssignWallIconLibrary`
  - Lives in wall's `Resources/` folder so runtime can load it
  - Path auto-populated to `_config.marker_icon_library_resources_path`
  - Never shown as ObjectField -- managed internally
- `ResolveSpriteForKey(key)`: wall library first, then default library
- `AssignSpriteToLibraryAndGetKey(sprite, name)`: registers sprite to wall library,
    returns key

---

## 4. UI Structure (OnGUI Layout)

```
OnGUI()
  -> HandleUndoShortcuts()        [Ctrl+Z / Ctrl+Z+Shift / Ctrl+Y]
  -> DrawToolbar()                [title label]
  -> DrawTopConfigAndActions()
  -> DrawSyncAndWarnings()
  -> if _config == null: HelpBox "No config loaded" + return
  -> BeginScrollView
     -> DrawFramedFoldout("Global Scene Options", blue border)
        -> DrawConfigMutationScope(DrawGlobalSceneOptions, refreshRigOnChange: true)
     -> DrawFramedFoldout("Specific Marker Options", blue border)
        -> DrawConfigMutationScope(DrawSpecificMarkerOptions, refreshRigOnChange: true)
     -> EndScrollView
```

DrawFramedFoldout renders a colored left-border (2px accentColor bar) inside a
GUI.skin.box to visually nest global vs specific sections. Inner foldouts
(Marker, Badge, etc.) use plain EditorGUILayout.Foldout without the border.

### 4.1 Top Config and Actions

```
Scene Configuration (foldout)
  - Config path     [______] [Browse...]
  - Streaming path  [______] [Browse...]
  - Marker prefab   [______] [Browse...]
  - Correction anchor  [ObjectField]  <- must be PlacementCorrectionAnchor
  - Wall mesh (ref)  [ObjectField]

[Load Config]          [Populate Rig from JSON]
[Save All to JSON]     [Copy to StreamingAssets]  [Undo]  [Redo]
```

Button coloring:
- Clear Rig turns orange when rig has children
- Save All to JSON turns yellow when `_hasUnsavedChanges`
- Undo / Redo are disabled via `EditorGUI.DisabledScope` when no history available

DrawPathRow provides a text field + "..." button that opens OpenFilePanel,
converts to Assets-relative path via `AbsoluteToAssetPath`, rejects paths outside project.

### 4.2 Sync and Warnings

DrawSyncAndWarnings shows live status:
- Correction anchor missing: HelpBox warning
- Rig has children: HelpBox warning
- Unsaved changes: HelpBox warning
- Rig sync: green or red status label with out-of-sync count

### 4.3 Global Scene Options

Four foldout sections, each wrapped in DrawConfigMutationScope:

**Marker**: Background shape popup (Circle/Rounded Square/Hexagon/Diamond/Star/None),
icon library selector + category symbol table.

**Badge**: Enable toggle, badge background shape popup, badge category symbol table.
Badge section only if `marker_use_badge` is true.

**Outline**: Enable toggle, outline color popup (Gold/Same Hue/Free Colors),
outline level table with auto-spaced pct values. Remove uses trash icon.

**Effects**: Pulse defaults (amplitude, period), Sun defaults (period, stagger,
alpha channels, tint), Accent defaults (size, alpha, scales, breathe, beacon scales, tint).
HelpBox with new-effect extension instructions.

**Hierarchy Levels**: Table with columns for key/label/size_cm/show_label/
sun_effect/accent_effect/pulse/rotate_contour/reveal_delay_s/reveal_duration_s.
No cap on rows. **Pending additions, not yet built**: an explicit `priority`
int column (`_2.3_Marker_Hierarchy.md` §2-3, corrected from an earlier
draft that derived priority from row position instead) and a
`search_keywords` column (`_2.6_Select_Filter_Search.md` §6) — update this
line once either lands.

#### Outline free_colors

`free_colors` is editor-only (not a runtime `MarkerOutlineMode` enum value). When
selected, the outline level table shows per-level color fields directly. At runtime,
StatusRamp.Configure reads `color_hex` from each level row. The editor normalizes via
MarkerVisualsParser.TryParseOutlineMode with gold fallback.

#### Outline pct auto-spacing

RecomputeLevelPercentSpacing distributes `pct = (100 / (count-1)) * i` across levels.
Called on add, remove, and load. No cap on number of levels.

#### Defaults seeding

LoadConfig seeds empty sub-lists from editor-only helpers:
- DefaultCategoryStyles.Create() -> 6 heritage categories
- DefaultBadgeCategories.Create() -> 4 damage levels
- DefaultOutlineLevels.Create() -> 4 destruction levels
Seeded only when genuinely empty (not on every load).

### 4.4 Specific Marker Options

Per-POI list with foldout per POI (titled "N. Name (id)"). Each POI foldout has:

**Position** (foldout): X norm / Y norm sliders (0..1), "Use captured position" toggle.
If checked: X / Y / Z float fields for the CapturedPosition struct.

**Marker Style** (foldout): Name text field, Category dropdown (from category_styles),
Hierarchy Level dropdown (from hierarchy_levels + "(none)"), "Use Custom Symbol" toggle.
Custom symbol shows Sprite ObjectField + preview -- overrides just the icon, not color/ring/badge.

**Badge Style** (foldout, only if marker_use_badge): Badge category dropdown.

**Outline** (foldout, only if marker_outline_mode != "none"): "Has status" toggle.
If checked: Status level popup (from outline_levels, showing label + pct) or Status % slider
(if no outline levels defined). "Status unknown" toggle triggers ApplyUnknownStatusDefaults.

#### Unknown status

ApplyUnknownStatusDefaults searches `_config.outline_levels` for key="unknown" and assigns
its key+pct. Also searches `badge_categories` for key="unknown_damage" and assigns it.
This is an editor convenience -- runtime rendering is driven by status_level_key + badge_category strings.

#### Dropdown patterns

Category/BadgeCategory/HierarchyLevel dropdowns: collect available keys from config tables,
build a string array, find current index, Popup, write back the key. Empty selection means
"fall through to framework default" (not an error).

### 4.5 Symbol Table Renderer

DrawSymbolTable<T> is generic over entry type, driven by delegates:
- getPrimaryLabel / setPrimaryLabel (category or key)
- getIconKey / setIconKey
- getColorHex / setColorHex
- getDetails / setDetails
- showColorPicker (bool gate for color column)

Columns: primary | Details(...) | Symbol(Sprite ObjectField) | Choose(existing) |
Color(swatch+hex+pick) | Remove(trash)

**Pending addition, not yet built**: a `search_keywords` column
(`_2.6_Select_Filter_Search.md` §6, and a matching one on `_2.3`'s
separate Hierarchy table) — update this line once it lands, same as the
Hierarchy table's own pending `priority` column note below.

- Sprite ObjectField auto-registers to wall library on change via AssignSpriteToLibraryAndGetKey
- "Choose" opens ExistingSymbolPickerPopup (wall + default icons only, not full project)
- "Details" opens EntryDetailsPopup (persisted notes, not read-only help)
- Color swatch + hex field: DrawColorSwatchAndHex + TryParseHexColor for validation

### 4.6 OnSceneGUI

Draws cyan handles in the Scene view for:
- Each calibration anchor's position
- Each POI's resolved (pre-capture) position from POIPositionResolver

Does NOT edit positions -- scene objects must be moved via normal Transform tools,
then Capture Positions writes them back.

---

## 5. Data Types Reference

### 5.1 WallConfigData (Runtime)

Top-level config. Key fields:
- `wall_id` / `wall_name` / `immersal_map_id`
- `marker_shape` (string, parsed by MarkerVisualsParser)
- `marker_style` (LEGACY string: gold/same_hue/badge -- kept for backward compat)
- `marker_outline_mode` (string: gold/same_hue/free_colors/none)
- `marker_use_badge` (bool)
- `marker_icon_library_resources_path` (string, for runtime Resources.Load)
- `badge_shape` (string)
- `category_styles`: List<CategoryStyleEntry> -- {category, icon_key, color_hex, details}
- `badge_categories`: List<BadgeCategoryEntry> -- {key, label, icon_key, color_hex, details}
- `outline_levels`: List<OutlineLevelEntry> -- {key, label, pct, line_style, color_hex}
- `hierarchy_levels`: List<HierarchyLevelEntry> -- {key, label, size_cm, show_label,
  sun_effect, accent_effect, pulse, rotate_contour, reveal_delay_s, reveal_duration_s}
- `effect_defaults`: EffectDefaults -- {pulse, sun, accent} with amplitude/period/alpha/tint
- `calibration_anchors`: List<CalibrationAnchor> -- {id, x_norm, y_norm, captured_position}
- `pois`: List<POIData>

### 5.2 POIData (Runtime)

- `id` / `name` / `x_norm` / `y_norm`
- `category` (string -> CategoryPalette)
- `badge_category` (string -> BadgeCategoryPalette)
- `has_captured_position` (bool -- the real switch, not null check)
- `captured_position` (CapturedPosition struct: x, y, z)
- `captured_position_source` / `captured_position_timestamp`
- `has_status` / `status_pct` / `status_unknown` / `status_level_key`
- `hierarchy_level_key`
- `has_custom_symbol` / `custom_symbol_key`

### 5.3 CapturedPosition

Struct with float x, y, z. Always paired with `has_captured_position = true` when written.
POIPositionResolver checks `has_captured_position` before using -- never just `!= null`.

---

## 6. Testing

Location: `Assets/Framework/Tests/Editor/POIAuthoringToolWriteBackTests.cs`

Tests are **pure data tests** -- no scene, no EditorWindow instance, no Unity dependencies.
They construct `WallConfigData` in code and verify field read/write behavior that the
Capture Positions flow operates on.

| Test | Verifies |
|------|----------|
| `CapturePositions_OnlyUpdatesMatchedPOIs` | 3 POIs in config, 2 scene objects placed. Only 2 matched POIs get captured_position; 3rd untouched |
| `CapturePositions_PartialCapture_DoesNotAffectOthers` | Capturing one POI leaves others' fields unchanged |
| `CapturePositions_OriginPosition_IsStored` | A POI at (0,0,0) is stored without being treated as "no position" (distinguishes from null) |
| `CalibrationAnchors_HaveRequiredFields` | Every CalibrationAnchor has id, x_norm, y_norm, captured_position |

Test pattern: `CreateTestConfig()` factory builds minimal WallConfigData with 1
calibration anchor + 3 POIs. Tests simulate CapturePositions logic inline.

**Boundary note:** Tests validate the data contract, not the EditorWindow UI.
UI rendering is verified via manual inspection; data logic via tests. This is the
correct split -- the window is a shell with no serializable business logic.

Run via Unity MCP: `run_tests` with mode=EditMode. Acceptance: **zero failed tests**.

---

## 7. Gotchas

1. **Rig child naming is the binding key.** Each rig child is named `poi.id`.
   `RefreshRigVisuals` does `rig.Find(poi.id)`. Rename a POI's id -> repopulate rig.

2. **Undo uses two separate systems.** Rig objects use Unity `Undo`
   (InstantiatePrefab auto-registers, `Undo.DestroyObjectImmediate`). Config field edits
   use JSON snapshot list (`_configHistory`). Ctrl+Z / Ctrl+Y for config;
   `Edit > Undo` for rig objects. Do not mix them.

3. **`free_colors` is editor-only.** Not a runtime `MarkerOutlineMode` enum value.
   Editor normalizes it; runtime reads per-level `color_hex` via `StatusRamp.Configure()`.
   Do not add to `MarkerOutlineMode` enum.

4. **`marker_style` is legacy.** Modern fields: `marker_shape` + `marker_outline_mode`
   + `marker_use_badge`. `RefreshRigVisuals` has a fallback calling
   `MarkerVisualsParser.TryParseStyle` to derive outline+badge from legacy enum.

5. **`has_captured_position` is the real switch.** JSON can serialize a nullable struct
   as `captured_position: {}`. Both `POIPositionResolver` and `POIData` check the bool
   flag. Always set both fields together.

6. **Icon library must be in `Resources/`.** `AssetPathToResourcesPath` finds `/Resources/`.
   If wall library isn't under `Resources/`, `marker_icon_library_resources_path` is null
   and runtime falls back to defaults. Window shows HelpBox warning but does not block.

7. **Wrap field edits in `DrawConfigMutationScope`.** Without it, undo (Ctrl+Z) won't
   capture the change, `_hasUnsavedChanges` won't be set, and the rig won't refresh.

8. **OnSceneGUI draws handles but doesn't edit.** Scene view shows cyan handles for
   anchor + POI positions. Edit via normal Transform tools + Capture Positions.

9. **Defaults seeded on first load only.** LoadConfig seeds empty lists from editor-only
   helpers. Do not seed non-empty lists -- overwrites developer choices.

10. **`POIPositionResolver.TryResolvePosition` can fail.** If a POI without captured
    position can't resolve, PopulateRig logs a warning and skips it. Check logs if
    markers are missing from the rig.

---

## 8. Extension Points

To add a new marker effect type (see HelpBox in Global Effects for canonical list):

1. Create a class inheriting `MarkerEffect` (Runtime)
2. Add a flag to `MarkerEffectFlags` enum
3. Update `MarkerVisualsParser.ParseEffectFlags` to parse the new string
4. Wire it in `MarkerView.ApplyLabelState` or the appropriate init path
   **[resolved — confirmed by direct code read while grounding
   `_2.5_Displacement.md`: the `_2.3` rename from `ApplyHeroState` to
   `ApplyLabelState()` did happen. The stale name is fixed here; no
   further verification needed on this point.]**
5. Add a parameter to `HierarchyLevelEntry` (per-POI effects are driven by hierarchy
   level, not a per-POI effect foldout -- that was removed)
6. Add a gallery entry in `MarkerGalleryDefinitions`
7. Add a default row in `EffectDefaults` if it has tunable parameters
8. Add UI sliders in `DrawGlobalEffectsSection` for the new defaults