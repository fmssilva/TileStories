# _2.3.1 Vision Handout - Marker Hierarchy close-out (Tier-1)

## Status: DEFERRED deliverable from the 2.3 close-out run. For a vision-capable agent only.

This is the visual-verification handout for the deferred Vision Tier-1 of the
_2.3 Marker Hierarchy domain (spec cross-ref: work-plan.md Block E / spec sec 16
L759 + spec sec 15 Tier-1). A vision-capable agent must execute it; this agent
(text-only) cannot interpret screenshots.

Ground rule (spec sec 4.5 Tier-1): reject holistic "looks ok" answers. Every claim
below is a numbered, falsifiable yes/no item. Where a question is [no], describe
exactly what you saw and the matching gallery marker name (Hierarchy window
name `Gallery_{Group}_{Label}`). Pass gate: every item [yes] AND console
`[MarkerGalleryDiag][FAIL]` count == 0.

---

## 0. Prerequisites and scene setup

- Unity Editor open on the `TileStories` project
  (`C:/Users/franc/Desktop/TileStories/TileStories`).
- LivingRoom config synced Apps <-> StreamingAssets (close-out verified both
  MD5 = 6A8896ED52CB8E631EFD2ECD1E697071). Needed only for E4; the gallery
  itself (E1-E3) uses fabricated data and needs no sync.
- No AR device and no Immersal localisation required (Tier A mock applies).
- The gallery scene is EXCLUDED from Build Settings and never ships.

---

## 1. What to open

Scene (never shipped):
  Assets/Dev/MarkerGallery/MarkerGalleryScene.unity

Source files that drive every claim below:
- Assets/Framework/Runtime/DevTools/MarkerGalleryDefinitions.cs
    Data. `Entries` = `List<MarkerGalleryEntry>` built by `BuildEntries()`,
    grouped into the 11 groups in section 2. `BuildHierarchyLevels()` defines
    the 5 hierarchy levels (table in section 4).
- Assets/Framework/Runtime/DevTools/MarkerGalleryHarness.cs
    Spawns every entry through the REAL marker path (same `POI_Marker.prefab`
    + `MarkerView.Initialise` that `WallSession` uses -- Harness lines 62-79).
    Diagnostics ON: `enableDeepDiagnostics=true`, `forceDiagnosticsAllEntries=true`
    (Harness lines 23-24); emits `[MarkerGalleryDiag]` lines and `Debug.Assert`
    `[FAIL]` lines for checkable visual invariants.
- Assets/Framework/Runtime/UI/Markers/POI_Marker.prefab

Hierarchy levels (size strictly decreasing; only level_1 shows a label):
  level_1: 20 cm, label "1", sun_circles + ring_pulse, contour rotates
  level_2: 15 cm, no label, sun_contours, contour rotates
  level_3: 10 cm, no label, simple_sun, contour rotates
  level_4:  5 cm, no label, beacon, contour rotates
  level_5:  2 cm, no label, none, contour rotates

---

## 2. The Gallery (per-group checklist)

The harness lays out one marker per `MarkerGalleryEntry`, one horizontal row per
group. Each marker's GameObject name is `Gallery_{Group}_{Label}` (Harness line 67),
so match rendered markers to this table via the Hierarchy window.

Count cross-check: after Load + Play, read the console. Harness prints
`[MarkerGallery] Spawned {N} entries across {G} groups.` (line 59). Count
in-scene markers named `Gallery_*` and confirm G == 11 and markers == N.
Any missing instantiation is a hard fail. Also: zero `[MarkerGalleryDiag][FAIL]`
lines (they cover: null sprite, sun-disc corners not transparent -> square not
circular, missing expected sun-effect image).

### Group 1 -- Sanity
- Entry: "Single circle, civic, no status" (OutlineGold circle, civic icon, no status).
- Item 1.1: Is it a filled gold circle with a centered civic icon and NO status ring? [yes/no]
- Item 1.2: Is it a perfect circle (cornerAlpha ~0 at all 4 corners)? [yes/no]

### Group 2 -- Category (Step 1)
- 10 entries. `furniture` is overridden (definitions L39-42) to colour #8033CC.
- Item 2.1: Are there exactly 10 category markers in this row? [yes/no]
- Item 2.2: Is `furniture` purple #8033CC, distinct from the other 9 categories? [yes/no]
- Item 2.3: Does `art` (no KnownIcons entry) render a deterministic hash colour + fallback icon (not a missing-sprite quad)? [yes/no]
- Item 2.4: Do all 10 have a circle silhouette (status is off here)? [yes/no]

### Group 3 -- Shape (Step 2)
- One entry per `MarkerShape` enum value (Circle, Diamond, Hexagon, RoundedSquare, Star, None). OutlineGold, civic, no status.
- Item 3.1: Do all 6 silhouettes render, each matching its name (Diamond=diamond, Hexagon=hexagon, Star=star, None=bare icon)? [yes/no]
- Item 3.2: Is the civic icon centered inside each silhouette? [yes/no]

### Groups 4..6 -- Status - {OutlineGold, OutlineSameHue, Badge} (Steps 3-5)
- 8 entries per style: pct 0/20/40/60/80/100 + "No status axis (baseline)" + "Unknown fate (? badge)".
- Item 4.1 (OutlineGold): as pct rises 0->100, does the gold contour thicken/fill progressively darker, fully filled at 100%? [yes/no]
- Item 4.2 (OutlineSameHue): does the ring stay the icon hue while fill/darkness changes with pct? [yes/no]
- Item 4.3 (Badge): does the marker fill with the category colour up to pct, leaving a ring for the remainder? [yes/no]
- Item 4.4: Does the baseline entry (no status axis) show NO ring/fill-progress? [yes/no]
- Item 4.5: Does "Unknown fate (? badge)" render the UnknownColour (distinct from every known level colour; see StatusRamp.UnknownColor)? [yes/no]

### Group 7 -- Status+Shape (extra matrix)
- 12 entries: OutlineSameHue and Badge at 60%, one per shape (incl. None, Star).
- Item 5.1: Does 60% ring/badge render correctly on every non-Circle shape (Diamond, Hexagon, RoundedSquare, Star, None)? [yes/no]
- Item 5.2: On `None` shape, is only the status ring visible (no backdrop) with the civic icon centered? [yes/no]

### Group 12 -- Override
- 2 entries: `furniture` (#8033CC + fallback icon) and `art` (hash colour + fallback icon).
- Item 7.1: Does `furniture` override to purple #8033CC (not default civic colour)? [yes/no]
- Item 7.2: Does `art` show a deterministic hash colour + fallback icon? [yes/no]

### Group 13 -- Contour Rotation
- 4 entries: OutlineGold static, OutlineGold rotating (level_5), OutlineSameHue static, OutlineSameHue rotating (level_5). Circle, 40%, has_status=true.
- Item 8.1 (static): contour a fixed dash ring (no rotation)? [yes/no]
- Item 8.2 (rotating): does the SAME ring visibly ROTATE around the symbol? [yes/no]
- Item 8.3: Are rotating entries visibly SMALLER (level_5 = 2 cm) than static (Fallback = 12 cm)? [yes/no]

### Groups 14..16 -- Accent - {Ring Pulse, Simple Sun, Beacon}
- Each accent group has 2 entries: "No hierarchy" (Fallback 12 cm) and
  "With hierarchy level_1" (20 cm). The accent = the group's effect flag.
- Item 9.1 (Ring Pulse): thin contour, breathing (alpha/size pulse)? [yes/no]
- Item 9.2 (Simple Sun): filled disc behind the symbol that breathes? [yes/no]
- Item 9.3 (Beacon): thin contour that grows + fades in a sawtooth? [yes/no]
- Item 9.4: For EACH accent, does the effect render on BOTH "No hierarchy" AND
  "With hierarchy level_1" (i.e. effects are not hierarchy-gated)? [yes/no]
- Item 9.5: Is "With hierarchy level_1" visibly larger (20 cm) than "No hierarchy"
  (12 cm Fallback)? [yes/no]

### Group 17 -- None Background
- MarkerShape.None, civic icon, no status. Icon-only, no backdrop.
- Item 10.1: Is there NO backdrop behind the icon (just the icon)? [yes/no]
- Item 10.2: Is the civic icon still visible and centered? [yes/no]

### Group 18 -- Custom Symbol Override
- "POI with override", civic, OutlineGold Circle, no status. The harness does NOT
  set `has_custom_symbol` (the MarkerGalleryEntry struct has no field for it), so
  this entry renders a plain OutlineGold Circle -- override NOT exercised by gallery.
- Item 11.1: Confirm this marker is a plain gold circle (override NOT active),
  matching Item 1.1. [yes/no]

---

## 3. Structural corroboration (Tier 0 -- check first)

- Console: zero lines containing `[MarkerGalleryDiag][FAIL]`.
- Console: `[MarkerGallery] Spawned {N} entries across 11 groups.` present (Harness L59).
- EditMode + PlayMode suites already green in close-out (EditMode 127/127, PlayMode 31/31 incl. MarkerGalleryTests).
- Vision Tier-1 is the human/vision confirmation pass; it corroborates the items above, it does not replace automated asserts.

## 4. Hierarchy-level visual claims (only level_1 & level_5 are gallery-keyed)

The gallery does NOT contain a single row of all five levels. The full 5-level
monotonic ramp (20>15>10>5>2 cm) + per-level effect ramp is proven by Tier-0 unit
tests:
- HierarchyLevelSizeRangeTests (Editor)
- MarkerViewRuntimeTests.HierarchySize_RectRatioScalesBySizeCmAndIsMonotonic
  (PlayMode; spec §16 L773-774, "RectTransform-ratio asserts")

Gallery-keyed hierarchy entries: level_1 (Accent "With hierarchy level_1", 2
entries) and level_5 (Contour Rotation "rotating", 2 entries). Fallback (12 cm
grey) = Accent "No hierarchy" + Contour Rotation "static".
- Item 12.1: Is each level_1 marker the LARGEST in its row (20 cm) and labeled "1"? [yes/no]
- Item 12.2: Is each level_5 marker the SMALLEST (2 cm) and unlabeled? [yes/no]
- Item 12.3: Is each Fallback (12 cm) medium grey, unlabeled? [yes/no]
- Item 12.4: Does level_1 show sun_circles + ring_pulse, while level_5 shows NO effect? [yes/no]

## 5. E2 -- Edit-Mode authoring resize (zero Play Mode)

1. Do NOT Enter Play Mode. Window -> Tile Stories -> POI Authoring.
2. Load `Apps/LivingRoom/config.json`.
3. In the Hierarchy table, change one POI's "Hierarchy Level" from `level_5` to `level_1`.
4. Does the Scene-view marker resize immediately (OnValidate -> MarkerLayout.Apply)
   from 2 cm to 20 cm and gain a label "1"? [yes/no]
5. Revert to `level_5`. Returns to 2 cm, label hidden? [yes/no]
6. Select the level row and click "-" while a POI references it. Does the marker fall
   back to 12 cm grey circle, no label, no ring? [yes/no]
7. Did the change persist to `config.json`? (Save All to JSON first if you edited.) [yes/no]

## 6. E4 -- Phase B real LivingRoom config (18 POIs)

1. Populate rig from the real LivingRoom config.json (18 POIs, 5 hierarchy levels).
2. Item 13.1: Exactly 18 markers spawned (one per POI)? [yes/no]
3. Item 13.2: Every POI's `hierarchy_level_key` resolves to 1 of 5 levels (no 12 cm Fallback)? [yes/no]
4. Item 13.3: Five sizes appear strictly decreasing (20/15/10/5/2 cm)? [yes/no]
5. Item 13.4: Markers clustered per POI, no overlap mess (MarkerOverlapResolver offsets)? [yes/no]
- Item 13.5: Console `[POI] spawned 18/18`, ZERO errors/warnings during spawn? [yes/no]

## 7. Reporting format

For each numbered item (1.1 .. 13.5), return: item number, a single [yes]/[no],
and if [no]: a 1-2 sentence description of what you saw + the matching
`Gallery_{Group}_{Label}` name from the Hierarchy window. No prose summary.
Pass gate: every item [yes] AND console `[MarkerGalleryDiag][FAIL]` count == 0.
